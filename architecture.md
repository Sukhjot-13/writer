# Architecture — Writer App

> **Project root:** `/Users/sukhjot/Desktop/untitled folder 2/writer-app` — Next.js (App Router, TypeScript, Tailwind v4) writer/practice app per `docs/writer_app_requirements.md` (v1.4, FR-1…FR-50) and `docs/Plan.md`.
> **Status:** All milestones **M1–M7 DONE (2026-08-10)** — M1 editor + conversion + PDF + library · M2 Q&A blocks + practice controls · M3 DeepSeek + question import + copy/paste · M4 instructions management (edit/save/reset/history FR-21/22, per-document snapshots + conversion toggle FR-23, TOKENS validation FR-47, version in status bar FR-28) · M5 polish + storage + parse-back (drag-reorder, Enter-split/Backspace-merge FR-3, tags FR-5/18, backup ZIP, HTML→blocks FR-41, MongoDB + Blob FR-44) · UI polish pass (grouped two-row toolbar, Geist font applied, light-only theme, de-jargoned labels). **M6 REDESIGN (2026-08-10):** home page is a dashboard (`/` lists documents + "New document"; editor moved to `/doc/[id]`) · AI conversion returns **editable structured blocks** (template/offline mode deleted; `lib/structuring.ts` canonical JSON-array parser; one conversion, then local edits re-render instantly) · AI enrichment for ALL text (paragraphs get translation/analysis/vocab/expressions) · questions inside paragraphs become `qa` blocks, provided answers kept, never invented · clean worksheet cards (question + ONE reference answer field, extras behind "＋ chips") · **Practice master key** (questions-only view, "My answer" boxes, Check reveals model answers side-by-side, answers saved separately, Reset practice) · **on-demand preview + PDF** (full-screen PreviewSheet via stateless `POST /api/preview`; `POST /api/documents/[id]/pdf` with `{ doc, variant }` — no convert-first gating, `canDownloadPdf`/`previewStale` removed) · **three PDF variants** (`full` / `questions` / `my-answers`) · test generator planned-only (see suggestions.md). **DESIGN PASS 2 (2026-08-10, user feedback):** new **`essay` block type** (a continuous 2–3+ paragraph passage = ONE block with ONE shared translation/analysis/vocab set and ONE practice answer field — fixes per-paragraph enrichment and per-paragraph answer boxes) · **structure frozen in practice** (no add, no per-block controls/tags/heading level, no drag) · **split add control** (`+ <last-chosen>` direct click, ▾ caret opens the full menu; last choice remembered in localStorage) · **super-subtle enrichment chips** (opacity-0 hover-reveal — 99% AI-generated) · **Enter no longer splits paragraphs** (newline only). **DESIGN PASS 2 round 4 (2026-08-10, user pasted the original spec):** the preview sheet now renders at the SPEC's SCREEN size — base 11.5px (not the print 10.5px), 18mm page margins (not 14mm) — while `@media print` keeps true print behavior; French prose keeps its 1.15em lead (~13.2px) with the English translation secondary at 0.9em (~10.4px) per the user's choice. **M6 round 2 (2026-08-10, user feature batch):** AI conversion now also reports **spelling/grammar/punctuation corrections** for qa blocks — the AI NEVER edits text; each qa card shows a "Suggested corrections" list with per-row **Apply** (changes ONLY that one, first occurrence) and **✕** dismiss (lib/suggestions.ts pure logic; `Suggestion` type + tolerant parsing through structuring/schemas; instructions Part 1 gains CORRECTIONS + typography rules incl. accents & French spacing) · **PDF page footers** `1/7` style on every page of all three variants (react-pdf `render` prop + `lib/pdf-labels.ts`) · **auto-grow everywhere** (components/AutoGrowTextarea.tsx wraps lib/auto-grow.ts — Q&A answer/analysis/practice fields, ParagraphFields analysis, practice paragraph/essay answers, essay paragraphs, the three paste modals; grows on every render so AI imports and suggestion Apply re-measure). **M7 (2026-08-10, user feature batch):** **Focus mode** toggle in the toolbar (next to Practice) — shows ONLY the main content (question + answer; paragraph/essay text); translations/analysis/vocab/expressions/suggestions all hidden (same plumbing pattern as practiceMode: Editor state → Toolbar → BlockList → Block → QaBlockForm/ParagraphFields) · **Q&A field order** question → translation → analysis → answer EVERYWHERE (editor, HTML template, PDF — user: "it should show question then translation then analysis then answer") · **Copy dialog = exactly two options** — "For sharing" (selective plain-text copy with toggles) and "For AI" (one-click instruction + serialized content for another AI to restructure; its JSON response pastes back via Paste blocks (AI) — the app itself makes no AI call) · **copyable instructions above the JSON + HTML paste boxes** (shared `InstructionCopyBox`; Paste questions stays as-is per user) · **home = library** (`/library` redirects to `/`; Writer brand icon+text now links home) · **preview field toggles** (per-field + overall "All extras" — translations/analyses/vocab/model answers for qa + paragraph/essay; headings, questions and paragraph text are main content, never hidden) · **InstructionsEditor crash fix** (guarded all 4 `entry.content` sites — history entries without content no longer throw at line 211). **M7 round 2 (2026-08-10, user feedback batch):** **Focus mode is DEFAULT ON** · **essay `heading` field** (optional, never forced — instructions/prompt/paste-blocks instruction updated; renders above the passage in template + PDF; practice mode shows ONLY the heading as the writing prompt — the passage is the task, not reading material; parse-back restores it from `.block-essay-heading`) · **Copy dropdown reduced to ONE "Copy…" button** (the three FR-39 items removed — AI-instruction copies live in the paste sections; `onCopyPrompt`/Editor `copyPrompt`/`copyToClipboard` helper and the `/api/export/prompt` route deleted) · **paste textareas capped** (`max-h-[45vh]` + `overflow-y-auto` in AutoGrowTextarea — long pastes scroll instead of pushing the modal out of the window) · **preview toggles fixed** ("All extras" now checked = everything VISIBLE — was inverted; stale-closure fixed by passing the new hidden values straight to the POST) · **response label moved ABOVE the answer** in template + PDF (it labels the answer area). Build green; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (pre-existing deferred), M6 24/24, M7 19/19. **M7 round 4 (2026-08-10, user feedback batch):** **View ▾ dropdown REMOVED** — the preview sheet's field toggles cover hide/show-all (render-time, no data mutation), so `VisibilityCounts`/`setAllQaFlags`/the 4 bulk-flag props are deleted (counts stay for the status bar); **practice answers NEVER render in the preview** (`showUserAnswers` flag in the HTML template, default OFF — the preview/display PDF omit userAnswer for qa/paragraph/essay; only the server-side my-answers PDF variant and explicit opt-in show them; a future "review of the practice area" option will flip the same flag); **checking answers in practice for paragraphs/essays no longer shows the English translation** — the checked state is a purely visual emerald tint on the answer box (French-first practice); **Focus checkbox inverted to an UNCHECKED "Detailed" toggle** (focus mode IS the default — `detailed: false`); **toolbar UI tidied** (grouped actions with border dividers, shared `TogglePill`, Practice gets an emerald active tint, quiet "Reset practice" text link replaces the View dropdown's only practice-only action); **Instructions moved to the home screen** (editor toolbar nav links removed — the brand icon is the only home link); **editor column widened** `max-w-2xl → max-w-4xl` (user: "a lot of blank space around the text fields on laptop"). Build green; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (pre-existing deferred), M6 24/24, M7 29/29. **M7 round 5 (2026-08-10, user feedback batch):** **👁/🙈 hide toggles REMOVED from the question translation and the answer** (user: "remove the view or hidden togle from question translation and answer as it is now in preview" — the preview sheet's per-field checkboxes cover visibility; the legacy `hideTranslation`/`hideModelAnswer` flags stay in the data model and the renderers still honor them for older documents); **question translation renders BELOW the question** in its own `<p class="qa-question-translation">` (was an inline `<em>` inside `.qa-question-text` — same line; user: "it is showing in same line as question it should be below it"; PDF mirrors with a `questionBody` column View; parse-back recovers the new element with the em-in-question fallback for older exports); **Answer label (responseLabel) field REMOVED from the editor** (user: "remove answer label field as it is not going to change at all so why there is option for user to write the reponse tag" — it is always "RÉPONSE"; the renderers keep using the stored value, default "RÉPONSE", and the schema/prompt/AI output shape are unchanged). Build green; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (pre-existing deferred), M6 24/24, M7 30/30. **M7 round 6 (2026-08-10, user feedback batch):** **NO browser popups** — deleting a document is now an IN-APP two-step confirm inside the card (Delete → red banner "Delete? JSON/HTML/PDF removed" → Delete/Cancel; user: "when deleting a document it gives a browser pop up i dont want that it should be there somewhere"); the practice "Reset practice" confirm is an in-app banner too (never `window.confirm`); `alert()` failures replaced by an inline error banner; **home shows only the 10 most recent documents** + a "View all N documents in the Library →" link (user: "on home page it shows the recent one… only show 10 there and a link to library"); **a REAL Library page at `/library`** (user: "make a library page it will show all the documents… option for making folder too") — every document + **folders**: `Folder` type + optional `Document.folderId`, folder CRUD on the storage interface (FS: `data/folders.json`; Mongo: `folders` collection), `/api/folders` (GET/POST) + `/api/folders/[id]` (PATCH/DELETE), `PATCH /api/documents/[id]` for moving (folderId null = unfiled; `updatedAt` untouched — moving isn't editing), folder chip bar on the library page with create/rename/delete (deleting a folder UNFILES its documents — never deletes them), per-card "Move to…" select, folder filter chips + "No folder" chip; **editor column widened `max-w-4xl → max-w-6xl`** (user: "there is still a lot of empty space around the paragraph and questions"). Build green; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (pre-existing deferred), M6 24/24, M7 30/30, **M8 23/23** (folders). **M7 round 6b (2026-08-10, user feedback batch — "folder thing is not working… when I click on it and make a document in there it doesn't automatically moves it there… Document card should be the same on the home and the library page"):** the **folder bar and the per-card "Move to…" select now render on the HOME page too** — cards are identical on home and library (home passes the full document list + `limit=10`; `gridSource` slices the grid while `allTags`/`folderCounts` count everything, so "All · N" and folder chips are accurate on home) · **clicking a folder then creating a document files it automatically** — with a folder selected, the bar shows "+ New document in 'name'" (`NewDocumentButton` gains `folderId`; the new doc lands in the folder from its first save — no library detour) · **↻ Regenerate removed from the cards** (the editor still has it) and replaced by a **⬇ download icon** that downloads the document with ALL its fields as `<title>.json` (title/tags/folder/every block incl. translations/analyses/vocab/expressions/practice answers/suggestions + practice settings — `downloadDoc()`; the editor's Download PDF still covers the printable rendering). · **Library link back in the editor's navbar** (user: "add library on navbar too" — the title row links → `/library` next to the Writer brand; the brand still goes home). Build green; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (pre-existing deferred), M6 24/24, M7 30/30, M8 23/23. **M7 round 7 (2026-08-10, user feedback — "when I scroll down, I have to go to the top again if I want to toggle the detailed version… Should we add a floating option to toggle the detailed?" (user chose the floating pill) + "add a togle in the writer page for auto save" + "add a simple toggle for dark mode" + "when it toggle detailed on the writer page it shifts my question as new data is added… then it again shift me on another part"):** **floating Detailed pill** — `components/FloatingDetailedToggle.tsx` (NEW): a fixed bottom-right copy of the toolbar's Detailed toggle that fades in (translate-y + opacity transition) only once the document is scrolled past `SHOW_AFTER_SCROLL = 200`px (the toolbar is out of view), hides again at the top; it drives the SAME `detailed` state via `onToggleDetailed` — no new state, no divergence; `containerRef` prop = the editor's scroll div ref (`scrollRef` in Editor); renders the shared `TogglePill` (now `export`ed from Toolbar — architecture.md already listed it as an export) in a `shadow-lg ring-1 ring-zinc-900/5` shell at `fixed bottom-5 right-5 z-40` (below the preview sheet's z-50 overlay). **Bug fixed the same day (user: "there is no floating togle"):** scroll detection listens to BOTH `window.scrollY` and the container's `scrollTop` — the app-shell height chain (`flex h-full` on the editor root vs `min-h-full` on `<body>`) means the DOCUMENT scrolls at the window level, so a container-only listener never fired; either scroller past the threshold shows the pill · **Autosave toggle** — a `TogglePill` "Autosave" in the toolbar's document-actions group (Convert | Save | Preview | Autosave), ON by default (M6 behavior); the editor's quiet debounced autosave effect is guarded by `autosave` (`if (… || !autosave) return;` + dep) — off = only Save (button / ⌘S) persists, manual saves untouched; the preference is remembered in localStorage `writer-app:autosave` (like `writer-app:add-type` / `writer-app:copy-selection`; loaded in a mount effect with try/catch); when off, the status bar shows an "Autosave off — Save manually (⌘S)" chip · **DARK MODE** — `components/ThemeToggle.tsx` (NEW): a 🌙/☀️ icon button that flips `.dark` on `<html>` and persists `writer-app:theme` ("dark"/"light"); placed in the editor toolbar title row (next to Library), the home header and the library header. The mechanism is a **variable-swap palette** in `app/globals.css` (M7 round 7): every `@theme` token now references a runtime var with the light value as fallback (`--color-zinc-50: var(--zinc-50, #f7f9fb)`, … `--color-white: var(--white, #fff)`), and a `.dark { … }` block (placed after `:root` so the overrides win) re-defines those vars — so EVERY existing component class (bg-white surfaces, zinc text/borders, blue/emerald accents, focus ring, selection) flips with zero `dark:` classes. Dark palette = PURE-BLACK inversion of the spec ramps (M7 round 8, user: "in dark mode, the background should be black so that it saves battery" — OLED/AMOLED pixels are OFF at #000, so the canvas `--bg: #000000` AND the dominant surfaces `--white: #000000` (toolbar/cards/modals/content area) are pure black; only the smallest tints stay near-black for hover/input feedback — `--zinc-50: #0a0e15`, `--zinc-100: #11161f`, borders `--zinc-200: #1c2431`; text `--fg: #e6ebf2`, blue-600 `#3d6da8`, emerald-600 `#3d8a46` — both LIGHTER so they read on black; focus ring `#5b8ac9`, selection `#2a3e63`). Two supporting sweeps: **`text-white` → `text-[#fff]`** (29 sites — button labels on colored backgrounds must stay literal white since `--white` now remaps to the dark surface) and **modal backdrops `bg-zinc-900/40` → `bg-black/40`** (4 modals — zinc-900 inverts to the LIGHTEST ramp in dark, so the 40% backdrop would become a light wash; black works in both). The preview sheet is a sandboxed iframe WITHOUT `.dark` → always stays light (the print spec is light-only by design); `app/layout.tsx` gained a `next/script` (beforeInteractive) that applies the saved theme BEFORE first paint (no flash of light). Semantic ramps (red/amber/rose/orange — banners, confirms, suggestion badges) keep Tailwind defaults; they read fine on dark · **DETAILED-TOGGLE SCROLL-ANCHOR (user: toggling Detailed shifts the view)** — `Editor.toggleDetailed` (M7 round 7): before flipping `detailed`, captures the block row currently ~100px from the viewport top (`elementFromPoint(innerWidth/2, 100)` → `closest("[data-block-id]")` — **`data-block-id={block.id}` added to the block row root in Block.tsx**), records its `getBoundingClientRect().top`, then after the re-render (`requestAnimationFrame` — React has committed) scrolls `window.scrollY + (newTop − oldTop)` so the row returns to the exact viewport position; no-op when the anchor is gone or nothing moved (e.g. scrolled at top). Both the toolbar toggle and the floating pill use it. Content added BELOW the anchor never moves it — only height changes above cause a delta correction. Type-check clean; build green; smoke suites SKIPPED at the user's request. **M7 round 9 (2026-08-10, user feature batch — "add a floating button for dark and light mode too ....top right" + "the questions which are not answered are not easy to spot in light mode … it should be subtle" + "add a field for synonyms recommendation so that it will help in enhancing my vocab"):** **floating theme toggle** — `components/FloatingThemeToggle.tsx` (NEW): fixed `top-5 right-5 z-40` copy of the toolbar's 🌙/☀️ that fades in only once the navbar scrolled out of view (`SHOW_AFTER_SCROLL = 200`, dual window+container scroll listeners like the Detailed pill); renders `ThemeToggle` in a `rounded-full bg-white p-1.5 shadow-lg ring-1 ring-zinc-900/5` shell (bg-white = dominant-surface token, black in dark); **ThemeToggle now stays in sync across instances** — every instance listens for a custom `writer-app:theme-change` event + the `storage` event and re-reads the html class, and `toggle()` dispatches the event, so the navbar and floating buttons never disagree · **subtle "Unanswered" treatment** — practice-mode "My answer" boxes for qa (QaBlockForm) and paragraph/essay (Block.tsx) switch from the default dashed-blue to `border-dashed border-amber-300 bg-amber-50/40` while `userAnswer` is empty, plus a small `Unanswered` text (inherits labelCls's uppercase style, amber-600) next to the "My answer" label on qa cards; typing flips the box back to blue. Subtle by request ("it should be subtle not too much in your face"): soft amber border, whisper tint, no icons. **Amber added to the @theme block in globals.css** (default Tailwind values as fallback; the .dark block does NOT redefine amber so it keeps light-mode values and reads on black) — registering it is what makes the amber *utilities exist*. **Round-9 fix (2026-08-10, user: "the fields where there is no answer i cant see the amber in both light and dark mode" + "still no glow or anything"):** the user meant the **WRITER's Answer field** (a question with no model answer written in the editor) — the SAME subtle amber treatment as practice now applies to the `modelAnswer` textarea in BOTH focus and detailed modes while it's empty (`border-dashed border-amber-300! bg-amber-50/40!` + a small amber `Unanswered` label next to "Answer"; typing flips it back to a plain field). Underneath, two real bugs were fixed: (1) registration order does NOT affect the cascade — Tailwind v4 sorts same-property utilities ALPHABETICALLY by name, so `border-zinc-200`/`bg-white` (z > a, w > a) always landed AFTER the state colors in the generated CSS and amber/blue NEVER showed on `inputCls`-bearing fields (in either mode; the practice "My answer" blue border had the same silent bug since M6; paragraph/essay practice boxes were unaffected — their base string has no zinc border/bg-white); (2) fixed with the **`!` important suffix** on the state classes (`border-amber-300! bg-amber-50/40!` / `border-blue-200!`) — verified in the generated CSS (`.border-amber-300\!` etc. carry `!important`, beating the later non-important zinc/white rules in both light and dark; .dark leaves amber at defaults so #fcd34d + #fffbeb@40% read on pure black). Also (user: "remove the line reference answer… leave empty for a question where there is no answer"): the `Reference answer… (leave empty for a question without a provided answer)` **placeholder is GONE** from the Answer field (focus + detailed modes) — an unanswered field is genuinely empty, the amber + label carry the signal · **synonyms field (vocab growth)** — new block-level `synonyms?: {term,def}[]` on QaContent/ParagraphContent/EssayContent (parallel to vocab/expressions): structuring schemas + `optList` normalization accept it; `BLOCK_FORMAT_SPEC` shapes + `docs/html_instructions.md` enrichment rules + JSON shapes gain `"synonyms":[{"term":"…","def":"…"}]` with a SYNONYMS rule (2–3 REAL French synonyms fitting the context when a basic word is worth enriching; never invent; omit when nothing worth enriching); `serializeQa` emits a `SYNONYMS: term|def; …` marker (round-trips through Copy for AI); editor UI: `+ Synonyms` chip in QaBlockForm and ParagraphFields (auto-reveals when content exists, hide clears data, RowEditor rows term|meaning); print: the vocab grid gains a third **"Synonymes"** column in both `vocabGridHtml` (html-template.ts — colCount drives `one-col`/`two-col`) and `VocabGridPDF` (pdf.tsx); Copy → For Sharing includes `Synonymes : …` under the Vocabulary toggle. **M7 round 9 rename (2026-08-10, user: "change the text convert with ai to something else it dosent suit it" → picked "rethink with ai"):** the toolbar primary button is now **"Rethink with AI"** (busy label "Rethinking…", snapshot checkbox "Rethink with this document's snapshot rules (v…)") — comments/docs in Toolbar, CopyDialog, Editor and the instructions route updated; and the Paste-blocks (AI) `BLOCKS_INSTRUCTION` is now **composed from the shared `BLOCK_FORMAT_SPEC`** (exported from lib/prompt.ts) — the hand-copied paste instruction had drifted: it lacked the synonyms shapes/SYNONYMS rule and the suggestions/CORRECTIONS rules, so an external AI fed via Copy → For AI → Paste blocks would NOT have produced synonyms/corrections while the in-app Rethink with AI did; by construction they can no longer drift (user: "if i ask ai to convert it will it give me suggested synonyms or not?"). **M7 round 10 (2026-08-13, to-do item 2 — "analysis/breakdown point system"):** the user chose **free-text bullet points** — no new field, the existing `analysis` IS the breakdown; lines starting with `- ` (dash + space) render as real lists in both renderers: `lib/html-template.ts` `renderInlineMarkdown` splits on `\n` and groups consecutive bullet lines into ONE `<ul class="point-list">` (prose lines stay outside; inline `code`/`**bold**`/`*italic*` still apply inside points via a private `inline()` helper; CSS `.point-list { margin: 2px 0 0 18px; padding: 0; } .point-list li { margin: 1px 0; }`) and `lib/pdf.tsx` gains a private `bulletText(text)` (bullet lines get a `•  ` prefix — react-pdf has no list element) applied at the 3 analysis sites (qa, paragraph, essay). `docs/html_instructions.md` gains an ANALYSIS POINTS rule (write each distinct point on its own line starting with `- `; mix prose and points freely). The structured `analysisPoints` field alternative is fully designed in suggestions.md but deliberately NOT built (~10 files vs 3). **M7 round 12 (2026-08-13, user Q&A — "why is there still reset to repo button… in test there should not be answers"):** (1) the Instructions reset button is RELABELED **"Discard my edits — restore repo copy"** — item 10's auto-sync already propagates repo changes, so the button's remaining job is throwing away YOUR OWN newer edits (auto-sync never overwrites the newer writer) + snapshotting them to history first; (2) **test mode = practice mode** (the user's conclusion, and the code already agreed: practice hides all answers until Check — the teacher's key). The one gap was that generated test documents opened with every answer visible: the Test generator (both paths — `buildTestDocument` and `POST /api/test`) now sets **`Document.opensInPractice = true`** (types + `documentSchema` optional boolean), and the **Editor's init effect auto-enables practice mode** when the loaded doc carries the flag — a test opens questions-only, Check reveals answers, toggling off returns to normal editing; reloads always start in practice. smoke-m9 grows 18 → 21 (flag on every path / absent on normal docs / schema round-trip). This file is updated after every change; it is the latest and current state of the app.

**Locked decisions (never revisit):** PDF engine = `@react-pdf/renderer` only (no Puppeteer/Chrome anywhere) · design tokens parsed at runtime from the instructions file `TOKENS` block (changing colors = editing `docs/html_instructions.md` only) · storage pluggable (filesystem now, MongoDB + Vercel Blob later) · no auth in v1, `ownerId` seams kept (FR-45) · one-change-one-file (AI → `lib/ai.ts`, PDF → `lib/pdf.ts`, storage → `lib/storage.ts`, design → instructions file, FR-48) · conversion is AI-only and block-based (M6 — preview/PDF render on demand from the current document, no stale-preview gating) · practice answers (`userAnswer`) are private: never sent to the AI, never serialized into prompts.

---

## Runtime layout (FR-17)

```
data/                          # gitignored runtime storage
  folders.json                 # library folders [{id,name,createdAt,updatedAt}] (M7 round 6)
  documents/<id>/document.json # source blocks (editable truth)
  documents/<id>/document.html # generated HTML (saved alongside the doc)
  documents/<id>/document.pdf  # generated PDF
  documents/<id>/instructions.snapshot.md # per-document rules (FR-23)
  instructions/active.md       # editable instructions (seeded in M4)
  instructions/history/*.md    # version history (M4)
```

## Files

### `docs/html_instructions.md` — Structuring & enrichment instructions (single source, FR-47)
- **Purpose:** THE system prompt for AI conversion + the design system. **M6 rewrite:** Part 1 CONVERSION PROTOCOL — how the AI **structures** content (question detection rules, provided answers kept verbatim, never invent answers for unanswered questions, French→English enrichment for qa AND paragraphs, respect HIDE flags, never output `userAnswer`/HTML). **2026-08-10 design pass (user's PDF spec):** Part 2 PRINT-READY DESIGN SYSTEM — the user's own print spec nearly verbatim (GLOBAL STYLE: A4, 18mm margins, Georgia/Times 11.5px, colors #1a1a1a / #1e3a5f / #2c5f2d; TYPOGRAPHY & ELEMENTS; REUSABLE COMPONENTS for general + Q&A blocks; CRITICAL RULES: break-inside avoid, one-column answer boxes, no floats). Ends with the machine-readable `<!-- TOKENS --> … <!-- /TOKENS -->` block: `colors` (mainText, heading, accentGreen, lightBg, highlightBg, border, tableStripe, **vocabBg, rowBorder (NEW 2026-08-10)**, tagBg, tagText, badgeBg, badgeText), `fonts` (base, mono, pdf), `sizes` (base, print, small), `spacing` (pageMargin, printMargin, cardPadding, answerPadding), `radius` (card, badge, tag). **Changing a color = editing this file only.** The renderers (`lib/html-template.ts` + `lib/pdf.tsx`) implement this spec; the editor UI is remapped to the same palette (globals.css) so screen ≈ paper.
- **Functions:** none (data file — parsed by `lib/tokens.ts`).
- **Note (design pass 2, 2026-08-10):** Part 1 CONVERSION PROTOCOL now has an ESSAYS rule — a continuous 2–3+ paragraph passage becomes ONE `essay` block (all paragraphs in its `paragraphs` array), never per-paragraph blocks; each essay gets exactly ONE translation/analysis/vocab/expressions set. New output shape `{"type":"essay","heading":"…","paragraphs":["…","…"],…}` — **`heading` is OPTIONAL (2026-08-10 #5):** include a short title only when the passage has one or clearly suggests one — never invent, never force (practice mode shows the essay heading as the writing prompt). Part 2 stays the user's spec STRUCTURE (byte-checked incl. #eef2f7/#d8dfe8 grid tokens) with two deliberate size corrections from round 3 (user feedback "french text is small"): relative sizes rewritten `rem`→`em` (rem resolves against the 16px root, making English translations bigger than the French base — a spec-wording bug) and a new "Paragraph / essay prose: 1.15em" rule so the French passage leads the page.
- **Note:** the rewrite does NOT propagate to existing `data/instructions/active.md` — at the time the user clicked "Reset to repo file" once. **To-do item 10 (2026-08-13) landed: `syncActiveFromRepo` auto-syncs this file into storage whenever the repo copy is newer, so instructions changes here propagate with no manual step; the /instructions button is now the "discard my edits" escape hatch (relabeled round 12).**
- **Note (2026-08-13, to-do item 2):** Part 1 ENRICHMENT gains an **ANALYSIS POINTS rule** — when an "analysis" has multiple distinct points, write each point on its own line starting with `- ` (dash + space); the app renders them as bullet points (HTML `<ul class="point-list">`, PDF `•`-prefixed lines). Mix prose and points freely; never force points onto a single-sentence analysis. (The renderer changes live in `lib/html-template.ts` `renderInlineMarkdown` + `lib/pdf.tsx` `bulletText`.)

### `docs/writer_app_requirements.md` / `docs/Plan.md` / `docs/suggestions.md` / `docs/architecture.md` / `to-do.md`
- **Purpose:** Requirements spec (v1.4), implementation plan (M1–M5), improvement log, and project-level architecture doc (parent folder scope). Documentation only. **`to-do.md` (project root, 2026-08-13 — decisions recorded + 🧭 NEW-SESSION HANDOFF section at top, build-ready):** the user's task list — 9 items: (1) copy dialog wording — practice answers kept as "My practice answers" with helpers, `Modèle :` → `Réponse :`, qa practice `Réponse :` → `Ma réponse :`, French headings for question/answer translation + grammar note; (2) **DONE (M7 round 10, 2026-08-13 — free-text bullets; details in the status line + git history; removed from to-do per the user)**; (3) DB — user supplies the MongoDB Atlas connection string for `.env.local` (Mongo backend already built, M5; `BLOB_READ_WRITE_TOKEN` only needed lazily for html/pdf file artifacts at Vercel deploy); practice-questions download/preview confirmed covered by the preview's Empty-lines toggle + hidden model answers; users-later via existing `ownerId` seams (FR-45); (4) Detailed-toggle focus retention (`data-focus-id` on textareas, capture/restore focus + caret in the same rAF as the scroll-anchor, fallback to the block's Answer field); (5) the long-planned **test generator**, now specified: Test dialog on library + home — pick documents → Questions/Essays/Both → optional AI/random with counts (each count disabled when its type isn't selected; random average 3–5 questions / 1–2 essays) → new document titled "Test — <date>"; (6) autosave = keep 1.2s debounce + add a page-leave flush (`visibilitychange`/`pagehide`) — no interval setting; a later beforeunload "unsaved changes" alert is recorded in suggestions.md; (7) card ⬇ = full-variant PDF; JSON download moves to the Preview sheet; backup ZIP ships `document.json` + `document.pdf` per doc in folders named sanitized-title + short random code (replaces `doc.id/` — smoke-m5 zip checks updated); (8) French headings in the "Analyse :" style ("Traduction :" above the question translation, "Grammaire :" above the grammar note) + the RÉPONSE label renders only when an answer or empty lines exist — never alone (template + PDF + copy); (9) build the smart Paste sniffing (`[`→blocks, `<`→HTML, else questions) + Copy presets ("Worksheet (no answers)", "Questions only") + the backup refinement; ideas 3–5, 7 dropped by user; (10) **NEW (2026-08-13, user pain point: "this is a headache going here and there and click reset to repo")** — the active instructions must live in storage and be READ from there, auto-synced from `docs/html_instructions.md` when the repo file changes (the "newer writer wins" mtime rule — repo mtime > active's last-write time → overwrite active via the existing `storage.writeInstructions`, NO history snapshot for machine syncs; user edits newer than the repo change win; no new schema, no UI change; ~4 files: `lib/instructions.ts` helper wired into `getInstructionsState` + `resolveConversionInstructions`, `+getInstructionsEditedAt` on the storage interface, both backends implement it, smoke-m4 gains repo-newer/user-newer/seeding/history checks). Until built: the manual "Reset to repo file" on /instructions stands. **Suggested build order (in the handoff): 10 → 1 → 8 → 6 → 7 → 4 → 9 → 5.** The handoff section also carries the verification commands, standing notes (architecture.md update after every change; user reseeds active.md via "Reset to repo file" after instruction changes; commit locally, push after user confirms) and the file map.

### `lib/types.ts` — Shared data model (Plan §5)
- **Purpose:** Single source of truth for `Block`, `QaContent`, `ParagraphContent`, `EssayContent`, `Document`, `Folder` shapes (FR-1/33/34/45; Folder added 2026-08-10 M7 round 6).
- **Functions:**
  - `createBlock(type: BlockType, id?)` — factory for a fresh block of any type with a unique id and type-appropriate empty content.
  - `createDocument(title?, id?)` — factory for a fresh unsaved document (uuid id when omitted, `ownerId: null`, `source: "editor"`). M6: optional `id` lets the editor keep the route's id so the first save creates it.
  - `setBlockContent(block, content)` / `replaceBlockType(block, type)` — copy-with-content / convert-type helpers.
- **Types:** **`Folder` (2026-08-10 M7 round 6):** `{ id, name, createdAt, updatedAt }` — library folders; **`Document.folderId?: string`** (2026-08-10 M7 round 6) — optional folder reference; undefined = unfiled; storage clears it when the folder is deleted so it never dangles; **`Document.opensInPractice?: boolean` (2026-08-13 — test mode):** set by the Test generator — the editor opens such documents in practice mode by default (answers hidden until Check reveals them; a test is practice with a teacher's key). Optional — absent on all normal documents, never persisted from the editor UI.
- **Types:** `BlockType = "title" | "heading" | "paragraph" | "essay" | "qa" | "separator"` (essay added 2026-08-10); `ParagraphContent` (M6: `text`, `format?`, `translation?`, `analysis?`, `vocab?`, `expressions?`, `userAnswer?` — practice answer, M6 parity with qa; **`synonyms?` (2026-08-10)**); **`EssayContent` (2026-08-10):** `paragraphs: string[]` (the whole passage), ONE shared `translation?`/`analysis?`/`vocab?`/`expressions?` set (+ **`synonyms?` (2026-08-10)**), ONE `userAnswer?` — an essay is a single unit, never per-paragraph enrichment or per-paragraph answers; `QaContent` (question, questionTranslation, grammarNote, responseLabel, userAnswer, modelAnswer, answerTranslation, analysis, vocab, expressions, **`synonyms?` (2026-08-10 — richer synonyms for vocab growth, `{term, def}[]` like vocab)**, hideTranslation, hideModelAnswer, **`suggestions?: Suggestion[]` — AI-reported corrections, 2026-08-10**); **`Suggestion` (2026-08-10):** `{ id (crypto.randomUUID at AI-parse time), kind: "spelling"|"grammar"|"punctuation", field: "question"|"modelAnswer", original (verbatim substring as written), suggestion (corrected replacement), reason? }` + `SuggestionKind` type; `setBlockContent`/`replaceBlockType` helpers (both handle essay). **Preview display options (2026-08-10 M7 round 3):** `PreviewHidden = { translations, analyses, vocab, modelAnswers }` (all booleans — true = HIDDEN, the wire shape shared by /api/preview + the pdf route), `PreviewOptions = { hidden: PreviewHidden; emptyLines: boolean }`, `EMPTY_PREVIEW_HIDDEN` (all false) + `DEFAULT_PREVIEW_OPTIONS = { hidden: EMPTY_PREVIEW_HIDDEN, emptyLines: false }` — the editor's default preview state.

### `lib/schemas.ts` — zod validation schemas
- **Purpose:** API payload validation (Plan §3/§10). Mirrors `lib/types.ts`; `.loose()` on content objects preserves unknown/future fields through save/load.
- **2026-08-10 M7 round 6 additions:** `documentSchema.folderId` (optional — older docs validate unchanged); `folderSchema` (full Folder shape); `createFolderPayloadSchema` / `renameFolderPayloadSchema` (`{ name: string }` — `.trim().min(1).max(80)`, whitespace-only rejected); `moveDocumentPayloadSchema` (`{ folderId: string | null }` — the PATCH body). **2026-08-13 (test mode):** `documentSchema.opensInPractice` — `z.boolean().optional()` (explicit so test docs round-trip validation; older docs validate unchanged).
- **Exports:** `blockSchema` (discriminated union on `type`; paragraph content += `translation`/`analysis`/`vocab`/`expressions`/`userAnswer` — M6; **essay variant 2026-08-10** — `paragraphs: string[]` required, optional shared enrichment + `userAnswer`, `.loose()`; **`heading` optional (2026-08-10 #5)**), `documentSchema`, `saveDocumentPayloadSchema` (`{ doc, html?, instructionsVersion? }` — M6), **`suggestionSchema` (2026-08-10):** `id?` on the wire (AI output has none — the `.transform` guarantees one so the inferred type IS a `Suggestion` and saved docs round-trip ids intact); per-field `.catch()` tolerance — a malformed entry degrades to an empty `original`/`suggestion` (dropped downstream) instead of failing the whole list; kind defaults `"spelling"`, field catches to `"modelAnswer"`, original/suggestion `min(1).max(500)` (runaway-AI guard). Also added to `qaContentSchema.suggestions`. **`hiddenOptionsSchema` (2026-08-10 M7 round 3):** `{ translations?, analyses?, vocab?, modelAnswers? }` — four optional booleans, `.optional()` at the object level; SHARED by `/api/preview` and `/api/documents/[id]/pdf` so the two routes parse the same wire shape (missing keys → false).

### `lib/tokens.ts` — TOKENS block parser (FR-47)
- **Purpose:** Reads the instructions file and extracts the machine-readable design tokens at runtime. Never rewrites source files.
- **Constants:** `REPO_INSTRUCTIONS_PATH` (`docs/html_instructions.md`), `ACTIVE_INSTRUCTIONS_PATH` (`data/instructions/active.md`).
- **Functions:**
  - `parseTokensBlock(markdown, defaults)` — extracts `<!-- TOKENS -->`…`<!-- /TOKENS -->`, parses whitespace-lenient `section:`/`key: value` lines (strips quotes), merges over `defaults`; returns `DesignTokens | null` (null when the block is missing). **2026-08-10:** explicit mappings added for `vocabBg` and `rowBorder` (`?? defaults` — every key is mapped EXPLICITLY, never by generic spread, so new token keys must be added here).
  - `readActiveInstructions()` — seeds `data/instructions/active.md` from the repo copy on first run (M4, FR-21), then reads it; throws if the repo copy is missing.

### `lib/design-tokens.ts` — Runtime design tokens (FR-43)
- **Purpose:** Cached access to the parsed tokens + fallback defaults so the app never crashes. Both renderers (`html-template.ts`, `pdf.ts`) consume the same token object → HTML preview and PDF can never drift apart.
- **Exports:** `DesignTokens` interface (colors incl. `vocabBg` + `rowBorder` — 2026-08-10), `DEFAULT_TOKENS` (fallback, kept in sync with the instructions file — incl. `vocabBg: "#eef2f7"`, `rowBorder: "#d8dfe8"`), `getTokens(): Promise<DesignTokens>` (cached), `invalidateDesignTokensCache()` (dropped on instructions save — M4), `getTokensFromInstructions(instructions: string): DesignTokens` (parses arbitrary content — used by snapshot conversion, FR-23).

### `lib/storage.ts` — Pluggable storage interface + factory (FR-44/45)
- **Purpose:** The only storage gateway app code uses. v1: filesystem (`DATA_DIR`); Vercel deploy: MongoDB + Vercel Blob (M5, `lib/storage-mongo.ts`). Owner seams on every operation (v1 ignores them).
- **Exports:** `StorageBackend` interface (incl. M4 additions `listInstructionsHistory()` → `{ version, savedAt }[]` newest-first, `readInstructionsVersion(version)` → content or null; **M7 round 6 folder CRUD:** `listFolders()` (name-sorted), `createFolder(name)`, `renameFolder(id, name)` → Folder or null, `deleteFolder(id)` — deleting a folder UNFILES its documents (clears folderId), never deletes them; **2026-08-13 (to-do item 10):** `getInstructionsEditedAt(): Promise<number>` — when the active instructions were last written (FS: `stat(data/instructions/active.md).mtime`; Mongo: the `"active"` row's `savedAt`; missing file/row → 0), the timestamp side of the auto-sync rule), `getStorage()` (factory — MongoDB when `MONGODB_URI` set, else FS), `createMongoBlobStorage()` (M5 — real Mongo/Blob implementation, no longer a throw stub).

### `lib/storage-mongo.ts` — MongoDB + Vercel Blob storage implementation (M5, FR-44)
- **Purpose:** The remote backend behind the pluggable interface. MongoDB collections (explicitly typed rows): `documents` (`_id` = doc id, all doc fields), `files` (`_id`, `url`, `contentType` — blob URL index per attachment), `instructions` (`_id` = `"active"` | `"history:<version>"`, `content`, `savedAt`), **`folders` (`_id` = folder id, name, createdAt, updatedAt — M7 round 6)**. Lazy singleton connection (cached promise). Missing instructions seeded from the repo copy on first read (same M4 behavior as FS). **2026-08-13 (to-do item 10):** `readInstructions` calls `syncActiveFromRepo(backend)` first — if the repo file's mtime is newer than the active row's `savedAt`, the repo copy overwrites the active row (machine sync — no history snapshot); user saves after a repo change keep their content (newer writer wins).
- **Functions:**
  - `getDb()` (private) — lazy `MongoClient.connect` cache; throws with a clear message when `MONGODB_URI`/`BLOB_READ_WRITE_TOKEN` are absent.
  - `createMongoBlobStorage()` — returns a `StorageBackend`: `listDocuments` (find + sort updatedAt desc), `getDocument`, `saveDocument` (updateOne `$set` + upsert), `deleteDocument` (best-effort blob `del` + deleteMany), `readFile/writeFile/deleteFile` (Blob `put`/fetch/`del` + files index rows), instructions ops (read/write/snapshot/history/version — see `lib/instructions.ts` for the same flow; **2026-08-13 item 10:** `readInstructions` auto-syncs from the repo file, `getInstructionsEditedAt` → the `"active"` row's `savedAt`), **folder ops (M7 round 6):** `listFolders` (name-sorted), `createFolder`, `renameFolder` (findOneAndUpdate → Folder or null), `deleteFolder` (deleteOne + `$unset folderId` on the folder's documents — unfile, never delete).
- **Note:** PUT/POST document routes keep the same `persistDocument` flow — only the backend differs; API behavior is identical between FS and Mongo/Blob.

### `lib/storage-fs.ts` — Filesystem storage implementation (FR-17)
- **Purpose:** `data/` layout per FR-17 (+ `data/folders.json` for library folders, M7 round 6). Path-traversal guard via `SAFE_FILENAMES` allowlist.
- **Functions:**
  - `createFSStorage(dataDir)` — returns a `StorageBackend`: `listDocuments` (scan `data/documents/*/document.json`, skip invalid, sort updatedAt desc), `getDocument` (null when missing/corrupt), `saveDocument` (mkdir + pretty JSON), `deleteDocument` (recursive rm), `readFile/writeFile/deleteFile(docId, filename)`, `readInstructions` (seeds from repo copy when missing, M4; **2026-08-13 item 10:** then calls `syncActiveFromRepo` — repo file newer than the active copy → repo content replaces it via the internal write, no history snapshot; user saves after a repo change win), `writeInstructions`, `snapshotInstructions` (copies to `history/<version>.md`), `listInstructionsHistory` (newest-first by mtime), `readInstructionsVersion` (sanitizes `[^\w.-]` → `_`), **`getInstructionsEditedAt` (2026-08-13 item 10)** — `stat(data/instructions/active.md).mtime.getTime()`, 0 when missing, **folder ops (M7 round 6):** `listFolders` (read `folders.json` → name-sorted; missing file = empty), `createFolder` (uuid + timestamps, append, write), `renameFolder` (update name/updatedAt → Folder or null), `deleteFolder` (remove from `folders.json` + `unfileDocuments` — clears `folderId` on every document referencing the folder and rewrites those document.json files; the documents themselves are NEVER deleted).

### `lib/instructions.ts` — Instructions management (M4, FR-21/22/23/47)
- **Purpose:** THE owning file for the runtime instructions lifecycle: idempotent first-run seeding from the repo copy, version hashing, save-with-history (previous version snapshotted before overwrite), reset-to-repo, per-document snapshots (`documents/<id>/instructions.snapshot.md`), and resolution of which instructions a conversion uses (active vs. snapshot). Token-cache invalidation is coupled to every write so design changes take effect immediately (FR-47). **2026-08-13 (to-do item 10 — "NO more Reset to repo file"):** also owns the **auto-sync** rule — every read of the active instructions (FS + Mongo `readInstructions`, which both `getInstructionsState` and `resolveConversionInstructions` flow through) calls `syncActiveFromRepo`, so a repo-file change propagates to storage automatically and the manual "Reset to repo file" button on /instructions is now redundant (but harmless).
- **Functions:**
  - `hashVersion(content)` — sha1 of the content, first 8 hex chars.
  - `seedInstructionsIfMissing(activePath)` — idempotently copies `docs/html_instructions.md` → the given path (default `ACTIVE_INSTRUCTIONS_PATH`) + invalidates the token cache.
  - `syncActiveFromRepo(storage)` (2026-08-13, to-do item 10) — the "newer writer wins" rule: `repoMtime = mtime(docs/html_instructions.md)` vs `activeEditedAt = storage.getInstructionsEditedAt()` (0 when never written). If `repoMtime > activeEditedAt` → the repo file changed after the active copy was last written → copies the repo content over the active copy via a dedicated internal write (invalidates the token cache, writes NO history snapshot — history is for user saves; the version after a sync = repo hash). If `activeEditedAt >= repoMtime` → the user (or the last seed) is newer → active untouched. First-run (missing active copy) falls out of the same path (0 < repo mtime → seeds).
  - `getInstructionsState(storage)` — `{ content, version, source: "active", history }`.
  - `saveInstructions(storage, content)` — throws `InstructionsError` when no `<!-- TOKENS -->` block (FR-47); snapshots the current version to history, writes active, invalidates the token cache; returns the new version.
  - `resetInstructions(storage)` — restores the repo copy as active (previous kept in history); returns the new version.
  - `readDocumentSnapshot(storage, docId)` — reads the doc's snapshot → `{ content, version } | null`.
  - `resolveConversionInstructions(storage, docId, useSnapshot)` — snapshot rules when `useSnapshot` and the doc has one, else the active instructions.
  - `InstructionsError` — typed error (TOKENS validation, missing repo copy).

### `lib/html-template.ts` — Styled HTML generator (FR-9, Plan §8.1)
- **Purpose:** Deterministic, self-contained styled HTML from block data + shared tokens. M6: now THE single HTML renderer (the offline "template conversion" mode was deleted — this renders previews/downloads only). Follows `docs/html_instructions.md`: A4 `@page`, Georgia/Times, token colors, `.qa-block` cards, `break-inside: avoid` in print. Full Q&A rendering: sequential circular badges, translation `<em>`, grammar note, response label, user-answer box (dashed left border), model answer, answer translation, analysis, vocab grid (`.two-col`/`.one-col`). **M6 paragraph enrichment:** `p.p-translation` (English translation under the text), `.p-analyse` ("Analyse :" box), and the same vocab/expressions grid as Q&A cards — plus the paragraph practice answer rendered as a `.qa-user-answer` dashed box (M6 bug fix, parity with qa). **M7 round 9 (synonyms):** `vocabGridHtml(vocab, expressions, synonyms?)` gains a third **"Synonymes"** column (reuses `qa-vocab-row`/`qa-vocab-term`); `colClass` is now `two-col` when more than one of the three lists exists (was `vocab && expressions`) else `one-col`; all three call sites (paragraph/essay/qa) pass `content.synonyms`/`c.synonyms`. Color policy: every color from tokens only (FR-47); omission rules FR-36: hidden elements omitted entirely.
- **Functions:**
  - `escapeHtml(text)` — HTML-escapes user content (XSS defense; preview iframe is also sandboxed).
  - `renderInlineMarkdown(text)` — light inline markdown: `` `code` ``, `**bold**`, `*italic*` (applied after escaping). **2026-08-13 (to-do item 2 — bullet points):** now splits on `\n` — lines matching `/^\s*-\s+(.*)$/` become `<li>` items, and CONSECUTIVE bullet lines group into ONE `<ul class="point-list">` (prose lines stay outside the list; a blank/other line flushes the current group). Inline markdown still applies INSIDE points via a private `inline()` helper (escape first, then code/bold/italic replacements — same order as before). CSS added to the style block: `.point-list { margin: 2px 0 0 18px; padding: 0; } .point-list li { margin: 1px 0; }` (indented under the "Analyse :" label, quiet).
  - `buildCss(tokens, paper = false)` (private) — the `<style>` block; when `paper` is true, appends the A4-sheet screen rules. **2026-08-10 #4 (user: "preview must be same size as the spec"):** the sheet now renders at the SPEC's SCREEN design — `body` stays at base `11.5px` (the old `body { font-size: print }` override was dropped) and `.document` pads with `pageMargin` (`18mm`), so the preview matches "GLOBAL STYLE: margins 18mm, base 11.5px" exactly. True print behavior lives in the `@media print` rules (base CSS: 10.5px, padding 0; paper block: margin/shadow stripped), so printing the preview still produces a clean A4 at print size/14mm. **2026-08-10 #3:** every secondary `font-size` is `em` (base-relative — `rem` would resolve against the 16px root and render English translations BIGGER than the French base; that was the "french small, english bigger" bug). `.block-paragraph` leads at `1.15em` (~13.2px at the 11.5px base; English translation 0.9em ≈ 10.4px, clearly secondary).
  - `generateTemplateHTML(doc, tokens, opts?: TemplateRenderOpts)` — builds the full HTML document (doctype, `<style>` from tokens, `<main class="document">` with block sections; Q&A numbering is sequential across the doc). `printMode: true` (preview route) shows the document as an A4 sheet at the spec's SCREEN size (base 11.5px, 18mm margins, white + shadow) — the `@media print` rules inside keep true print behavior when the preview is printed. **TemplateRenderOpts (2026-08-10, M7):** exported interface `{ printMode?: boolean; hidden?: { translations?, analyses?, vocab?, modelAnswers? }; emptyLines?: boolean; showUserAnswers?: boolean }` — the `hidden` flags (from the preview sheet's field toggles) omit those enrichment sections for qa + paragraph/essay blocks; **`emptyLines` (2026-08-10 M7 round 3 — the old "questions" sheet as a toggle)** swaps the model-answer slot for a blank ruled area; **`showUserAnswers` (2026-08-10 M7 round 4)** — practice answers (`userAnswer`) render ONLY when explicitly requested; the preview NEVER passes it (user: "it is showing practice answers why, it should not do this… same for para… in future we will add an option for the review of the practice area"), so the template defaults it OFF — smoke-m5's parse-back round trip passes `true` to keep testing the `.qa-user-answer` recovery path, and the future practice-review option will flip the same flag; passed through to `blockToHtml`/`qaBlockHtml`.
  - `blankAnswerHtml()` (private, 2026-08-10 M7 round 3) — `<div class="blank-answer">` with 4 `<div class="line"></div>` ruled lines (dashed border rows; the CSS rule lives in `buildCss`, the element markup is what smoke tests assert on).
  - `vocabGridHtml(vocab, expressions)` — 2026-08-10: grid background `t.colors.vocabBg`, row borders `t.colors.rowBorder` (spec tokens).
  - `tagClass(tag)` (private) — sanitizes user tags into CSS classes (`#past-tense` → `tag-past-tense`).
  - `qaVisible(doc, content, kind)` (private) — visibility check: hidden when the per-block flag OR the document `practice` default is set (FR-34/35).
  - `vocabGridHtml(vocab, expressions)` (private) — builds the vocab/expressions grid for BOTH qa cards, enriched paragraphs AND essays (2026-08-10); `.two-col` when both lists exist, `.one-col` otherwise.
  - `qaBlockHtml(doc, block, tokens, number, opts)` (private) — assembles a full `.qa-block` card in the **2026-08-10 (M7) field order** (question → **question translation BELOW the question (2026-08-10 M7 round 5 — its own `<p class="qa-question-translation">`; was an inline `<em>` in `.qa-question-text`, same line — user: "it is showing in same line as question it should be below it"; the question row is `.qa-question` flex (badge + `.qa-question-body` flex column so the translation drops under the text)** → analysis → **response label (moved ABOVE the answer 2026-08-10 #5 — it labels the answer area)** → model answer → answer translation → user answer → grammar note → vocab grid), each optional part gated by the `hidden` opts (`translations` hides question + answer translations, `analyses` hides analysis, `modelAnswers` hides the model answer, `vocab` hides the vocab/expressions grid); omitted parts never emitted. **2026-08-13 (to-do item 8 — French headings + no lonely "RÉPONSE"):** the question translation and the grammar note are led by quiet bold French labels in the "Analyse :" style — `<strong>Traduction :</strong>` above the translation, `<strong>Grammaire :</strong>` above the grammar note (the grammar note is `Grammaire :` even when empty-content is absent — gated by `content.grammarNote` as before); the **RÉPONSE label renders ONLY when there is something to label** — `content.responseLabel && (modelAnswer || blank || userAnswer)` — so a question with no answer and no empty-lines blank never shows a lonely "RÉPONSE" (smoke-m2 fixture: b2 + b4 with visible answers = 2 labels; with `emptyLines` on, b3's hidden answer + b5's missing answer get the blank ruled area → 4 labels). **Empty-lines slot (2026-08-10 M7 round 3):** the answer slot renders `${modelAnswer || blank}` — when the model answer is hidden AND `emptyLines` is on, a `blankAnswerHtml()` ruled box takes its place (the old "questions" sheet behavior as a toggle); when the model answer is visible the blank never renders. Paragraph/essay cases mirror this: a blank box renders after the user answer when `emptyLines && !userAnswer` (unanswered writing tasks get ruled space). **User-answer gating (2026-08-10 M7 round 4):** the `.qa-user-answer` div renders as `${opts.showUserAnswers && content.userAnswer ? … : ""}` — with the flag off (the preview default) no practice answer ever appears for qa/paragraph/essay; the blank condition is likewise suppressed when a practice answer IS being shown (no blank under a real answer).
- **Essay rendering (2026-08-10):** `blockToHtml` case "essay" wraps in `.block-essay` (margin 0 0 0.9em) — **an optional `<h3 class="block-essay-heading">` (2026-08-10 #5, navy bold 0.95em)**, paragraphs as `<p class="block-paragraph">`, then the single `userAnswer` as `.qa-user-answer` dashed box, then the ONE translation/analysis/vocab-grid set, exactly like the paragraph case.

### `lib/pdf.tsx` — @react-pdf/renderer PDF generation (FR-14/15, Plan §8.2)
- **Purpose:** The ONLY PDF engine. Generates A4 PDFs from block data (never from HTML) — no Chrome/Puppeteer anywhere. Styles come from the shared tokens (FR-43): Times-Roman, token colors, print margins (~14mm). `.tsx` because it contains JSX. **M6:** three variants via `PDFVariant` — `"full"` (everything), `"questions"` (shareable practice sheet: title + questions + blank answer areas only), `"my-answers"` (after practice: questions + the user's own answers, no reference answers — send for checking). **M6 bug fix:** paragraphs participate in practice PDFs — `"full"` renders paragraph `userAnswer` (dashed box, parity with qa); `"my-answers"` includes paragraphs with the user's written answer (or `BlankAnswerArea` when unanswered). **Essay (2026-08-10):** `BlockToPDF` case "essay" mirrors the paragraph case — all paragraphs as one continuous passage, single userAnswer box, one enrichment set (full only). **2026-08-10 #5:** an optional heading leads the passage (`styles.essayHeading` — bold, heading color); on the PRACTICE sheets (questions / my-answers) ONLY the heading renders (the passage is the writing task — the sheet shows the title + rules/answer, not the source text; user: "in practice it should only show the heading"). Block filter: title + qa + **essay on BOTH non-full variants**; standalone paragraphs remain my-answers-only. Paragraph/essay translation/analysis/vocab grid render only in `"full"`. **2026-08-10 M7 round 9 (synonyms):** `VocabGridPDF` gains a third **"Synonymes"** column (alongside Vocabulaire Clé / Expressions Avancées) — `synCol = col("Synonymes", synonyms ?? [], Boolean(vocabCol) || Boolean(exprCol))`; the grid renders when ANY of the three lists is non-empty and drops the column entirely when its list is empty (parity with the HTML template's `vocabGridHtml` three-column logic). **2026-08-13 (PDF bug-fix pass):** three fixes — (1) **footer crash** (react-pdf 4.6.0 bug: a `fixed` + `position: absolute` + `bottom` footer gets a garbage ~1e22pt height on continuation pages of long docs → pdfkit rejects the coordinate with "unsupported number: -1.29e+22" and the download 500s; fixed by pinning `height` to one footer line on the footer View — the box is small, invisible); (2) **QA toggle forwarding** — `BlockToPDF`'s `case "qa"` now forwards `hidden`/`emptyLines` to `QABlockPDF` (they were missing, so the display PDF always rendered translations/analyses/vocab/model answers even when the preview toggles hid them); (3) **card wrap** — `<View wrap={false}>` on each QA card = `break-inside: avoid` (react-pdf v4.6 has no `breakInside` style; `wrap={false}` moves the whole card to the next page instead of splitting it — 84/84 cards verified un-split; the old note below about `minPresenceAhead` being the only option is superseded — `minPresenceAhead` remains on the question row for widow control).
- **Functions:**
  - `lengthToPt(value)` — converts token lengths ("14mm", "11.5px", "0.8rem") to points for react-pdf.
  - `bulletText(text)` (private, 2026-08-13 — to-do item 2) — turns free-text bullet lines into PDF bullets: lines matching `/^\s*-\s+/` get a `•  ` prefix (dash stripped), other lines pass through; applied at the THREE analysis sites — `QABlockPDF`'s `{content.analysis}` and the paragraph/essay `{c.analysis}` Texts (react-pdf has no list element, so the `•` prefix is the list).
  - `BlockToPDF({ block, doc, tokens, qaNumber, variant })` — maps a block to react-pdf elements; QA numbering sequential across the doc.
  - `QABlockPDF(...)` (private) — full Q&A card; omission matrix by variant (`hideAnswers = variant !== "full"`, `showUser = variant === "my-answers"` — **2026-08-10 M7 round 4, was `variant !== "questions"`**: practice answers render ONLY on the my-answers variant, so the display PDF (UI posts no variant → "full") matches the preview byte-for-byte; user: "it is showing practice answers why" —, `showExtras = variant === "full"`) over the `qaVisible` rules (FR-36). **2026-08-13 (to-do item 8 — no lonely "RÉPONSE"):** the response label renders only when `content.responseLabel && hasAnswerArea` (`hasAnswerArea` = a model answer, a blank answer area, or a shown user answer — the PDF mirror of the template's gating; the copy output never emits the label at all, so there is no lonely-label path there). **2026-08-10 (M7):** field order reordered to question → analysis → **response label (2026-08-10 #5: moved above the answer)** → model answer → blank answer area → answer translation → user answer → grammar note → vocab grid — mirrors the editor + HTML template (user-requested order). **2026-08-10 M7 round 5:** the question row is badge + a `questionBody` column View (`flex: 1`) — the question translation renders BELOW the question on its own line (was a nested Text inside `questionText`; mirrors the HTML template's `.qa-question-translation`). Paragraph/essay cases use the same `showUser` gate and `(variant !== "full" || emptyLines) && !(showUser && c.userAnswer)` blank condition. **2026-08-13 (badge centering, measured):** the question-number badge digit rendered ~3pt HIGH in the circle (pixel-measured at 96dpi: dy −4px on the 18pt badge) — flex centering centers the LINE BOX, not the glyph. A first fix (`lineHeight: "18pt"` = full badge height) measured WORSE (~5.25pt high) because react-pdf anchors the baseline high inside the line box — the correct direction is a SMALLER line box so flex centering pushes the digit down: `badgeText` now uses unitless `lineHeight: 1.3` (→ 1.3 × fontSize ≈ 7.85pt line box → line-box top ≈ 5.1pt → digit lands on the 9pt circle center).
  - `BlankAnswerArea({ basePt, color })` (private) — empty ruled dashed area ≈4 lines (FR-49) for unanswered questions in non-full variants.
  - `qaVisible(doc, content, kind)` (private) — same visibility logic as the HTML template (FR-34/35).
  - `generatePDFBuffer(doc, tokens, opts?)` — renders `<PDFDocument>` via `renderToBuffer`; `opts.variant` selects the variant (M6 replaces the old `practice?` flag); non-full variants filter blocks to title + qa + essay (2026-08-10), with paragraphs only on my-answers. (2026-08-10: vocab grid rows use `t.colors.vocabBg` background + `t.colors.rowBorder` borders — matches the HTML spec.) **Display options (2026-08-10 M7 round 3):** `opts.hidden?: Partial<PreviewHidden>` (optional fields — the zod route schema is partial; undefined → shown) + `opts.emptyLines?: boolean` (default false). `QABlockPDF` gates `showTranslation`/`showModelAnswer`/analysis/vocab-grid on the matching hidden flag, and renders a `BlankAnswerArea` where the model answer is hidden when `emptyLines` is on (or `hideAnswers` on practice variants, unchanged); paragraph/essay blocks get a blank area when `emptyLines && !userAnswer` (unanswered writing tasks). The `variant` axis and the hidden/emptyLines axes compose independently. **Page footer (2026-08-10):** every page (all three variants) carries a `fixed` absolutely-positioned View at `bottom: printMargin` (the margin band below the content box — react-pdf absolute positioning is page-relative) with a `<Text render={({ pageNumber, totalPages }) => pageNumberLabel(pageNumber, totalPages)} />` — "1/7" style footers, fontSize `basePt * 0.8`, color `tokens.colors.border`. Verified end-to-end: full = 1/2, 2/2; questions/my-answers = 1/3, 2/3, 3/3 (TJ hex-run decode). **2026-08-13:** the footer View's `height` is PINNED to one footer line (`Math.ceil(basePt * 0.8 * 1.6)`) — react-pdf 4.6.0 computes a garbage height (~1e22pt) for bottom-anchored `fixed` elements on continuation pages, which made pdfkit throw "unsupported number" and 500 the download for any document long enough to paginate (see the purpose paragraph above).

### `lib/html-to-blocks.ts` — HTML→blocks parse-back (M5, FR-41)
- **Purpose:** Best-effort parser that reconstructs editable blocks from rendered HTML (imported via "Paste HTML back" or the editor's "Parse to blocks" button on external-html documents). Scans for the class vocabulary emitted by `lib/html-template.ts` (`block-title`, `block-heading`, `block-paragraph`, **`block-essay` (2026-08-10)**, `block-separator`, `block-qa`/`qa-block`; inside `.qa-block`: `.qa-question-text` (with the **2026-08-10 M7 round 5 `<em>`-translation fallback for older exports** — current exports put the translation in its own `.qa-question-translation` below the question), `.qa-grammar-note`, `.qa-response-label`, `.qa-user-answer`, `.qa-answer` exact-single-class, `.qa-translation`, `.qa-analyse`, `.qa-vocab-grid` `.two-col`/`.one-col` → `.qa-vocab-col` with headers "Vocabulaire Clé"/"Expressions Avancées"). Purely string-based — no DOM — runs identically in the browser and in node smoke tests. Unrecognized elements become paragraph blocks with the raw HTML preserved. Bold/italic/code markers restored as markdown (FR-42). Hide flags can't be detected from rendered HTML (hidden fields are omitted, so they never fabricate).
- **Exports:** `parseHtmlToBlocks(html): { blocks, unparsedCount }`, `collectTopLevel(html): Element[]`.
- **Functions:** `classList(attrs)`, `decodeHtml`, `textOnly(inner)`, `innerToMarkdown(inner)`, `elementEnd(html, start, tag)`, `collectTopLevel(html)`, `extractMain(html)`, `byClass(inner, cls)` (stack-based deep traversal — the M5 infinite-loop fix), `elementsByClass(inner, cls)`, `parseRows(container, rowClass)`, `parseQa(inner)`, **`parseEssay(inner)` (2026-08-10 — `.block-paragraph` children → `EssayContent.paragraphs`, plus the single userAnswer/translation/analysis/vocab-grid like `parseQa`; **`.block-essay-heading` → `heading` (2026-08-10 #5)**)**, `blockWithContent(type, content)`. **2026-08-13 (to-do item 8):** the round trip strips the generator's French labels before markdown recovery — the `.qa-question-translation` inner HTML drops `<strong>Traduction :</strong>` (both the raw tag form and the markdown-form `**Traduction :**` it becomes after `innerToMarkdown`) and the grammar note drops `<strong>Grammaire :</strong>` the same way (same pattern as the existing "Analyse :" strip); smoke-m5 verifies the values round-trip without the labels.

### `lib/structuring.ts` — Canonical AI block parser (M6, NEW)
- **Purpose:** THE one JSON-array parser for AI-structured content. Both the convert route and the client-side "Paste blocks (AI)" modal consume it, so the AI output shape, the Copy-for-AI format, and the paste round-trip are always the same format (FR-42). Tolerant: strips markdown fences, extracts first `[`…last `]`, zod-validates each entry, maps validated entries to blocks via the typed factories.
- **Exports:** `extractJsonArray(raw)` (fence-stripped slice → JSON or null), `aiBlockEntrySchema` (zod discriminatedUnion on `type` — title/heading/paragraph/**essay (2026-08-10)**/qa/separator, `.loose()`, optional fields; qa requires `question`; paragraph carries `text` + `translation`/`analysis`/`vocab`/`expressions` (+ **`synonyms` (2026-08-10)**); essay carries required `paragraphs: string[]` + **optional `heading` (2026-08-10 #5)** + the same optional enrichment (+ `synonyms`); qa carries the same enrichment incl. `synonyms` + **`suggestions` (2026-08-10) — `z.array(suggestionSchema).catch([]).optional()`: the array-level catch is the final safety net so a malformed list can never kill the qa block**), `parseStructuredBlocksResponse(raw): Block[]` (validates + normalizes: trims empty strings, drops empty lists via `optList` (synonyms included), qa defaults `responseLabel: "RÉPONSE"` + `hideTranslation: false` + `hideModelAnswer: false`; essay filters empty paragraphs and drops the block when none remain; skips empty-text entries). **Suggestions normalization (2026-08-10):** `optSuggestions(items)` — trims original/suggestion/reason, drops empty entries (the schema's per-field catches already degraded malformed ones to empty), caps at 10, assigns `crypto.randomUUID()` per row (works server + browser — precedent: `createBlock`); field mismatches are NOT dropped at parse — the editor's render-time filter (`lib/suggestions.ts`) handles them losslessly.

### `lib/suggestions.ts` — AI corrections logic (2026-08-10, NEW)
- **Purpose:** Pure (no React/DOM) rules for the AI-reported corrections list on qa blocks. The AI NEVER edits text — the user applies or dismisses each row individually.
- **Functions:**
  - `targetText(c, s)` — the field text a suggestion targets (`question` or `modelAnswer`).
  - `visibleSuggestions(c)` — cosmetic stale filter: only suggestions whose `original` still exists verbatim in the field render (user edits hide them, never corrupt them).
  - `applySuggestion(c, s)` — replaces the FIRST occurrence of `original` with `suggestion` (literal string replace — no regex injection), removes ONLY that row, then re-filters the surviving rows against the post-replacement text (an applied edit invalidates overlapping suggestions); a stale `original` → dismiss-only, text untouched.
  - `dismissSuggestion(c, id)` — removes the row from content (so it stays gone across re-renders).
- Covered by `tests/smoke-m6.ts`.

### `lib/auto-grow.ts` — Textarea auto-grow helper (2026-08-10, NEW)
- **Purpose:** The `autoGrow(el)` DOM helper extracted from Block.tsx so Q&A cards, enrichment fields and practice answers share one implementation (FR-25).
- **Functions:** `autoGrow(el)` — reset height to auto, then clamp to `scrollHeight`.

### `lib/pdf-labels.ts` — Pure PDF label helpers (2026-08-10, NEW)
- **Purpose:** Lives OUTSIDE `lib/pdf.tsx` so smoke tests can import it without dragging in `@react-pdf/renderer` (the tests tsconfig only compiles `../lib/**/*.ts`).
- **Functions:** `pageNumberLabel(pageNumber, totalPages)` — `"1/7"` style label for the PDF footer.

### `lib/questions.ts` — Question-list parsing + AI structuring (M3, FR-32/38)
- **Purpose:** Local numbered/bulleted-list parser for question import, plus the AI-structuring prompt + tolerant JSON response parser (uses `extractJsonArray` from `./structuring`).
- **Functions:**
  - `splitQuestions(text)` — splits a pasted list: numbered (`1.`/`1)`), bulleted (`-`/`*`), blank-line separated; continuation lines merge into the current item; a numbered/bulleted line always starts a new item.
  - `questionsToQaBlocks(questions)` — question-only Q&A blocks with `responseLabel: "RÉPONSE"`.
  - `buildStructuringUserPrompt(questions)` — numbered list + exact JSON-array schema instruction for the AI.
  - `parseStructuredQaResponse(raw)` — tolerant: `extractJsonArray` → zod `.loose()` per entry → `QaContent[]`; empty array on garbage.

### `lib/prompt.ts` — Prompt assembly + block serialization (M3, FR-12/39, Plan §9.1)
- **Purpose:** The user section serializes every block with type markers (`<TITLE>`, `<HEADING LEVEL="2|3">`, `<PARAGRAPH FORMAT="markdown">`, `<ESSAY>` with an optional `<HEADING>…</HEADING>` first line (2026-08-10 #5) + `<P>…</P>` per paragraph (2026-08-10), `<SEPARATOR/>`, `<QA>` with `QUESTION:`/`QUESTION_TRANSLATION:`/`GRAMMAR_NOTE:`/`RESPONSE_LABEL:`/`MODEL_ANSWER:`/`ANSWER_TRANSLATION:`/`ANALYSIS:`/`VOCAB: term|def; …`/`EXPRESSIONS: …`/`SYNONYMS: term|def; …` (2026-08-10 M7 round 9 — mirrors the vocab/expressions marker so synonyms round-trip through Copy for AI)/`HIDE_TRANSLATION:`/`HIDE_MODEL_ANSWER:`). **M6:** `USER_ANSWER:` was REMOVED from `serializeQa` — practice answers are private and never serialized anywhere (the "Copy for AI" markers and the convert prompt both exclude them). `buildAIPrompt` ends with the exact JSON-blocks demand: "Return ONLY a JSON array of block objects — no markdown fences, no explanations, no HTML" with the SIX block shapes (essay included), plus the rule "Group consecutive prose paragraphs of the same passage into ONE essay object with a single shared enrichment set — never split an essay into per-paragraph parts", "Omit any optional field you cannot fill with confidence", "Never invent an answer for an unanswered question — leave modelAnswer out entirely", "Never include user answers". **CORRECTIONS demand (2026-08-10):** the qa JSON shape example carries `"suggestions":[{"kind":"…","field":"…","original":"…","suggestion":"…","reason":"…"}]` and a CORRECTIONS paragraph — check every qa question/modelAnswer for spelling (accents), grammar, punctuation (French spacing: no space before `,` `.` `;`, space before `:` `;` `!` `?`); NEVER rewrite the text (verbatim); one suggestion per distinct mistake with `original` matching verbatim; omit `suggestions` when correct; max 10. **Suggestions are never serialized** in `serializeQa`/`serializeBlocksForAI` (conversion-time feedback, not document content — verified by smoke-m6).
- **Functions:**
  - `serializeQa(c)` (private) — one QA block in marker format (no user answer).
  - `serializeBlocksForAI(doc)` — whole document with type markers (was also the `markerText` of the export-prompt route — **route removed 2026-08-10 #5**, the Copy menu is now a single FR-50 button). Essay → `<ESSAY>` with optional `<HEADING>` + `<P>` per paragraph.
  - `buildAIPrompt(doc, instructions, goal?)` — `{ system, user }`: system = active instructions verbatim; user = optional `GOAL: …` line + serialized blocks + the JSON-blocks demand.
  - `serializePlainText(doc)` — quick plain-text flattening (kept for parity even though the FR-39 copy UI was removed); essay = heading + paragraphs joined with blank lines (2026-08-10 #5).
  - `buildAICopyText(doc, instructions)` — **2026-08-10 (M7):** the copy dialog's "For AI" tab — one copyable text = the convert-time instructions verbatim (the same resolution Rethink with AI uses: active file or doc snapshot, fetched by CopyDialog via `GET /api/instructions?docId&useSnapshot`) + `\n\nConvert the content below into structured document blocks following the instructions above. ` + BLOCK_FORMAT_SPEC + `\n\n=== CONTENT ===\n` + `serializeBlocksForAI(doc)`. The other AI's response pastes back via Paste blocks (AI) (same seam, same parser) — the app makes no AI call. Practice answers stay private (never included — verified by smoke-m7).
  - `BLOCK_FORMAT_SPEC` (exported constant — was private; **2026-08-10 M7 round 9:** now `export`ed so PasteBlocksModal's `BLOCKS_INSTRUCTION` composes it) — **2026-08-10:** the shared response-format directive (JSON block shapes incl. essay + qa `suggestions` and **`synonyms` on paragraph/essay/qa (2026-08-10 M7 round 9 — "when the passage uses a basic/everyday word worth enriching, add 2–3 REAL French synonyms to `synonyms` (term = the synonym, def = its English meaning) that fit the context — never invent a word; never suggest a synonym that changes the meaning; omit `synonyms` entirely when nothing worth enriching is present"**, essay-grouping rule, CORRECTIONS, omit/never-invent/never-user-answers rules) used by **THREE** consumers — `buildAIPrompt` (in-app Rethink with AI), `buildAICopyText` (Copy → For AI) and PasteBlocksModal's `BLOCKS_INSTRUCTION` — one source of truth so the in-app convert instruction, the copy instruction and the paste-block instruction can never drift apart (2026-08-10 M7 round 9: the paste instruction was hand-copied and MISSED the synonyms + suggestions rules — user asked "if i ask ai to convert it will it give me suggested synonyms or not?"; fixed by composition).

### `lib/validate.ts` — HTML validation & wrapping (M3, FR-10)
- **Purpose:** Pasted HTML is normalized before import: fences stripped, sanity-checked, fragments wrapped in a full document, DOCTYPE prepended. Never trusted raw. (AI conversion no longer goes through this — the convert route returns blocks, not HTML.)
- **Functions:**
  - `validateAndWrapHtml(input)` — strip fences → keep full documents (`<!doctype html>`/`<html`) as-is → otherwise wrap fragment with `<title>` from the first `<h1>` (fallback "Document").

### `lib/save.ts` — Document persistence flow (FR-17, M6)
- **Purpose:** Shared by POST/PUT document routes: always writes `document.json`; when a preview exists, also writes `document.html` + regenerates `document.pdf`. **M6:** `html?` is passed only by the import-html route; regular saves carry `instructionsVersion?` — when present, the server snapshots the ACTIVE instructions to `documents/<id>/instructions.snapshot.md` (FR-23), which is what makes "convert with this document's snapshot rules" possible later.
- **Functions:**
  - `persistDocument(storage, doc, html?, instructionsVersion?)` — saves JSON, then (if html) writes the html file + renders and writes the PDF file; when `instructionsVersion` is provided, reads the active instructions and writes the per-doc snapshot.

### `lib/ai.ts` — DeepSeek client (M3, FR-8/30/31, one-change-one-file FR-48)
- **Purpose:** THE ONLY file that talks to the AI. Plain `fetch` against `${baseUrl}/v1/chat/completions` (OpenAI-compatible), temperature 0.3, Authorization Bearer. Env-configurable base URL + model. Actionable error messages (FR-30). Token-usage console logging per conversion (FR-31).
- **Functions:**
  - `getAIConfig()` — `{ apiKey, baseUrl, model }` from `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` (default `https://api.deepseek.com`) / `DEEPSEEK_MODEL` (default `deepseek-chat`).
  - `hasAIKey()` — true when `DEEPSEEK_API_KEY` is set.
  - `stripMarkdownFences(text)` — removes ```…``` code fences from AI output.
  - `convertWithAI(system, user)` — POST chat completion; throws `AIError` (message + optional HTTP status) on API errors; strips fences; logs usage (FR-31).

### `lib/zip.ts` — Zero-dependency ZIP writer (M5, backup)
- **Purpose:** Minimal ZIP archive builder (deflate method 8, node:zlib `deflateRawSync`) for the library backup — no external archive dependency.
- **Functions:**
  - `createZip(entries: { name: string; data: Buffer }[]): Buffer` — local file headers (0x04034b50, 30 bytes), central directory (0x02014b50, 46 bytes, offsets at 42), EOCD (0x06054b50, 22 bytes); per-entry CRC-32 from a table-driven `crc32` (256-entry `CRC_TABLE`).

### `lib/backup.ts` — Backup naming helpers (2026-08-13, to-do item 7, NEW)
- **Purpose:** Pure (no fs) helpers for the backup ZIP's folder names — extracted so smoke-m5 can pin the sanitization rules without touching the route.
- **Functions:**
  - `sanitizeBackupFolder(title)` — strip `/ \ : * ? " < > |`, collapse whitespace runs, trim, strip trailing dots/spaces; empty result → `"document"`.
  - `shortCode()` — 4 lowercase alphanumerics (`Math.random` pick from a 36-char alphabet) so title collisions never overwrite.

### `lib/tags.ts` — Tag parsing (M5, FR-5/18)
- **Purpose:** Normalizes raw tag input ("French, #past-tense, french") into canonical tag strings.
- **Functions:**
  - `parseTags(raw: string): string[]` — lowercase, strip `#` prefixes, whitespace → `-`, trim, dedupe, drop empties.

### `lib/paste-sniff.ts` — Smart-paste content sniffer (2026-08-13, to-do item 9, NEW)
- **Purpose:** The pure rule behind the Smart paste box — one paste area that routes content to the right import without asking the user which kind it is. No DOM, no React — smoke-tested directly.
- **Exports:** `PasteKind = "blocks" | "html" | "questions"`, `sniffPasteKind(text: string): PasteKind`.
- **Functions:**
  - `sniffPasteKind(text)` — `trimStart` the input, then: first char `[` → `"blocks"` (AI block JSON), `<` → `"html"`, anything else → `"questions"` (numbered/bulleted question lists); empty/whitespace-only → `"questions"` (the harmless default — the user just types a question).

### `lib/test-generator.ts` — Test generator, random path (2026-08-13, to-do item 5, NEW)
- **Purpose:** Builds a practice TEST document from chosen source documents WITHOUT the AI — picks qa/essay blocks locally (instant, free) so the Test dialog's "random" path never needs the API. Practice answers are stripped; model answers are KEPT (the test taker checks against them). The AI path lives in `app/api/test/route.ts`; this module is what smoke-m9 covers.
- **Exports:** `TestCounts { questions?: number | null; essays?: number | null }`, `defaultQuestionCount()`, `defaultEssayCount()`, `testTitle(now = new Date())`, `buildTestDocument(docs: Document[], counts?: TestCounts): Document`.
- **Functions:**
  - `defaultQuestionCount()` — `3 + floor(random * 3)` → 3–5 (the random-average rule).
  - `defaultEssayCount()` — `1 + floor(random * 2)` → 1–2.
  - `testTitle(now)` — `Test — ${toLocaleDateString("en-GB", { day: "numeric", month: "short" })}` → "Test — 13 Aug" (deterministic when `now` is passed — smoke-testable).
  - `buildTestDocument(docs, counts)` — pools the qa blocks (question `trim()` non-empty) + essay blocks across ALL chosen docs; `nQuestions = min(counts.questions ?? default, pool.length)`, same for essays; picks WITHOUT duplicating ids; each picked block is copied via `setBlockContent(block, { ...content, userAnswer: undefined })` (practice answers never carry over — private); result is `createDocument(testTitle())` with the picked blocks (title/paragraph blocks never carry over). Counts clamp to what exists (a pool of 1 qa + 0 essays with counts 5/2 → 1 qa); no usable content → an empty test document. **2026-08-13 (test mode):** the result also sets `opensInPractice = true` — the editor opens the test in practice mode (answers hidden until Check; see Editor).

### API routes (`app/api/…`)
- `api/convert/ai/route.ts` — **POST** `{ doc, goal?, useSnapshot? }` → `{ blocks, instructionsVersion }` (AI conversion, FR-8/29/30, **M6: returns editable structured blocks**). Zod-validates; missing key → 400 with actionable message (FR-30); instructions via `resolveConversionInstructions` (FR-23) as the system prompt; `buildAIPrompt` → `convertWithAI` → `parseStructuredBlocksResponse`; `blocks.length === 0` → 502 with a hint about the snapshot-rules toggle; `instructionsVersion = hashVersion(instructions)` returned so the editor records it on save (FR-23).
- `api/preview/route.ts` — **POST** `{ doc, hidden?, emptyLines? }` → `{ html }` (M6, NEW). Stateless on-demand preview: zod-validates the document, `getTokens()` → `generateTemplateHTML(doc, tokens, { printMode: true, hidden, emptyLines })`. The editor posts its CURRENT doc (unsaved edits included) — no save, no artifact written. (Bug fix 2026-08-10 #1: unwraps `payload.doc ?? body` like `/pdf` and `/convert/ai` — the editor's `{ doc }` wire shape previously hit a 400 "Invalid document payload". **2026-08-10 #4:** `printMode` semantics changed — the preview now shows the SPEC's SCREEN design (base 11.5px, 18mm margins) instead of the forced print size; `@media print` still applies when the sheet itself is printed. **2026-08-10 #3 (M7):** `hiddenOptionsSchema` (4 optional booleans: translations/analyses/vocab/modelAnswers, `.optional()` at the object level) validated via zod and passed to the renderer — the preview sheet's field toggles need no new endpoint. **2026-08-10 M7 round 3:** `emptyLines` — a top-level `boolean` (read via `typeof` check, default false) forwarded to the renderer; the sheet's "Empty lines" checkbox needs no new endpoint.)
- `api/documents/route.ts` — **GET** list (optional `?owner=` filter, FR-45) → `{ documents }`; **POST** `{ doc, instructionsVersion? }` → creates + persists artifacts (201).
- `api/documents/[id]/route.ts` — **GET** → `{ doc, snapshotInfo: { version, differs } | null }` (404 if missing; `differs` = snapshot version ≠ active version — FR-23); **PUT** `{ doc, instructionsVersion? }` (id must match route; persists artifacts); **DELETE** → 204; **PATCH (2026-08-10 M7 round 6)** `{ folderId: string | null }` (move between library folders — `moveDocumentPayloadSchema`; null = unfiled; `updatedAt` untouched — moving is organization, not editing; content never touched). `params` typed as `Promise<{ id }>` (Next 15+ convention).
- `api/documents/[id]/html/route.ts` — **GET** download HTML (saved file, else freshly generated from block data — regenerate, FR-20); attachment filename from doc title.
- `api/documents/[id]/pdf/route.ts` — **GET** `?variant=full|questions|my-answers` + **POST** `{ doc?, variant?, hidden?, emptyLines? }` (M6; **M7 round 3:** POST accepts the SAME display options as `/api/preview` — `hiddenOptionsSchema.parse(payload.hidden)` + `emptyLines` boolean → `generatePDFBuffer(parsed.data, tokens, { variant, hidden, emptyLines })`; the UI no longer sends `variant` — the Preview sheet's Download PDF button posts exactly what's displayed; `variant` stays for GET/tests). Both generate the PDF instantly from block data via `generatePDFBuffer` — the POST variant renders the editor's CURRENT doc (unsaved edits included, no gating, FR-46 gate removed); `parseVariant` helper; per-variant attachment filenames (`-questions` / `-my-answers` suffixes).
- `api/documents/[id]/regenerate/route.ts` — **POST** re-convert from JSON (template render) + re-render PDF (FR-20); 404 when missing.
- `api/convert/structure/route.ts` — **POST** `{ questions: string[] }` (1..200) → `{ blocks }` (FR-32 AI mode, used by Paste questions → "Structure with AI"). Missing key → 400; instructions as system prompt + `buildStructuringUserPrompt`; `parseStructuredQaResponse`; empty result → 502.
- `api/documents/import-html/route.ts` — **POST** `{ html, title? }` → 201 `{ doc, html }` (FR-40). `titleFromHtml` (title tag or first h1), `validateAndWrapHtml`, new document with `source: "external-html"` + empty blocks, `persistDocument` (with html → writes html + pdf).
- `api/documents/backup/route.ts` — **GET** full-library backup ZIP (M5). Lists documents → entries **`<sanitized title>_<4-char code>/` folders (2026-08-13, to-do item 7 — was `doc.id/`; `sanitizeBackupFolder` + `shortCode()` from `lib/backup.ts` so any OS can unzip: `/ \ : * ? " < > |` forbidden, whitespace collapsed, trailing dots/spaces stripped, empty title → "document")** — each folder carries `document.json` (pretty) ALWAYS + `document.pdf` (generated on demand when missing, via `generatePDFBuffer`) → `createZip` → `Content-Type: application/zip` + `Content-Disposition: attachment; filename="writer-app-backup-<date>.zip"`; body via `new Uint8Array(zip)` (Buffer isn't a valid BodyInit).
- `api/folders/route.ts` — **GET** → `{ folders }` (name-sorted); **POST** `{ name }` (2026-08-10 M7 round 6) → `createFolderPayloadSchema` (trimmed, 1–80 chars), server generates id/timestamps → 201 `{ folder }`.
- `api/folders/[id]/route.ts` — **PATCH** `{ name }` (2026-08-10 M7 round 6) → rename via `renameFolderPayloadSchema`, 404 when missing; **DELETE** → 204 — unfiles the folder's documents via the storage backend (never deletes documents).
- ~~`api/export/prompt/route.ts`~~ — **REMOVED 2026-08-10 #5:** the Copy dropdown is now a single "Copy…" button (FR-50 dialog only) — the FR-39 marker/system/plain-text copies moved into the paste sections, so this route had no UI path left.
- `api/test/route.ts` — **POST** `{ docIds: string[], questions?: number, essays?: number }` → 201 `{ id }` (2026-08-13, to-do item 5 — the Test generator's AI path). Validates `docIds` (non-empty strings); missing `DEEPSEEK_API_KEY` → 400 with the actionable "add DEEPSEEK_API_KEY to .env.local" message (FR-30 pattern); loads the docs via `storage.getDocument` (missing ids skipped); `nQuestions`/`nEssays` only when `typeof === "number" && > 0` (else null → the AI picks by the random-average rule); system prompt = `resolveConversionInstructions(storage, null, false)` + a user prompt that says "Create a French practice TEST from the material below." + the counts (defaults "3–5 questions" / "1–2 essays") + "Return ONLY a JSON array of block objects" + the `<MATERIAL>`-fenced `serializeBlocksForAI` output of each chosen doc (practice answers never serialized — standing rule); `convertWithAI` → `parseStructuredBlocksResponse` (empty → 502); the parsed blocks go into `createDocument(testTitle())` **+ `opensInPractice = true` (2026-08-13 — tests auto-open in practice mode, answers hidden until Check)** + `storage.saveDocument`; returns the new id so the dialog opens the test in the editor. `AIError` → its message/status; anything else → 500.
- `api/instructions/route.ts` — **GET** → `getInstructionsState(storage)` (content/version/history — FR-21/22); **2026-08-10:** with `?docId=`/`?useSnapshot=true` → `resolveConversionInstructions(storage, docId, useSnapshot)` (content/version only) so Copy → "For AI" fetches the same instructions a conversion uses; **PUT** `{ content }` (zod min 1) → `saveInstructions`, `InstructionsError` → 400 with its message.
- `api/instructions/reset/route.ts` — **POST** → `resetInstructions(storage)` → `{ ok, version }` (FR-22).

### `next.config.ts`
- **Purpose:** `serverExternalPackages: ["@react-pdf/renderer"]` so the PDF engine works in route handlers (FR-14/15).

### `.gitignore` / `.env.local.example`
- **Purpose:** `/data` runtime storage ignored; `.env*` ignored except the committed example; env vars per requirements §12 (DEEPSEEK_*, DATA_DIR, MONGODB_URI, BLOB_READ_WRITE_TOKEN).

### `components/Editor.tsx` — Main editor (client, M6 redesign)
- **Purpose:** All document state + flows. M6: init by `docId` (route `/doc/[id]`; unknown ids start fresh with the same id so the first save creates the doc — no localStorage draft anymore); convert with AI → POST /api/convert/ai → the doc's blocks are REPLACED with the AI's editable structured blocks (practice answers + hide flags carried over by matching question text); save sends `{ doc, instructionsVersion }` (the version from the last conversion — drives snapshot bookkeeping); **autosave (M6 bug fix; M7 round 7: now toggleable):** quiet debounced (~1.2s) server save on every edit — practice answers and any change land on the server without touching the toolbar's busy state (guards: `busyRef` + `savingRef`, dirty-flag cleared only when the saved snapshot is still current; **the effect also returns early when the toolbar's Autosave toggle is off — `if (loading || !doc || !isDirty || !autosave) return;` + `autosave` in the deps — so off = only Save (button / ⌘S) persists; manual saves are untouched**); on-demand preview (POST /api/preview → full-screen PreviewSheet) with **downloads INSIDE the preview (2026-08-10 M7 round 3 — user: "see the exact document before downloading"):** Download PDF posts `{ doc, hidden, emptyLines }` (the current display options) to the pdf route, Download HTML downloads the in-memory `previewHtml` string — both byte-match what the sheet shows; Practice master key (all blocks shown — questions AND paragraphs get "My answer" boxes; Check/Hide-answers cycle + Reset practice clears qa AND paragraph answers); Cmd/Ctrl+S + Cmd/Ctrl+Enter shortcuts (FR-7); global visibility buttons (FR-35); copy-for-AI (FR-39); paste blocks/questions/HTML (M6 adds "Paste blocks (AI)") + copy-for-sharing dialog (FR-50); instructions version in the status bar (FR-28/47); snapshot-aware conversion toggle (FR-23); block operations (update/convert-type/remove/move/insert-after/append + M5 drag-reorder, Enter-split, Backspace-merge); document tags (M5); "Parse to blocks" for imported HTML (M5, FR-41).
- **Functions:** **init effect (2026-08-13 — test mode):** when the loaded doc carries `opensInPractice` (set by the Test generator), `setPracticeMode(true)` right after `setDoc` — a test opens in practice mode with ALL answers hidden; Check reveals them (the teacher's key); toggling off returns to normal editing for the session; a reload of a test doc always starts in practice again. `beginBusy`/`endBusy` (busy label via state + `busyRef` so keyboard handlers never read stale state), `mutateDoc(fn)` (marks dirty), `updateBlock`/`convertBlock`/`removeBlock`/`moveBlock`/`insertAfter`/`appendBlock`/`setTitle` (useCallback ops), `reorderBlock(fromId, toId)`, `splitBlock(id, rest)`, `removeBlockFocusUp(id)`, `updateBlockTags(id, tags)`/`setDocTags(tags)`, `parseToBlocks()` (GET /api/documents/[id]/html → `parseHtmlToBlocks` → replace blocks + `source: "editor"`), `convert(goal)` (M6: single AI path — replace blocks, carry over practice data, record `lastConvertInstructionsVersion`), `openPreview(optionsOverride?)` (M6; **2026-08-10 M7 round 3:** accepts `PreviewOptions` — the sheet's toggles call `openPreview(next)` so the NEW values go straight into the POST; a `previewSeqRef` sequence guard discards out-of-order responses when toggles race (only the latest render applies), `copyPrompt(part)` (FR-39), `applyImportedBlocks(blocks)` (paste questions/blocks result — replace when empty, else append; closes both modals), `applyImportedHtml(doc, html)` (paste-HTML result — adopt the doc, resets snapshot info), `resetPractice()` (M6: clear every qa/paragraph/**essay** `userAnswer` + uncheck — 2026-08-10; **M7 round 6:** the confirm is an IN-APP red banner below the toolbar — `confirmingReset` state, never `window.confirm` — user: "when deleting a document it gives a browser pop up i dont want that"; the toolbar's "Reset practice" click arms the banner, Clear answers/Cancel disarm), `persist()` (M6: the server round-trip — POST when `!persistedRef`, else PUT, `{ doc, instructionsVersion }`; clears the dirty flag only when the doc didn't change mid-flight), `save()` (busy-wrapped persist for the Save button / Cmd+S), autosave effect (M6: 1.2s debounce on `[doc, loading, isDirty]` → quiet `persist()` → "Saved automatically"; **2026-08-13, to-do item 6 — page-leave flush:** a `visibilitychange` listener + a `pagehide` listener call an immediate flush — when the tab hides or the page is being unloaded, any pending dirty doc persists RIGHT NOW (no waiting for the debounce, which would be killed by the unload); guarded by the same `autosave` toggle + dirty check; listeners cleaned up on unmount), **`downloadPdfDisplay()` (2026-08-10 M7 round 3 — replaces the old 3-variant `downloadPdf(variant)`):** POSTs `{ doc, hidden: options.hidden, emptyLines: options.emptyLines }` to the pdf route (no variant — exactly what the preview shows), filename `safeFilename(title, "pdf")`; **`downloadHtmlDisplay()` (replaces `downloadHtml()`/`ensureSaved()`):** downloads the in-memory `previewHtml` string as a Blob (`text/html;charset=utf-8`) — byte-identical to the sheet; **`downloadJsonDisplay()` (2026-08-13, to-do item 7):** `JSON.stringify(docRef.current, null, 2)` as `<title>.json` — the ONLY artifact with every field (all blocks incl. translations/analyses/vocab/expressions/practice answers/suggestions + practice settings); wired to PreviewSheet's `onDownloadJson`; `downloadBlob()`/`safeFilename()`/`copyToClipboard()`/`blockHasContent()` helpers (2026-08-10: array-valued content — essay `paragraphs` — counted by any non-empty string, not just `term`/`def` rows); computed `counts` (FR-37 — status bar only, see below). State: `practiceMode` + `checked` (M6), **`detailed` (2026-08-10 M7 round 4 — replaces `focusMode`; `useState(false)`, UNCHECKED = focus mode is the default. User: "one should not show focus on top and have it selected… it should show the other way around like the detailed": the checked box is now "Detailed" = reveal translations/analysis/vocab; passed down to Toolbar → BlockList → Block → QaBlockForm)**, **`showPasteSmart` (2026-08-13, to-do item 9 — the Smart paste modal flag; `onPasteSmart={() => setShowPasteSmart(true)}` passes to Toolbar; `PasteSmartModal` renders after `PasteHtmlModal` with `onResult={applyImportedBlocks}` + `onImported={applyImportedHtml}` — the two result paths reuse the block/HTML importers)** + **`focusSnapshotRef` (2026-08-13, to-do item 4 — see `toggleDetailed` below)**, `previewOpen` + `previewHtml` (M6), **`previewOptions` (2026-08-10 M7 round 3 — replaces `previewHidden`):** `PreviewOptions = { hidden, emptyLines }` (`DEFAULT_PREVIEW_OPTIONS`); `previewOptionsRef` keeps the LATEST options for the post-POST async parts (a stale-closure fix — `optionsOverride ?? previewOptionsRef.current`); PreviewSheet's `onOptionsChange(next)` → `setPreviewOptions(next)` + `void openPreview(next)` re-renders immediately on every toggle; `previewSeqRef` guards out-of-order responses**, `lastConvertInstructionsVersion` (M6), `aiModel`, `instructionsVersion`, `snapshotInfo` + `useSnapshot`, modal flags (`showPasteQuestions`/`showPasteBlocks`/`showPasteHtml`/`showCopyDialog`; the CopyDialog render passes **`useSnapshot={useSnapshot}`** (2026-08-10 — the copy dialog's "For AI" payload resolves instructions the same way a conversion does)); refs `docRef`/`persistedRef`/`useSnapshotRef`/`lastConvertRef`/`busyRef`/`savingRef`/`convertRef`/`saveRef`/**`previewOptionsRef`/`previewSeqRef` (2026-08-10 M7 round 3)**/**`scrollRef` (M7 round 7 — the editor's `overflow-y-auto` scroll container, passed to FloatingDetailedToggle so it appears only when the toolbar is scrolled out of view)**. **M7 round 7 state:** `autosave` (`useState(true)` — loaded from localStorage `writer-app:autosave` in a mount effect with try/catch; `toggleAutosave()` flips it and persists `"on"`/`"off"`); the floating pill is rendered as a fixed sibling of the scroll area (`<FloatingDetailedToggle detailed={detailed} onToggleDetailed={toggleDetailed} containerRef={scrollRef} />`), and the status bar shows an **"Autosave off — Save manually (⌘S)"** chip whenever `!autosave`. **M7 round 9:** `<FloatingThemeToggle containerRef={scrollRef} />` mounts right after FloatingDetailedToggle — the fixed top-right 🌙/☀️ copy (same `scrollRef` scroller-tracking). **`toggleDetailed()` (M7 round 7, user: "when it toggle detailed… it shifts my question as new data is added… then it again shift me on another part"):** the Detailed scroll-anchor — captures the block row near the viewport top (`document.elementFromPoint(innerWidth/2, 100)` → `closest("[data-block-id]")`, recording its `getBoundingClientRect().top`), flips `detailed`, then in `requestAnimationFrame` (React has committed) scrolls by `newTop − oldTop` so the row returns to the exact viewport position; no-op when the anchor vanished or nothing moved. Both the toolbar toggle and the floating pill call it (they share the same setter). **2026-08-13 (to-do item 4 — focus retention, user: toggling Detailed used to drop the caret out of the answer field):** `toggleDetailed` now ALSO preserves the focused editor field — it captures `activeSnap` (`document.activeElement` when it carries a `data-focus-id` + caret `selectionStart/End`), prefers it, and falls back to `focusSnapshotRef.current` — a document-level **`pointerdown` (capture phase) listener that snapshots the last focused editor field BEFORE the click** (Chrome moves focus to the clicked checkbox on mousedown, so a click-time capture would see the checkbox); in the same rAF (after the scroll correction) it refocuses `getElementById(id)` and restores `setSelectionRange(snap.start, snap.end)` (try/catch for input types that throw); when the field only exists in the OTHER mode, a per-block fallback chain tries `["answer", "text", "heading", "p0"]` scoped to the same blockId; no snapshot → current behavior (no restore). Every editor field carries `data-focus-id="${blockId}:${field}"` (QaBlockForm: userAnswer/question/answer/questionTranslation/analysis/answerTranslation/grammarNote; ParagraphFields: translation/analysis; Block: essay heading/paragraphs/text/userAnswers). **convert() carry-over (2026-08-10 #2, bug fix — user-reported answers lost):** practice answers now survive conversion for EVERY block type — qa matches on question text (+ hide flags), paragraphs on exact text, essays on joined paragraphs; when the AI groups previously-separate paragraphs into ONE essay, their answers merge in order (`essayAnswerFromParagraphs` run-matcher, module-level helper). **2026-08-10 M7 round 4 (user: "we dont need view option as for the preview everything is in there"):** `setAllQaFlags` DELETED (the FR-35 bulk-flag actions + View dropdown are gone — the preview sheet's per-field toggles hide/show at render time without mutating block data; `counts` survives only for the status bar "X/Y hidden" display). **Editor column (user: "a lot of blank space around the text fields on laptop… on phone it is fine"):** `max-w-2xl` → `max-w-4xl` — the fixed 672px column left huge gutters on desktop; phone (full-width) untouched. **2026-08-10 M7 round 6 (user: "there is still a lot of empty space around the paragraph and questions"):** widened again `max-w-4xl → max-w-6xl` (1152px) — the remaining desktop gutters around the block fields are gone.

### `components/Toolbar.tsx` — Primary actions (client, M6 redesign, M7, FR-29/30/37/38/39/46/50)
- **Purpose:** Two-row toolbar: **title row** = ✎ brand + "Writer" wordmark as a **link home (M7 — clicking the Writer icon/text returns to the dashboard)** + borderless large title input + subtle tags input (M5, FR-5/18) — **nav links (2026-08-10 M7 round 6b — user: "add library on navbar too"):** the title row carries a **Library link** (→ `/library`, subtle zinc text link beside the brand — the real library/folders page needs a doorway from the editor; the brand itself still links home) + **🌙/☀️ `ThemeToggle` (M7 round 7 — the dark-mode toggle, right after Library)**; **Instructions moved to the home screen (2026-08-10 M7 round 4, user: "instructions tab is in the document why… put it on home screen") and the Library link was removed then but RESTORED in round 6b** — the Library link removed (user: "dont need button for library as it is basically home and can be go there using the writer icon" — the brand link IS the way home); **actions row (2026-08-10 M7 round 4 tidy — three groups separated by `border-l border-zinc-200 pl-2.5`):** (1) **Rethink with AI split button** (renamed 2026-08-10 M7 round 9 — user: "change the text convert with ai to something else it dosent suit it"; the busy label is "Rethinking…"; primary rethinks; caret opens the goal input + snapshot-rules checkbox labeled "Rethink with this document's snapshot rules (v…)"; M6: single AI mode, template removed) + Save + Preview (M6: full-screen sheet) + **Autosave pill (M7 round 7 — checked = quiet debounced save, the default; off = only Save / ⌘S persists; preference in localStorage `writer-app:autosave`)**; (2) Practice group — **Practice toggle** (M6 master key, emerald active tint) + **Detailed checkbox** (M7 round 4 — the INVERTED Focus checkbox: UNCHECKED = focus mode is the default; checking reveals translations/analysis/vocab; title: "Detailed mode: show translations, analysis and vocabulary. Off = focus on the main content only (the default)") + when practiceMode: Check answers / Hide answers button (emerald when checked) + a quiet **`Reset practice`** text link (M7 round 4 — the View dropdown is gone; this is its only practice-only action, now `text-xs text-zinc-400 hover:text-zinc-700`); (3) **Copy…** button (single, opens the FR-50 copy dialog) + **Paste ▾** (blocks (AI) / questions / HTML — M6 adds blocks; **2026-08-13, to-do item 9:** "Smart paste…" is the FIRST dropdown item — hint "One box — detects questions, blocks, or HTML" — the three dedicated Paste buttons stay as shortcuts). **2026-08-10 M7 round 3 (user: "download option should be moved to preview"):** the Download ▾ dropdown is GONE — downloads live in the Preview sheet as Download PDF + Download HTML (the user sees the exact document before downloading); `onDownloadPdf(variant)` prop removed. **2026-08-10 M7 round 4 (user: "we dont need view option as for the preview everything is in there"):** the **View ▾ dropdown is GONE** (Hide/Show all translations + answers FR-35, "Reset practice answers…" in practice) — the preview sheet's field toggles cover hide/show at render time, so `VisibilityCounts`/`counts` and the 4 `onHideAll*/onShowAll*` props are deleted. `Dropdown` supports per-item `disabled` + `"—"` divider rows; all FR-* jargon removed from visible labels (kept in code comments).
- **Exports:** `TogglePill` (module component — shared checkbox-pill used by Practice, Detailed, **Autosave (M7 round 7)** and the **floating Detailed pill (FloatingDetailedToggle imports it — `export` added M7 round 7, matching this doc)**: `label`/`checked`/`onChange`/`title`/`activeCls`, white pill with a blue accent checkbox; Practice passes the emerald active tint). (`VisibilityCounts` export REMOVED 2026-08-10 M7 round 4; `ConvertMode` export REMOVED in M6.)
- **Functions:** `ActionButton` (module component), `Dropdown` (module component — fixed overlay for outside-click close), default export; convert-goal + tagsDraft state; props `practiceMode`/`checked`/`onToggleChecked`/`onResetPractice` (M6), **`detailed`/`onToggleDetailed` (M7 round 4 — replaces `focusMode`/`onToggleFocus`: unchecked = focus, the default)**, **`autosave`/`onToggleAutosave` (M7 round 7 — the Autosave TogglePill in the document-actions group; checked = quiet debounced save ON, the default)**, `onPreview` (M6), `onPasteBlocks` (M6), **`onPasteSmart` (2026-08-13, to-do item 9 — opens the Smart paste modal)**, `snapshotInfo`/`useSnapshot`/`onToggleSnapshot` (FR-23). (`onDownloadPdf`/`onDownloadHtml` REMOVED 2026-08-10 M7 round 3 — downloads moved into the preview; PDFVariant import gone; `onHideAllTranslations`/`onShowAllTranslations`/`onHideAllAnswers`/`onShowAllAnswers` REMOVED 2026-08-10 M7 round 4 — View dropdown gone.) **2026-08-10 #5 (user: "why are there 4 options — there should only be one"):** the Copy ▾ dropdown is GONE — a single **`Copy…` ActionButton** opens the FR-50 copy dialog (the three FR-39 items + `onCopyPrompt` prop were removed; the AI-instruction copies live in the paste sections).

### `components/FloatingDetailedToggle.tsx` — Floating Detailed pill (client, M7 round 7, NEW)
- **Purpose:** Fixed bottom-right copy of the toolbar's Detailed toggle so the user never has to scroll back to the top to reveal translations/analysis/vocab mid-document (user: "when I scroll down, I have to go to the top again if I want to toggle the detailed version of the page when I don't remember, what's the meaning of a question" — user chose the floating pill over a per-question translate reveal). It drives the SAME `detailed` state as the toolbar toggle (`onToggleDetailed` prop) — no new state, no divergence.
- **Constants:** `SHOW_AFTER_SCROLL = 200` — scroll distance (px) past which the pill appears (the two-row toolbar is out of view by then).
- **Props:** `detailed`, `onToggleDetailed`, `containerRef` (`React.RefObject<HTMLDivElement | null>` — the editor's `overflow-y-auto` scroll div, passed down from Editor as `scrollRef`).
- **Functions:** internal `useEffect` — tracks `scrolled` when EITHER scroller passes the threshold: `el.scrollTop > SHOW_AFTER_SCROLL` (container) **or** `window.scrollY > SHOW_AFTER_SCROLL` (**fix 2026-08-10 round-7 follow-up, user: "there is no floating togle"** — the app-shell height chain resolves against `min-h-full` on `<body>`, so the DOCUMENT scrolls at the window level and the container's scrollTop stays 0; the pill now listens to both with passive listeners and an initial call without waiting for the first event; cleans up on unmount/ref change). Renders `TogglePill` (imported from Toolbar — exported there M7 round 7) inside a `shadow-lg ring-1 ring-zinc-900/5` shell at `fixed bottom-5 right-5 z-40`; hidden state = `pointer-events-none translate-y-2 opacity-0` with a `transition-all duration-200` fade/slide. Covered by the preview sheet (z-50) when open.

### `components/ThemeToggle.tsx` — Dark-mode toggle (client, M7 round 7, NEW)
- **Purpose:** Tiny 🌙/☀️ icon button (h-7 w-7, hover:bg-zinc-100) that flips the `.dark` class on `<html>` — the variable-swap palette in globals.css does the rest (no `dark:` classes anywhere). Preference persisted to localStorage `writer-app:theme` ("dark"/"light"); `app/layout.tsx`'s beforeInteractive script applies it pre-paint so reloads don't flash. Reused in the editor toolbar title row (next to Library), the home header, the library header AND the floating top-right button (**FloatingThemeToggle**, M7 round 9). The preview sheet (sandboxed iframe, no `.dark`) always stays light.
- **Constants:** `THEME_KEY = "writer-app:theme"`; `THEME_EVENT = "writer-app:theme-change"` (M7 round 9 — custom event so every instance stays in sync).
- **Functions:** `isDark()` (module helper — reads the html class); mount effect syncs `dark` state with the html class, then listens for `THEME_EVENT` + the `storage` event (other tabs) and re-reads the class; `toggle()` flips the class + persists + dispatches `THEME_EVENT`. No props.

### `components/FloatingThemeToggle.tsx` — Floating dark/light button (client, M7 round 9, NEW)
- **Purpose:** User: "add a floating button for dark and light mode too ....top right" — the navbar's 🌙/☀️ scrolls away with the toolbar, so a fixed top-right copy follows the user down the document (the Detailed pill is bottom-right — the two never overlap). Fades in (opacity + -translate-y) only once the navbar scrolled out of view (`SHOW_AFTER_SCROLL = 200`px), hidden at the top where the navbar's own toggle is visible; dual window + container scroll listeners (same app-shell scrolling situation as FloatingDetailedToggle). Renders `ThemeToggle` in a `rounded-full bg-white p-1.5 shadow-lg ring-1 ring-zinc-900/5` shell (`bg-white` = the dominant-surface token, black in dark mode) at `fixed right-5 top-5 z-40` (below the preview sheet's z-50 overlay).
- **Props:** `containerRef` — the editor's scroll container ref (the editor's `scrollRef`).
- **Functions:** scroll effect (SHOW_AFTER_SCROLL threshold, both scrollers); renders the floating shell. State: `scrolled`.

### `components/BlockList.tsx` — Block list (client, M5 drag-reorder, M6)
- **Purpose:** Renders blocks in order with per-block controls and the bottom add-block affordance. Native HTML5 drag-and-drop reordering (M5, FR-3). **M6:** practice mode shows EVERY block (bug fix: paragraphs were hidden) — qa/paragraph/essay blocks render their "My answer" cards (via `Block`), title/heading read as read-only context; the bottom `AddBlockMenu` is hidden in practice so structure can't change mid-practice. **2026-08-10:** structure FULLY frozen in practice — `draggable={!practiceMode && …}`; `lastType` state (`"paragraph"` default) persisted to localStorage (`writer-app:add-type`), consumed by the split AddBlockMenu (direct `+` adds `lastType` via `onAppend`, caret picks a type) and by each block's `＋` add-below (`onInsertAfter(block.id, lastType)`).
- **Functions:** maps visible blocks → `Block` rows; `onDragStart`/`onDragOver`/`onDrop`/`onDragEnd` handlers (dragId/overId state); `pickType(type)` (set + persist `lastType`); props `practiceMode` + `checked` (M6) + **`detailed` (M7 round 4 — renamed from `focusMode`)** forwarded to `Block`.

### `components/Block.tsx` — Single block editor row (client, FR-1/3/25, M5, M6)
- **Purpose:** Text editing for title/heading/paragraph/essay (auto-grow textarea), Q&A form (renders `QaBlockForm` with M6 `mode`/`checked`), separator render, `/` slash-command popup (FR-2), FR-3 keyboard editing (Enter-split, Backspace-merge). **M6:** paragraphs render `ParagraphFields` (AI enrichment: translation/analysis/vocab/expressions) under the textarea when NOT in practice mode. **M6 practice (bug fix):** paragraphs render `PracticeParagraphCard`; title/heading render as read-only text; the slash menu / textarea editing are absent in practice. Header row: type label, heading-level select, per-block tags input (M5, FR-5), ↑/↓/＋/✕ control cluster (revealed on hover/focus); title/heading textareas get document typography.
- **2026-08-10 redesign:** `PracticeParagraphCard` is now a CONTINUOUS flow — paragraph text in normal document typography (no card, no "Paragraph" label, no duplicated boxes) with the dashed "My answer" textarea flowing directly beneath; checked → the answer box gets an emerald tint. The old boxed layout (separate labeled boxes) was removed per user feedback — "a paragraph is a continuous thing". Unused `inputCls`/`labelCls` import from RowEditor removed.
- **2026-08-10 essay (NEW block type):** `PracticeEssayCard` — the whole passage as continuous document-typography paragraphs + EXACTLY ONE "My answer" textarea (essays were being treated like per-paragraph Q&A; now one field for the whole essay). Normal mode: per-paragraph auto-grow textareas ("+ Add paragraph" button) + shared `ParagraphFields` enrichment. **Enter-split (FR-3) no longer applies to paragraphs/essays** — Enter inserts a newline (`block.type !== "paragraph"` guard); qa/heading still split. **Practice freeze:** controls cluster, tags, heading select, slash menu all `!practiceMode`-guarded.
- **2026-08-10 #3 (M7 — focus mode):** `detailed` prop threaded from BlockList; when NOT detailed (the default — focus mode), paragraphs render ONLY the textarea (no ParagraphFields enrichment) and essays render only the paragraphs (no shared enrichment) — Q&A delegates to QaBlockForm's focus branch.
- **2026-08-10 #5 (essay heading):** the normal essay editor gains an optional heading input above the paragraphs ("Essay title (optional)…", semibold — the AI fills it when possible); **`PracticeEssayCard` now shows ONLY the heading** (17px semibold — or a muted "(essay title)" placeholder) as the writing prompt + the single "My answer" textarea — the passage text is NOT shown in practice (user: "in practice it should only show the heading for the essay").
- **2026-08-10 M7 round 4 (user: "when I check the answer in paragraph it is showing me the translation fix it"):** the green "Reference" translation box is GONE from both practice cards — paragraphs and essays have no model answer, so revealing the English translation was wrong for French-first practice. Checking an answer now only EMERALD-TINTS the answer textarea (`border-emerald-400 bg-emerald-50/40` vs the unchecked `border-dashed border-blue-200`). A future practice-review option may add real reference material (noted in the code header comment).
- **2026-08-10 #2 (user feedback): French-first practice typography** — the passage text leads at `text-[17px] text-zinc-900`, the "My answer" textarea at `text-[16px]` (both French — the material being practiced), and the English Reference box is clearly secondary at `text-[13px] text-zinc-600`. Practice is for French, not English.
- **2026-08-10 #3 (user feedback): preview/output French-first typography** — root cause: the template's secondary sizes were `rem` (root-relative = 16px), so English translations rendered at 14.4px while French paragraphs sat at the 10.5px print base — English literally bigger. All secondary `font-size` values are now `em` (base-relative: 0.9em/0.88em/0.82em/0.78em/0.8em) and `.block-paragraph` leads at `1.15em` (≈12.1px in print). PDF mirrors with `basePt * 1.15` for paragraphs/essays, translations stay `basePt * 0.9`.
- **2026-08-10 round 2 — auto-grow everywhere:** the local `autoGrow` callback-ref helper was REMOVED (moved to `lib/auto-grow.ts`); the practice answer textareas (`PracticeParagraphCard`, `PracticeEssayCard`) and the essay's per-paragraph textareas are now `AutoGrowTextarea`. The primary title/heading/paragraph textarea keeps its own ref+effect (needs `selectionStart` for Enter-split).
- **2026-08-10 M7 round 7:** the block row root div carries **`data-block-id={block.id}`** — the scroll-anchor target for the editor's `toggleDetailed` (elementFromPoint + closest).
- **2026-08-10 M7 round 9 (subtle "Unanswered" state):** `PracticeParagraphCard` and `PracticeEssayCard` answer textareas now switch from dashed-blue to `border-dashed border-amber-300 bg-amber-50/40` while `userAnswer` is empty (answered = plain blue dashed, checked = emerald as before) — unanswered items are spot-able in light mode without any loud styling (user: "the questions which are not answered are not easy to spot in light mode… it should be subtle").
- **Exports:** `BLOCK_LABELS`, `SLASH_TYPES` (essay added after paragraph); `PracticeParagraphCard` + `PracticeEssayCard` (module components).
- **Functions:** `handleKeyDown`, `handleChange`, `applySlash(type)`; tags input (`tagsDraft`, commit on blur/Enter). **2026-08-13 (to-do item 4 — focus retention):** `blockId` prop threaded into `PracticeParagraphCard`/`PracticeEssayCard` + both `ParagraphFields` call sites (essay + paragraph); `data-focus-id` added — practice paragraph/essay answer textareas `${blockId}:userAnswer`, essay heading `${blockId}:heading`, essay per-paragraph `${blockId}:p${i}`, the main paragraph textarea `${blockId}:text` (the fallback chain targets for the Detailed-toggle refocus).

### `components/QaBlockForm.tsx` — Q&A card editor (client, FR-4/26/33/34/37, M6)
- **Purpose:** Two modes driven by the Practice master key. **Normal:** question + ALWAYS-visible "Answer" (modelAnswer) textarea with 👁/🙈 toggle (sets `hideModelAnswer`), optional extras behind "＋ chips" (question translation + EyeToggle, grammar note, answer label, answer translation, analysis, vocab, expressions) — M6: the reference answer is no longer an optional chip and `userAnswer` is no longer a normal-mode field; a muted note appears when a practice answer exists. **Practice (unchecked):** blue-tinted read-only question + "My answer" box (userAnswer, dashed border). **Practice (checked):** + green read-only "Model answer" box (or a "No model answer saved" placeholder). Rounded card container.
- **2026-08-10:** chips are SUPER subtle — `opacity-0 transition-opacity duration-200 group-hover:opacity-100 group-focus-within:opacity-100` (hover-reveal on the block's `group` root; 99% of enrichment comes from the AI, manual entry is the rare fallback).
- **2026-08-10 round 2 — "Suggested corrections" list:** under the Answer textarea (normal mode only, never practice), `SuggestionList` renders `visibleSuggestions(content)` — amber-tinted rows with a kind badge (spelling amber / grammar orange / punctuation rose), a Question/Answer field tag, ~~original~~ → suggestion, italic reason, an **Apply** button (`onUpdate(applySuggestion(content, s))` — changes ONLY that one, first occurrence) and a **✕** dismiss (`onUpdate(dismissSuggestion(content, s.id))`). Rows whose `original` the user already edited away simply don't render; dismissed rows are removed from content and stay gone. All logic lives in `lib/suggestions.ts` (pure, smoke-tested).
- **2026-08-10 round 2 — auto-grow:** modelAnswer, analysis and the practice "My answer" textareas are now `AutoGrowTextarea` (grow with content, re-measured on every render so AI imports and suggestion Apply re-grow them).
- **2026-08-10 #3 (M7):** **focus mode** — when `detailed` is false (the DEFAULT — 2026-08-10 M7 round 4 renamed `focusMode?: boolean` → `detailed?: boolean` with the semantics inverted: `if (!detailed)` renders ONLY the Question input + Answer textarea, no suggestions, no chips, no extras). **Field order (M7):** normal-mode JSX reordered to question → questionTranslation → analysis → Answer (+SuggestionList) → answerTranslation → chips → grammarNote → vocab → expressions — the editor matches the preview/PDF order ("question then translation then analysis then answer part"; 2026-08-10 M7 round 5: "→ responseLabel" dropped from the order — the label is fixed).
- **2026-08-10 M7 round 5 (user feedback):** the **👁/🙈 `EyeToggle` is GONE** (component deleted — it was used only on the question translation and the answer; user: "remove the view or hidden togle from question translation and answer as it is now in preview"). The per-block `hideTranslation`/`hideModelAnswer` flags remain in the data model and the renderers keep honoring them (legacy documents), the editor just no longer edits them — the preview sheet's field checkboxes control visibility at render time. The **Answer label (`responseLabel`) field is GONE too** (user: "it is not going to change at all so why there is option for user to write the reponse tag") — removed from the `QaField` union, `OPTIONAL_FIELDS`, `usedFields` and the JSX; blocks still carry `responseLabel: "RÉPONSE"` by default (createBlock/structuring) and the renderers keep using the stored value.
- **2026-08-10 M7 round 9:** **`synonyms` field** added to the `QaField` union + `OPTIONAL_FIELDS` ("+ Synonyms" chip — label "Synonyms"), `usedFields` auto-reveals when content exists, `hideField` clears `synonyms`, and a `RowEditor` block renders it (placeholderTerm "synonym" / placeholderDef "meaning") — the AI recommends richer words per block; **subtle "Unanswered" state** — in practice mode, the "My answer" `AutoGrowTextarea` shows `border-dashed border-amber-300 bg-amber-50/40` while `userAnswer` is empty (answered = plain blue dashed as before) and the label gains a small amber `Unanswered` text next to "My answer" (inherits labelCls uppercase styling, no icons — subtle per user). **Round-9 follow-up fix (user: "the fields where there is no answer i cant see the amber in both light and dark mode" + "still no glow or anything"):** the user meant the **WRITER's Answer field** (a question with no model answer written), so the SAME subtle amber treatment now applies to the `modelAnswer` textarea in BOTH the focus-mode and detailed-mode renders — `border-dashed border-amber-300! bg-amber-50/40!` while `modelAnswer` is empty (typing flips it back to a plain `inputCls` field) plus a small amber `Unanswered` label next to "Answer". The **`!` important suffix is required on every state color** (`border-blue-200!` / `border-amber-300! bg-amber-50/40!` on the practice box too): Tailwind v4 sorts same-property utilities ALPHABETICALLY, so inputCls's `border-zinc-200`/`bg-white` (z > a, w > a) otherwise win the cascade and the amber never renders — in either mode (verified in the generated CSS: `.border-amber-300\!` etc. carry `!important`; `.dark` leaves amber at its defaults so #fcd34d + #fffbeb@40% read on pure black). The `Unanswered` label and the emerald "Model answer" label were already fine (their color classes sit on child elements). **Placeholder removal (user: "remove the line reference answer… leave empty for a question where there is no answer"):** the Answer textarea's `Reference answer… (leave empty for a question without a provided answer)` placeholder is deleted in BOTH the focus-mode and detailed-mode renders — an unanswered question's field is genuinely empty now (the amber + label carry the signal instead).
- **Functions:** `usedFields(content)` (module-level), `SuggestionList` (module component), default export form; `hideField(key)` clears + hides, `reveal(key)` shows; `RowEditor` + `inputCls`/`labelCls` MOVED to `components/RowEditor.tsx` (M6). **2026-08-13 (to-do item 4 — focus retention):** new `blockId: string` prop; every editor field carries `data-focus-id` — practice "My answer" `${blockId}:userAnswer`, focus-mode question `${blockId}:question` + Answer `${blockId}:answer`, detailed-mode question/answer `${blockId}:question`/`${blockId}:answer`, `questionTranslation`, analysis (AutoGrowTextarea), `answerTranslation`, `grammarNote` — the Detailed-toggle refocus targets (see Editor's `toggleDetailed`).

### `components/RowEditor.tsx` — Shared form primitives (client, M6, NEW)
- **Purpose:** Extracted from QaBlockForm so Q&A cards and paragraph enrichment share identical styling and the term/def row editor (vocabulary / expressions lists).
- **Exports:** `inputCls`, `labelCls` (shared class strings), `RowEditor({ rows, onRows, placeholderTerm, placeholderDef, termCls })` (term/def row list with add/remove). (2026-08-10: "+ Add row" button restyled to the muted emerald-hover look.)

### `components/ParagraphFields.tsx` — Paragraph/essay enrichment editor (client, M6, NEW)
- **Purpose:** AI enrichment for paragraphs AND essays (2026-08-10 — props loosened to `ParagraphContent | EssayContent`, so the ONE shared enrichment set of an essay edits here): translation input, analysis textarea (now `AutoGrowTextarea`, 2026-08-10), vocab/expressions RowEditor lists — mirrors QaBlockForm's "＋ chip" pattern: empty optional fields stay hidden, a chip reveals each, removing a field clears its data. Rendered under paragraph/essay editors when not in practice mode. **M7 round 9:** `synonyms` added to `ParagraphField` + `FIELDS` ("+ Synonyms" chip), auto-reveal + hide-clear, and a RowEditor block (term "synonym" / def "meaning").
- **2026-08-10:** chip row label renamed to "AI details:" (enrichment is AI-driven; manual entry is the fallback) and styled SUPER subtle — `opacity-0 group-hover:opacity-100` hover-reveal, same as QaBlockForm.
- **Functions:** `usedFields(content)` (module-level — auto-reveal), default export; `reveal`/`hideField`/`set` helpers.
- **2026-08-13 (to-do item 4 — focus retention):** new `blockId: string` prop; the translation + analysis fields carry `data-focus-id` (`${blockId}:translation` / `${blockId}:analysis`) so the Detailed-toggle refocus can find them.

### `components/AutoGrowTextarea.tsx` — Auto-growing textarea (client, 2026-08-10, NEW)
- **Purpose:** Wrapper around `lib/auto-grow.ts` for every multi-line text field (Q&A answer/analysis/practice, ParagraphFields analysis, practice paragraph/essay answers, essay paragraphs). Adds `resize-none overflow-hidden` (without overflow-hidden the scrollbar makes scrollHeight jitter) and a no-deps `useEffect` that re-measures on EVERY render — typing, mount, AND programmatic value changes (AI convert, suggestion Apply) that fire no onInput event.
- **Functions:** none (props = `React.TextareaHTMLAttributes`; forwards `onInput` after growing). **2026-08-10 #5 (user: "the text box goes out of limit — make it scrollable"):** base class now `resize-none overflow-y-auto max-h-[45vh]` — very long content (big pastes in the paste modals, long answers) scrolls inside the box instead of growing past the window.

### `components/AddBlockMenu.tsx` — Split "+" add control (client, FR-2, 2026-08-10 rewrite)
- **Purpose:** Split control (user feedback: "should have a dropdown and a default choice, and later the last chosen option"): the main button is `+ <LABELS[lastType]>` — a DIRECT click adds the last-chosen type (default `paragraph`); the `▾` caret opens the full type menu (paragraph/essay/heading/qa/title/separator — `ITEMS`), with `✓ default` marking the current `lastType`; picking an item from the menu adds it AND becomes the new default. One pill, two buttons, emerald hover.
- **Exports:** `ITEMS` list, `LABELS` map; default component `{ lastType, onAdd(type), onPickType(type) }`.

### `components/PreviewSheet.tsx` — Full-screen preview (client, M6, NEW; **rewritten 2026-08-10 M7 round 3**)
- **Purpose:** "Preview" opens this full-screen sheet (fixed inset-0 z-50) instead of the old always-on pane. Header: title, the **download buttons (2026-08-10 M7 round 3 — the toolbar's Download dropdown moved here, user: "user should be able to see the exact document before downloading"):** **Download PDF** (`onDownloadPdf` — posts `{ doc, hidden, emptyLines }`, exactly what the sheet shows; filename `safeFilename(title, "pdf")`, no variant suffix) + **Download HTML** (`onDownloadHtml` — the in-memory `previewHtml` string as a Blob, byte-identical to the sheet) + **Download JSON (2026-08-13, to-do item 7 — the JSON download moved HERE from the library cards):** `onDownloadJson` — the current doc serialized as `<title>.json` with EVERY field (practice answers included — the only artifact with them), disabled while `busy || !html`; a "no saving needed" hint, ✕ close. **Refresh REMOVED (2026-08-10 — it was redundant; toggles re-render automatically).** Body: A4 sheet in a sandboxed iframe (`sandbox=""` — no scripts, no same-origin, per suggestions.md). Placeholder when there's no HTML yet.
- **2026-08-10:** iframe class `mx-auto block min-h-[600px] w-full max-w-[210mm]` — transparent, so the printMode A4 sheet (its own white + shadow) shows against the sheet backdrop; the old `bg-white` + outer shadow were removed. **2026-08-10 #4:** the sheet content is now the spec's screen rendering (11.5px base, 18mm) — the iframe itself is unchanged.
- **2026-08-10 #3 (M7 — field toggles):** header has a "Show:" box — **"All extras"** master checkbox + per-field checkboxes (Translations / Analyses / Vocab & expressions / Model answers) + a note "Headings, questions & paragraph text stay" when anything is hidden. **Semantics (fixed 2026-08-10 #5 — was inverted):** every checkbox is `checked` = SHOWN — per-field `checked={!hidden[key]}` (toggling flips that key), master `checked={!anyHidden}` (`anyHidden = FIELD_LABELS.some(f => hidden[f.key])`); `toggleAll()` sets all four to `false` when ANYTHING is hidden (clicking "All extras" SELECTS all four options), else sets all `true` (hides all). **2026-08-10 M7 round 3 — "Empty lines" checkbox (user: "empty lines if selected should show empty lines as in download only questions"):** bordered on the left (`border-l`), `checked={options.emptyLines}` — toggling calls `onOptionsChange({ ...options, emptyLines: !options.emptyLines })`; it replaces the old "questions" PDF variant, so the variants dropdown is gone entirely.
- **Functions:** none (stateless — props `html`, `busy`, `options: PreviewOptions`, `onOptionsChange(next)`, `onDownloadPdf`, `onDownloadHtml`, **`onDownloadJson` (2026-08-13, to-do item 7)**, `onClose`). `PreviewHidden`/`PreviewOptions` types + `EMPTY_PREVIEW_HIDDEN`/`DEFAULT_PREVIEW_OPTIONS` live in `@/lib/types` (imported here — no local re-exports).

### `components/PasteQuestionsModal.tsx` — Paste-questions import (client, FR-32/38)
- **Purpose:** Single-step flow: paste a question list → live count of detected questions → "Structure with AI" (POST /api/convert/structure) or "Parse locally (offline)" (`questionsToQaBlocks`) → `onResult(blocks)`.
- **Functions:** `structureWithAI()` (async, inline errors), `parseLocally()`; `splitQuestions` memo on the pasted text.
- **Note (UI polish):** all four modals (`PasteQuestionsModal`, `PasteBlocksModal`, `PasteHtmlModal`, `CopyDialog`) share a unified chrome — `bg-zinc-900/40` backdrop with blur, rounded-2xl white panel with header bar (title + ✕ close) and a border-t footer with right-aligned Cancel/primary buttons.
- **2026-08-10 (M6 round 2):** the paste textarea is now `AutoGrowTextarea` (grows with a long paste; `autoFocus` passes through the props spread).

### `components/PasteBlocksModal.tsx` — Paste AI blocks back (client, M6, NEW)
- **Purpose:** The JSON block array from "Copy → Copy for AI" (or any external AI) re-imports client-side via `parseStructuredBlocksResponse` (lib/structuring.ts) — completing the copy → external AI → paste round-trip (FR-42) for the new block format. Inline error when no valid blocks are found.
- **Functions:** `importBlocks()` (validates + `onResult(blocks)`).
- **2026-08-10 (M6 round 2):** paste textarea → `AutoGrowTextarea`.
- **2026-08-10 #3 (M7):** an `InstructionCopyBox` ("Instructions for another AI") sits ABOVE the textarea — the module constant `BLOCKS_INSTRUCTION` (the JSON-shape contract + essay-grouping + never-invent-answers + never-include-user-answers rules) with a one-click copy button, so the user can hand the other AI its parsing context (user request; Paste questions intentionally unchanged). **2026-08-10 #5:** the essay shape in `BLOCKS_INSTRUCTION` now carries the optional `"heading"` + the never-invent-never-force rule. **2026-08-10 M7 round 9 (user: "if i ask ai to convert it will it give me suggested synonyms or not?"):** `BLOCKS_INSTRUCTION` is now **composed from the shared `BLOCK_FORMAT_SPEC`** (exported from lib/prompt.ts — `Convert the French practice material below into structured document blocks. ${BLOCK_FORMAT_SPEC} Keep every provided answer verbatim.`) instead of a hand-copied string — the synonyms shapes + SYNONYMS rule and the suggestions/CORRECTIONS rules were missing here, so the external-AI flow would NOT have produced synonyms/corrections while the in-app Rethink-with-AI flow did; by construction they can no longer drift.
- **2026-08-10 M7 round 3 (user: "paste blocks going out of screen even when the field is scrollable"):** the CARD scrolls as a whole — `max-h-[88vh] w-full max-w-xl overflow-y-auto` (the scrollable textarea alone wasn't enough: instruction box + textarea + buttons could still push the modal off-screen). Same fix in `PasteHtmlModal`.

### `components/PasteHtmlModal.tsx` — Paste HTML back (client, FR-40)
- **Purpose:** Paste HTML from any external AI → POST /api/documents/import-html → `onImported(doc, html)` (editor adopts the document; the amber "Parse to blocks" banner offers editing).
- **Functions:** `importHtml()` (async, inline errors).
- **2026-08-10 (M6 round 2):** paste textarea → `AutoGrowTextarea`.
- **2026-08-10 #3 (M7):** an `InstructionCopyBox` ("Instructions for another AI") above the textarea — `HTML_INSTRUCTION` constant (print-ready A4 worksheet spec for the other AI: base font 11.5px/10.5px printed, 18mm/14mm margins, Georgia, French-first) with one-click copy (user request).
- **2026-08-10 M7 round 3:** whole card scrolls (`max-h-[88vh] … overflow-y-auto` — same fix as PasteBlocksModal).

### `components/PasteSmartModal.tsx` — Smart paste box (client, 2026-08-13, to-do item 9, NEW)
- **Purpose:** ONE paste area that routes content to the right import without asking which kind it is — the paste-buttons shortcut trio stays, this is the "I don't know" box. A single `AutoGrowTextarea` + a live "Detected: X — N questions" indicator (from `sniffPasteKind` in `lib/paste-sniff.ts` + `splitQuestions` for the question count). The footer buttons are CONTEXTUAL — the modal shows only the action that fits what was detected: `[` → **Import blocks** (`parseStructuredBlocksResponse` → `onResult(blocks)`), `<` → **Import HTML** (POST `/api/documents/import-html` → `onImported(doc, html)`), otherwise → the SAME two options as PasteQuestionsModal: **Structure with AI** (POST `/api/convert/structure`) + **Parse locally (offline)** (`questionsToQaBlocks` → `onResult`). The result paths are exactly the existing importers — no new parse code.
- **Props:** `{ onClose, onResult: (blocks: Block[]) => void, onImported: (doc: Document, html: string) => void }` (the Editor passes `applyImportedBlocks`/`applyImportedHtml` — the same handlers as the dedicated modals).
- **Functions:** `importBlocks()`, `importHtml()` (async, inline errors), `parseLocally()`, `structureWithAI()`; `KIND_LABEL` map (blocks → "AI blocks", html → "HTML document", questions → "questions").

### `components/CopyDialog.tsx` — Copy dialog: exactly two options (client, FR-50, M7)
- **Purpose:** Exactly TWO tabs (user request — "this option and the ai instructions should be fine only these 2"): **"For sharing"** — checkboxes choose exactly what goes to the clipboard as clean plain text (paragraphs, title/headings, questions, user answers, model answers, translations, grammar notes, analysis, vocab). Translations + model answers off by default; Q&A numbering preserved (1., 2., …); last-used selection remembered in localStorage (`writer-app:copy-selection`); live preview of the output. **"For AI"** — one-click copy of `buildAICopyText(doc, instructions)` where `instructions` is fetched on open from `GET /api/instructions?docId=…&useSnapshot=true` when the snapshot toggle is on (else plain GET — same resolution a conversion uses, FR-23) so the copied payload uses the SAME rules as Convert with AI; payload = instructions + JSON format spec + `=== CONTENT ===` + serialized document (see lib/prompt.ts). The user can take the document to any other AI and paste its JSON response back via Paste blocks (AI) — no AI call in the app. Clipboard with textarea fallback; Copy disabled while instructions load (or on failure) on the AI tab, and when nothing is selected on the sharing tab. Props: `doc`, `useSnapshot` (from Editor's FR-23 toggle), `onClose`.
- **Exports:** `CopySelection` interface, `DEFAULT_SELECTION`, `buildCopyText(doc, sel)` (pure — covered by smoke tests), **`PRESET_WORKSHEET_NO_ANSWERS` + `PRESET_QUESTIONS_ONLY` (2026-08-13, to-do item 9 — pure `CopySelection` constants so the smoke tests pin what each preset copies):** worksheet = paragraphs/headings/questions/translations/grammar/analysis/vocab on, userAnswers + modelAnswers off; questions-only = ONLY questions (`{ paragraphs: false, headings: false, questions: true, …all false }`).
- **Functions:** `joinVocab` (private), `loadSelection` (private), `toggle(key)`, **`writeClipboard(payload)` (2026-08-13, to-do item 9 — extracted: navigator.clipboard + execCommand textarea fallback, shared by `copy()` and the presets)**, `copy()`, **`applyPreset(preset)` (2026-08-13, to-do item 9 — sets the selection state AND copies immediately: `setSelection(preset)` + `writeClipboard(buildCopyText(doc, preset))` — can't rely on the selection state update, so it builds the text from the preset directly; "Presets:" button row beside the checkbox grid in the share tab)**; **M7:** `tab` state (`"share" | "ai"`) + header tab buttons; `aiText` = `buildAICopyText(doc)` (memo); copy uses `tab === "share" ? text : aiText`. **2026-08-13 (to-do item 1 — French headings + honest labels, user: "keep my practice answers… Modèle : → Réponse : etc"):** checkbox labels — "User answers" → **"My practice answers"** (helper "what I wrote in practice"), "Model answers" → **"Answers"** (helper "the correct answer — the Answer field"); `buildCopyText` qa labels — model answer `Modèle :` → **`Réponse :`**, practice answer → **`Ma réponse :`** (was `Réponse :`), question translation gains **`Traduction de la question :`** (was bare), answer translation **`Traduction de la réponse :`** (was `Traduction :`), grammar note **`Grammaire :`** (was bare). **2026-08-10:** `buildCopyText` case "essay" — paragraphs joined with blank lines under the `paragraphs` selection, `Ma réponse : …` under `userAnswers` (indented when paragraphs are also copied). **2026-08-10 M7 round 9 (synonyms):** the qa branch's `vocab` selection also emits `Synonymes : term : def ; …` (after Expressions) via the same `joinVocab` — the sharing copy carries synonyms alongside vocab/expressions.

### `components/InstructionCopyBox.tsx` — Copyable instruction box (client, 2026-08-10, M7, NEW)
- **Purpose:** Shared box ABOVE the JSON and HTML paste textareas (user request: "provide the copy instructions for each in above the text box which user can use to provide other ai the context in which it need to parse the data"): a blue-tinted panel with the instruction title ("Instructions for another AI"), the instruction text, and a one-click **Copy instruction** button (navigator.clipboard + execCommand fallback → "Copied ✓"). Reused by PasteBlocksModal (`BLOCKS_INSTRUCTION`) and PasteHtmlModal (`HTML_INSTRUCTION`); Paste questions intentionally unchanged (user likes it as-is). **2026-08-10 M7 round 3 (user: "hide the instructions, give a toggle to see them — user will mainly use the copy button"):** the instruction TEXT is COLLAPSED by default — the header row (title + **Show/Hide** toggle + Copy instruction button) always stays visible; the text renders only while `open` (default `false`). The Copy button is NEVER hidden, so the common path is one click.
- **Functions:** internal `copy()` with clipboard + fallback; `copied` state resets after 2s; `open` state toggles the instruction text.

### `components/NewDocumentButton.tsx` — New-document button (client, M6, NEW; M7 round 6b: folderId)
- **Purpose:** Home page + library empty state: creates a fresh document client-side (`createDocument()` + one empty paragraph block, FR-24) and navigates to `/doc/<id>`; the first save then persists it. **2026-08-10 M7 round 6b (user: "when I click on [a folder] and make a document in there it doesn't automatically move it there"):** optional `folderId` prop files the new document into a library folder from the very first save (the editor's folder picker is a move, not a create) — LibraryList renders this button with the ACTIVE folder when a folder filter is selected ("+ New document in 'name'").
- **Functions:** `createNew()` (router.push to the new id; assigns `doc.folderId` when the `folderId` prop is set). Optional `className`, `folderId`, `label` props for call-site styling.

### `components/TestDialog.tsx` — Test generator dialog (client, 2026-08-13, to-do item 5, NEW)
- **Purpose:** "Test…" button + modal on the home page AND the library (the long-planned test-generator feature — was "PLAN ONLY"). Pick source documents (checkbox list with per-doc counts "N Q · N E"; rows disabled when a doc has no qa AND no essay blocks), choose type — Questions / Essays / Both — with "How many questions"/"How many essays" count inputs (each DISABLED when its type isn't selected; non-positive → red border + invalid), optional "Let AI pick randomly" checkbox (no counts → the AI picks), then **Create test**. **AI path:** POST `/api/test` `{ docIds, questions?, essays? }` → `router.push(/doc/${body.id})`. **Random path (no AI):** `buildTestDocument(selectedDocs, { questions, essays })` locally → POST `/api/documents` → `router.push(/doc/${body.doc.id})` — instant and free. At least one usable doc required; the result is a normal document titled "Test — <date>" — practice, preview, PDF, copy all work.
- **Props:** `{ documents: Document[] }` (rendered in the page headers).
- **Functions:** `createTest()` — dispatch between the AI and random paths; `toggleDoc(id)`; type radio state; count inputs wired to the selected type.

### `components/LibraryList.tsx` — Library cards (client, FR-18/19/20, M5, M6, M7 round 6, round 6b)
- **Purpose:** Document cards (title → editor `/doc/<id>` — M6, updated date, block count, tags) + **⬇ download PDF (2026-08-13, to-do item 7 — was the all-fields JSON download since M7 round 6b; the user decided ⬇ = the printable worksheet: `GET /api/documents/[id]/pdf?variant=full` → `<title>.pdf`; the JSON download MOVED to the Preview sheet — see Editor/PreviewSheet)** + Delete; client-side sort control (updated/created/title); M5: clickable `#tag` chips row with filter state, "⬇ Backup zip" button (filename parsed from the Content-Disposition header). M6: empty state uses `NewDocumentButton` (creates a fresh doc and navigates, instead of linking to `/`). **2026-08-10 M7 round 6:** NO browser popups — document delete is a two-step IN-APP confirm inside the card (Delete → red banner "Delete? JSON/HTML/PDF removed" → Delete/Cancel; `confirmingDelete` state), `alert()` failures are an inline red error banner (`error` state); **folder bar**: "All · N" + per-folder chips (name + count, filter toggle, ✎ inline rename, 🗑 two-step delete — deleting only unfiles) + "No folder · N" chip + "+ New folder" inline input; per-card "Move to…" folder select (PATCH, `pendingMove` busy state, folder name line on filed cards); **`recent` mode** (home). **2026-08-10 M7 round 6b (user: "folder shows as a tag… when I click on it and make a document in there it doesn't automatically move it there… Document card should be the same on the home and the library page"):** the **folder bar renders on HOME too** (previously `!recent`-gated) and the **per-card move select is on home cards too** — cards are identical on both pages; home passes the FULL documents list + `limit` (grid = first `limit` by updatedAt desc via the `gridSource` memo, while `allTags`/`folderCounts` chips describe the full list, so "All · N" and folder counts are accurate on home); **with a folder selected, "+ New document in 'folder'"** (emerald `NewDocumentButton` with `folderId` = the active filter) files the new document from its first save — no library detour; **↻ Regenerate removed from the card** (`regenerate()`/`regenerating` deleted; the `/api/documents/[id]/regenerate` route stays as a server API) and replaced by **⬇** (`downloadDoc()` — GETs the document and downloads it as `<title>.json` with EVERY field: title/tags/folder/blocks incl. translations/analyses/vocab/expressions/practice answers/suggestions/practice settings; the editor's Download PDF covers the printable rendering).
- **Functions:** `remove(doc)` (the actual DELETE — called from the confirm banner), `downloadDoc(doc)` (**2026-08-13, to-do item 7:** fetch `GET /api/documents/[id]/pdf?variant=full` → Blob `<title>.pdf` — the full printable worksheet; was the JSON download in M7 round 6b), `formatDate(iso)`, `downloadBackup()`, `createFolder()`, `renameFolder(id)`, `deleteFolder(id)` (clears the folder filter if it targeted the deleted folder), `moveDoc(doc, folderId)`; `allTags` memo (usage-frequency sorted), `folderCounts` memo (docs per folder + "none"), `gridSource` memo (recent+limit slice — round 6b); `useMemo` filtered (tag + folder) / sorted list by `sort` key. (Regenerate — FR-20 — was deleted 2026-08-10 M7 round 6b.)

### `app/page.tsx` — Home dashboard (M6, NEW; M7 round 6: recents only; round 6b: full-list + limit)
- **Purpose:** `/` — SERVER component (was the client editor route). Header (brand mark, headline, document count) + **Instructions link (2026-08-10 M7 round 4 — user: "instructions tab is in the document why… put it on home screen": moved from the editor toolbar; a `next/link` styled like the other outlined buttons)** + **`ThemeToggle` (M7 round 7 — 🌙/☀️ dark-mode toggle; a client component imported into this server page)** + `NewDocumentButton` + **`TestDialog` (2026-08-13, to-do item 5 — the Test generator; rendered in the header beside NewDocumentButton)** + **`LibraryList` in `recent` mode (M7 round 6, user: "only show 10 there and a link to library"):** fetches documents + folders, passes the FULL document list with `limit={RECENT_LIMIT}` (10) + `total` — LibraryList renders the first 10 as cards while the folder chips/counts describe everything (round 6b) — with a "View all N documents in the Library →" link to `/library` (shown whenever there are documents). `export const dynamic = "force-dynamic"` (fs read at request time — never prerendered). Metadata "Home — Writer App".

### `app/doc/[id]/page.tsx` — Editor route (M6, NEW)
- **Purpose:** `/doc/<id>` — async server page: `const { id } = await params` (Next 16: params is a Promise), renders `<Editor docId={id} />`. `force-dynamic` (the editor is fully client-side — never prerender a specific document). Metadata "Editor — Writer App".

### `app/library/page.tsx` — Library route (M6 → M7 round 6: REAL page)
- **Purpose:** `/library` — **the full archive (2026-08-10 M7 round 6, user: "make a library page it will show all the documents… option for making folder too"):** SERVER component fetching EVERY document + all folders (`Promise.all`), header with ← home link, "Library" title + counts (documents · folders), Instructions link + `NewDocumentButton` + **`ThemeToggle` (M7 round 7 — 🌙/☀️ dark-mode toggle)** + **`TestDialog` (2026-08-13, to-do item 5 — the Test generator; in the header beside NewDocumentButton)**, then `<LibraryList documents folders />` (full mode — sort/tags/backup/folder bar/per-card move select). `force-dynamic`. Metadata "Library — Writer App". (The old `redirect("/")` stub is gone.)

### `app/instructions/page.tsx` — Instructions route (M4, FR-21/22)
- **Purpose:** `/instructions` — server page shell (`force-dynamic`), renders `InstructionsEditor`. Metadata "Instructions — Writer App".

### `components/InstructionsEditor.tsx` — Instructions editor (client, M4, FR-21/22/47)
- **Purpose:** Edit the active instructions (the ONE place the design system + AI rules live): loads GET /api/instructions, textarea with dirty tracking, Save (PUT — rejected client-side/server-side when the TOKENS block is missing, FR-47), **"Discard my edits — restore repo copy" (2026-08-13 relabel; confirm dialog → POST /api/instructions/reset — repo changes auto-sync since to-do item 10, so the button's remaining job is discarding YOUR OWN newer edits; auto-sync never overwrites the newer writer, and the current version is snapshotted to history before the overwrite)**, version history list (version hash, date, char count; Preview → fills the editor as draft, Restore → PUT), status/error banners; explains the `<!-- TOKENS -->` block + FR-47.
- **Functions:** `load()` (initial GET), `save()` (PUT, dirty-gated), `reset()` (confirm + POST), `previewVersion(entry)`, `restoreVersion(entry)`; dirty state via `useState`. **2026-08-10 #3 (M7 — crash fix, user crash report "Cannot read properties of undefined (reading 'length')" at line 211):** all four `entry.content` sites are now guarded with `?? ""` — `setDraft(entry.content ?? "")` (Preview ×2), `body: JSON.stringify({ content: entry.content ?? "" })` (Restore), and `{(entry.content ?? "").length} chars` (history list) — a history entry with no content can no longer throw.

### `app/layout.tsx` / `app/globals.css` / `public/`
- **Purpose:** Root layout (`LayoutProps<"/">` — Next 16.3 global helper), Tailwind v4 global styles, static assets. Metadata: "Writer App — Online Writer + Practice". `layout.tsx` also carries a `next/script` (id `theme-init`, strategy `beforeInteractive`) that applies the saved dark-mode preference (`writer-app:theme` === "dark" → `.dark` on `<html>`) BEFORE first paint — no flash of light (M7 round 7). `globals.css`: **light by default, dark via the `.dark` class (M7 round 7 — user asked for a dark-mode toggle; the old design-pass "light-only" claim is gone)**, body font routed through `var(--font-sans)` so the loaded Geist font actually applies, global focus ring + selection color.
- **2026-08-10 design pass:** the Tailwind v4 `@theme` palette is REMAPPED to the PDF spec's ramp so the editor UI and the printed sheet share one look — `--background: #f7f9fb`, `--foreground: #1a1a1a`; zinc-* remapped to the spec grey ramp (#f7f9fb → #1a2738), blue-* to the navy ramp (#eef2f7 → #0d1a2e, blue-600 = #1e3a5f), emerald-* to the green ramp (emerald-50 = #e8f0e9 … emerald-600 = #2c5f2d); `:focus-visible` outline #1e3a5f, `::selection` #b9c9de. Document/print styles live in the instructions file, not here (FR-47).
- **2026-08-10 M7 round 7 (dark mode):** every `@theme` token is now **variable-indirected** — `--color-zinc-50: var(--zinc-50, #f7f9fb)` etc. for all of zinc (10), blue (10), emerald (9) plus `--color-white: var(--white, #ffffff)` (the dominant surface token; the 64 `bg-white` uses flip with it); `:root` sets `--background: var(--bg, #f7f9fb)`, `--foreground: var(--fg, #1a1a1a)`, `--focus-ring: var(--ring, #1e3a5f)`, `--selection: var(--sel, #b9c9de)`; the **`.dark` block** (LAST in the file so it beats `:root` at equal specificity) re-defines the runtime vars: canvas `--bg: #000000` (M7 round 8: PURE BLACK — user: "the background should be black so that it saves battery"; OLED pixels are off at #000) with the dominant surfaces following (`--white: #000000` — toolbar/cards/modals/content area; hover/input tints stay near-black `--zinc-50: #0a0e15` + `--zinc-100: #11161f` so hover feedback is still visible, borders `--zinc-200: #1c2431` … zinc-900 #eef2f7), text `--fg: #e6ebf2`, primary `--blue-600: #3d6da8` (blue-50 #16233a … blue-900 #7ba4d6 — LIGHTER than light mode so they read on dark), `--emerald-600: #3d8a46` (emerald-50 #152c1a … emerald-800 #6ec479), focus ring #5b8ac9, selection #2a3e63. Every component class switches with ZERO `dark:` utilities. **Supporting sweeps:** `text-white` → `text-[#fff]` (29 sites across 10 files — button labels on colored backgrounds must stay literal white since `--white` remaps to the dark surface) and modal backdrops `bg-zinc-900/40` → `bg-black/40` (4 modals — zinc-900 inverts to the lightest ramp in dark, so a 40% zinc backdrop would become a light wash; black works in both modes). Semantic ramps (red/amber/rose/orange: banners, confirms, suggestion badges) keep Tailwind defaults. **2026-08-10 M7 round 9:** an **amber ramp was added at the END of the `@theme` block, AFTER `--color-white`** — `--color-amber-50: var(--amber-50, #fffbeb)` … `--color-amber-800: var(--amber-800, #92400e)` (dark: keeps Tailwind's light-mode defaults — the amber-300 dashed border + amber-50/40 tint were tuned for light mode where unanswered questions were invisible; the .dark block does NOT redefine amber so it still reads). **Round-9 cascade fix (2026-08-10, user: "i cant see the amber in both light and dark mode"):** the amber utilities exist, but Tailwind v4 sorts same-property utilities ALPHABETICALLY by name in the generated CSS — `border-zinc-200`/`bg-white` (z > a, w > a) are emitted AFTER `border-amber-300`/`bg-amber-50/40` and win at equal specificity, so the state colors on inputCls-bearing fields never rendered (in EITHER mode; the blue answered border had the same silent bug). Fix: the state classes in QaBlockForm's practice box carry the **`!` important suffix** (`border-amber-300! bg-amber-50/40!` / `border-blue-200!`), which beats the later zinc/white rules in both light and dark (verified against the generated CSS; amber-300 #fcd34d + amber-50@40% read on pure black since .dark leaves amber at defaults). Paragraph/essay practice boxes (Block.tsx) needed nothing — their base class string has no zinc border or bg-white. Comment in globals.css corrected (registration-order claim was wrong). The preview sheet is a sandboxed iframe without `.dark` — always light (print spec is light-only by design).

### `tests/` — Smoke tests (M2 + M3 + M4 + M5 + M6 round 2 + M7 + M8 + M9)
- **Purpose:** In-project verification harness. **`tests/run-all.ts` (2026-08-13 — the user's token-saving ask: "make sure to make a file that runs all the test as when you run each and every test 1 by 1 it burns a lot of tokken… if any test fail we can jsut check the output or run only that test alone"):** the single all-suite runner — compiles once (`cd tests && npx tsc -p tsconfig.json`), then `node tests/build/tests/run-all.js` spawns EVERY suite (smoke-m2…smoke-m9) as an isolated CHILD PROCESS (a crash in one can't take down the others), prints per-suite PASS/FAIL/CRASH + their output, exits 1 when any failed; `run-all.js m7` filters to one suite for a re-run without re-reading everything. `smoke-m2.ts` (26 checks: QA HTML + PDF variants — patched in M6: `{ practice: true }` → `{ variant: "questions" }`; **2026-08-10 M7 round 4: the b3 user-answer check flipped to "NOT rendered (preview)" — practice answers never render without showUserAnswers**; **2026-08-13, to-do item 8 + fixture fix: b3/b4 now carry `responseLabel: "RÉPONSE"` so the gating checks are real — "RÉPONSE only where an answer exists" = exactly 2 labels in the default render (b2 + b4), "RÉPONSE shows with Empty lines on" = exactly 4 (b2/b3/b4/b5 — b3's hidden answer + b5's missing answer get the blank ruled area to label)**), `smoke-m3.ts` (48 checks: fences/validation, splitQuestions, structuring parser, prompt serialization + M6 "JSON blocks demand" / "user answer never serialized", buildCopyText, titleFromHtml + **2026-08-13 item 9: 11 new — both copy presets (worksheet = question/translation/grammar/analysis/vocab present, NO model answer, NO practice answer; questions-only = exactly `"1. Qu'est-ce que tu as fait hier ?"` with nothing else) + sniffPasteKind (`[` → blocks, `<` → html, plain → questions, leading whitespace ignored, empty → questions)**), `smoke-m4.ts` (31 checks: hashVersion, seeding idempotence, save→history→cache-invalidate, TOKENS validation, reset, per-doc snapshot write via persistDocument — M6: version-gated, no version → no snapshot, resolveConversionInstructions active-vs-snapshot, history ordering/content + **2026-08-13, to-do item 10 — 6 new auto-sync checks:** repo file newer → next read returns REPO content + repo version; machine sync leaves history untouched (no phantom snapshot); user edit after a repo change wins (editedAt reflects the user save); missing active copy → editedAt 0; first-run read seeds repo content (FR-21 intact)), `smoke-m5.ts` (37 passed + 1 deferred: parse-back round trip through the real template generator (incl. the 2026-08-13 item-8 label stripping — "Traduction :"/"Grammaire :" don't survive the round trip), tags parsing, ZIP structural signatures + inflate round-trip, **2026-08-13, to-do item 7 backup checks:** `sanitizeBackupFolder` forbidden-char strip / whitespace collapse / trailing dots-spaces trim / empty → "document" + `shortCode()` 4 chars, zip entries inflate back to source bytes; 1 deferred check — vocab grid row term/def attribution, see suggestions.md; **2026-08-10 M7 round 4: the round trip passes `showUserAnswers: true` so the `.qa-user-answer` recovery path keeps being tested**), **`smoke-m6.ts` (24 checks, 2026-08-10 round 2:** suggestions preserved with uuid ids + kind default, malformed suggestion object → list dropped + qa block survives (tolerant parse), empty original/suggestion dropped, cap of 10, prompt demand carries the CORRECTIONS rule + suggestions shape, `serializeBlocksForAI` never serializes suggestions, applySuggestion first-occurrence-only / only-selected-removed / overlapping-stale-removed / stale-original-dismiss-only, dismissSuggestion by id, visibleSuggestions stale filter incl. question-field targeting, documentSchema round-trip keeps suggestions, pageNumberLabel 1/7 + 12/12**), **`smoke-m7.ts` (37 checks, 2026-08-10 M7 + round 2 + round 3 + round 4 + round 5 + round 9 + round 10:** buildAICopyText — instruction heading, both JSON shapes present (essay shape now with `"heading"`), `=== CONTENT ===` marker, content follows (marker serialization), never includes practice answers; **round 9 — synonyms: the qa `SYNONYMS: se réveiller|to wake up; de bonne heure|early in the day` marker round-trips through buildAICopyText, the rendered template prints a `<div class="qa-vocab-header">Synonymes</div>` column containing the terms, and the grid is `qa-vocab-grid two-col` when synonyms join vocab/expressions (fixture carries a 2-term synonyms list on the paragraph)**; essay heading — `<HEADING>Ma ville</HEADING>` marker inside `<ESSAY>` + template renders `<h3 class="block-essay-heading">Ma ville</h3>` above the passage; qa field ORDER in the rendered HTML via indexOf (question < question translation < analysis < model answer < answer translation — apostrophe search uses the template's escaped `&#39;` forms); hidden toggles — all-four hides translations/analyses/vocab/model answers while MAIN content (question, paragraph text, heading, essay paragraphs + **essay heading**) and practice answers survive; vocab-only toggle leaves translations/analyses/answers intact; **round 3 — empty-lines: blank `<div class="blank-answer">` box (with `.line` rows) where the model answer is hidden, model answer still omitted, main content + enrichment survive, absent when the toggle is off, qa keeps the VISIBLE model answer (no blank under it), essay with no written answer gets writing space; PDF display mode — %PDF magic, hidden sections SHRINK the PDF, empty lines ADD content (react-pdf embeds subset fonts as CID/glyph codes so the smoke tests assert structure by size, not text — documented in the test); **round 4 — practice answers NEVER render in the preview (default output AND every toggle combination, for qa + paragraph + essay — only the my-answers PDF variant and explicit `showUserAnswers` opt them in; user: "it is showing practice answers why… in future we will add an option for the review of the practice area"); **round 5 (30/30)** — the question translation sits BELOW the question in its own `<p class="qa-question-translation">` (no inline `<em>` — "?<em>"/"<em>What" adjacency absent; user: "it is showing in same line as question it should be below it")**; **round 10 (2026-08-13, to-do item 2 — analysis bullet points, 33/33):** a bullet fixture (`bulletDoc` — qa whose analysis mixes prose + `- ` lines with inline markdown) — (1) `- ` lines render as `<ul class="point-list">` with `<li><strong>« où »</strong> = where</li>` (inline markdown works inside a point); (2) consecutive bullet lines group into ONE list while the prose line stays outside (`<ul class="point-list">` count === 1, no `<li>La phrase est au présent.</li>`); (3) the hidden-analyses preview toggle still removes the bulleted analysis (no `point-list` in the hidden render)**; **round 10 fix (2026-08-13 — a round-9 check that could NEVER pass was finally caught by the 37/37 run):** the "synonyms: qa marker serialized" check expected `SYNONYMS: se réveiller|to wake up; de bonne heure|early in the day` but the fixture carried the synonyms on the PARAGRAPH — and paragraphs serialize text-only by design (M3), `serializeQa` emits enrichment markers for qa blocks only; the qa fixture now carries its own context-appropriate synonyms (`la veille`, `le jour d'avant`) and the check asserts those — the qa marker round-trip is now genuinely verified**,
 **`smoke-m8.ts` (23 checks, 2026-08-10 M7 round 6 — library folders against the REAL FS storage in `.tmp-m8`:** starts empty; create returns id + timestamps; listFolders name-sorted case-insensitively; persisted to `data/folders.json`; rename updates name + updatedAt, missing id → null; folderId round-trips through save/get/listDocuments; unfiled docs have no folderId; deleteFolder removes the folder, KEEPS the documents, clears folderId on its documents and leaves CONTENT untouched (block text intact); unrelated docs untouched; deleting a missing folder is a no-op; schemas — documentSchema accepts folderId and accepts older docs without it, create/rename payloads accept names + reject whitespace-only, moveDocumentPayloadSchema accepts null + a folder id + rejects a missing folderId)**). Compiled with `tests/tsconfig.json` (outDir `tests/build`, `rootDir ..`, `@/*` alias via `paths`), run with `node --require <abs>/tests/alias-hook.js tests/build/tests/smoke-*.js` + `NODE_PATH=<abs>/tests/build` (absolute paths required). `tests/build` and `tests/.tmp-*` are gitignored.
, **`smoke-m9.ts` (21 checks, 2026-08-13, to-do item 5 — the Test generator's random path:** `testTitle` format ("Test — 13 Aug" from a known date + the prefix on the default), the random-average defaults in range (3–5 questions, 1–2 essays), explicit counts pick exactly N qa + N essays with no duplicate ids, both essays picked when asked (id-checked), practice answers STRIPPED (qa + essay) while model answers stay (the test checks against them), essay heading + paragraphs preserved, no title/paragraph blocks carried over, defaults never exceed the pool, counts clamp to what exists (1 qa + 0 essays with counts 5/2 → 1 qa), blank questions never picked, no usable content → an empty test document, **2026-08-13 test-mode trio: `opensInPractice` set on every buildTestDocument path (explicit/clamped/tiny), normal `createDocument` carries NO flag, `documentSchema` round-trips the flag)**). Compiled with `tests/tsconfig.json` (outDir `tests/build`, `rootDir ..`, `@/*` alias via `paths`), run with `node tests/build/tests/run-all.js` (or `run-all.js mN` for one suite; per-suite paths `node --require <abs>/tests/alias-hook.js tests/build/tests/smoke-mN.js` + `NODE_PATH=<abs>/tests/build` — absolute paths required). `tests/build` and `tests/.tmp-*` are gitignored.
- **Files:** `smoke-m2.ts`, `smoke-m3.ts`, `smoke-m4.ts`, `smoke-m5.ts`, `smoke-m6.ts`, `smoke-m7.ts`, `smoke-m8.ts`, `smoke-m9.ts`, `run-all.ts`, `tsconfig.json`, `alias-hook.js` (resolves `@/` to the emitted build tree at runtime).

---

## Environment Variables

| Variable | Required | Purpose | Referenced in |
|---|---|---|---|
| `DATA_DIR` | no | Storage root (default `./data`) | `lib/storage.ts` factory |
| `DEEPSEEK_API_KEY` | AI mode | DeepSeek API key; absent → 400 with actionable message (FR-30) | `lib/ai.ts` (`hasAIKey`) → routes `convert/ai`, `convert/structure`, **`test` (2026-08-13, item 5)**; `config` route reports presence |
| `DEEPSEEK_BASE_URL` | no | API base (default `https://api.deepseek.com`); model route is `${baseUrl}/v1/chat/completions` | `lib/ai.ts` (`getAIConfig`) |
| `DEEPSEEK_MODEL` | no | Model name (default `deepseek-chat`); shown in the editor status bar (FR-28) via `/api/config` | `lib/ai.ts` (`getAIConfig`), `api/config/route.ts` |
| `MONGODB_URI` | Vercel deploy | MongoDB storage switch (activates Mongo/Blob factory — M5, FR-44) | `lib/storage.ts` factory, `lib/storage-mongo.ts` |
| `BLOB_READ_WRITE_TOKEN` | Vercel deploy | Vercel Blob token for html/pdf file storage (M5, FR-44) | `lib/storage-mongo.ts` |

---

## Planned Changes

- **M6 done (2026-08-10):** dashboard home, AI-only editable-block conversion, practice master key, on-demand preview + 3-variant PDF, AI enrichment for all text, paste-blocks round-trip. Bug-fix pass done: preview payload, practice autosave, paragraphs in practice (editor + PDF + HTML). **Design pass 2 done (2026-08-10):** essay block type through the full stack, practice structure-freeze, split add control with remembered default, super-subtle hover-reveal chips, Enter = newline in paragraphs, instructions Part 1 essay rules (Part 2 verbatim). **M6 round 2 done (2026-08-10):** AI corrections (suggestions) for qa blocks with per-row apply/dismiss, punctuation/typography rules in the AI protocol, PDF `1/7` page footers, auto-grow everywhere. **M7 done (2026-08-10):** focus mode, Q&A field order (question → translation → analysis → answer in editor + preview + PDF), Copy dialog = exactly 2 options (sharing + For AI round trip via Paste blocks), copyable instructions above JSON/HTML paste boxes, home = library + Writer brand home link, preview field toggles (per-field + All extras), InstructionsEditor crash fix. **M7 round 2 done (2026-08-10, user feedback batch):** focus default ON, optional essay `heading` field (never forced; practice = heading-only prompt), Copy ▾ dropdown → single `Copy…` button + `/api/export/prompt` route deleted, paste textareas scrollable (max-h cap), preview "All extras" checkbox semantics fixed (checked = all visible) + stale-closure fix on toggle, response label moved ABOVE the answer in template + PDF. Type-checks clean; build green; smoke M2–M7 green (M5 32/33). **M7 round 3 done (2026-08-10, user feedback batch):** downloads moved into the Preview sheet (Download PDF + Download HTML render the CURRENT display — byte-identical; toolbar Download dropdown + 3-variant menu gone, `variant` still accepted by the pdf route for GET/tests), "All extras" clicking now SELECTS all four per-field options (uniform checked = shown semantics, `toggleAll()` sets all four false when anything is hidden), **Empty lines toggle** (blank ruled lines where the model answer is hidden + writing space for unanswered essays — the old "questions" PDF variant behavior as a toggle; no variant needed), PDF/HTML routes accept `{ doc, hidden, emptyLines }`, preview Refresh button removed, paste modals' WHOLE card scrolls (`max-h-[88vh] overflow-y-auto`), instruction text in the paste modals collapsed by default with a Show/Hide toggle (Copy button always visible). Type-checks clean; build green; smoke M2–M7 green (M5 32/33, M7 28/28); live-server e2e of the new display options verified. **M7 round 4 done (2026-08-10, user feedback batch):** View ▾ dropdown REMOVED (the preview's per-field toggles cover hide/show at render time — `VisibilityCounts`/`setAllQaFlags`/the 4 bulk-flag props deleted; `counts` kept for the status bar only); **practice answers NEVER render in the preview/display** (`showUserAnswers` default-OFF in the HTML template; PDF `showUser = variant === "my-answers"` — the display PDF matches the preview byte-for-byte; only the server-side my-answers variant + explicit opt-in show them; the future practice-review option flips the same flag); **checking paragraph/essay answers no longer reveals the English translation** (checked = emerald tint only — French-first practice); **Focus checkbox inverted** → UNCHECKED "Detailed" toggle (focus mode IS the default); toolbar UI tidied (border-divided action groups, shared `TogglePill`, emerald Practice tint, quiet Reset-practice link); **Instructions link moved to the home screen** (editor nav links gone — brand icon is the only home link); **editor column widened** `max-w-2xl → max-w-4xl` (desktop blank space). Type-checks clean; build green; smoke M2–M7 green (M5 32/33, **M7 29/29**); live-server e2e verified (preview without practice answers for qa/paragraph/essay, hidden+empty-lines unchanged, display PDF 200, my-answers GET 200 and BIGGER than full on a stored doc). **M7 round 5 done (2026-08-10, user feedback batch):** 👁/🙈 hide toggles REMOVED from the question translation + answer fields (visibility lives in the preview sheet; legacy `hideTranslation`/`hideModelAnswer` flags stay in the data model, renderers still honor them — `EyeToggle` component deleted); **question translation BELOW the question** in its own `.qa-question-translation` element (was an inline `<em>` on the same line — HTML template + PDF `questionBody` column View; parse-back recovers the new element with an em-in-question fallback for older exports); **Answer label field REMOVED from the editor** (responseLabel is always "RÉPONSE" — renderers keep using the stored value; schema/prompt/instructions unchanged). Type-checks clean; build green; smoke M2–M7 green (M5 32/33, **M7 30/30**); live-server e2e verified (translation element below the question, no inline em, hidden-translations toggle still gates, RÉPONSE renders, display PDF 200). **M7 round 6 done (2026-08-10, user feedback batch):** browser popups GONE — document delete + Reset practice are in-app two-step confirms, `alert()`s → inline error banner; **home = 10 most recent + "View all N documents in the Library →" link** (`RECENT_LIMIT = 10`, LibraryList `recent` mode hides sort/tags/backup/folder controls); **real Library page** — every document + folder chip bar (create / rename / delete — delete UNFILES documents, never deletes them), per-card "Move to…" select, folder + "No folder" filters; `Folder` type + `Document.folderId`, folder CRUD on the StorageBackend (FS `data/folders.json` + Mongo `folders` collection), `/api/folders` + `/api/folders/[id]` routes, `PATCH /api/documents/[id]` move endpoint (updatedAt untouched); **editor column `max-w-4xl → max-w-6xl`**. Type-checks clean; build green; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (pre-existing deferred), M6 24/24, M7 30/30, **M8 23/23**; live-server e2e verified (home 10 cards + library link, folder create→rename→move→delete lifecycle incl. doc-unfile-kept, PATCH 400 on invalid body, editor 200).
- **M7 round 11 done (2026-08-13 — ALL of to-do.md, built + tested in one pass, user: "do the things from the todo and then run the test after all the stuff is done"):** **item 10** — instructions auto-sync (`syncActiveFromRepo` — "newer writer wins": repo mtime vs active-edited-at; wired into BOTH backends' `readInstructions`; no history snapshots for machine syncs; the manual "Reset to repo file" button is now redundant but harmless); **item 1** — copy dialog wording + French headings ("My practice answers"/"Answers" labels; `Réponse :`/`Ma réponse :`/`Traduction de la question :`/`Traduction de la réponse :`/`Grammaire :` in `buildCopyText`); **item 8** — French "Traduction :"/"Grammaire :" headings in the HTML template (+ parse-back strips them) + the RÉPONSE label renders ONLY when there's something to label (answer, blank lines, or user answer) in template + PDF + copy; **item 6** — autosave page-leave flush (`visibilitychange` + `pagehide` → immediate persist); **item 7** — card ⬇ = PDF (full variant), JSON download moved to the Preview sheet (`downloadJsonDisplay`), backup ZIP folders = `sanitizeBackupFolder(title)_<4-char code>` + `document.pdf` always (generated on demand) + `lib/backup.ts`; **item 4** — Detailed-toggle focus retention (`data-focus-id` on every editor field + pointerdown-capture snapshot + caret restore in the same rAF + per-block fallback chain); **item 9** — Smart paste box (`PasteSmartModal` + `lib/paste-sniff.ts` — `[` → blocks, `<` → html, else questions; contextual buttons reuse the existing importers) + copy presets (`PRESET_WORKSHEET_NO_ANSWERS`/`PRESET_QUESTIONS_ONLY` + `writeClipboard`/`applyPreset`); **item 5** — Test generator (`TestDialog` on home + library; random path = `lib/test-generator.ts` `buildTestDocument` — instant/free, practice answers stripped, model answers kept, "Test — <date>" title; AI path = `POST /api/test`); **tests/run-all.ts** — the single all-suite runner (user's token-saving ask; `run-all.js mN` filters). Verified: `npx tsc --noEmit` clean; tests compile clean; **run-all: 7/8 suites pass** — M2 26/26 (fixture fix: b3/b4 gained `responseLabel` so the item-8 gating checks are real), M3 48/48, M4 31/31, M6 24/24, M7 37/37, M8 23/23, M9 18/18; M5 37 passed + the ONE documented pre-existing deferred check ("parse: vocab grid rows" — to-do.md); `next build` green (19 routes — `/api/test` added).
- Remaining: fix the one deferred M5 check (vocab grid row term/def attribution — see suggestions.md), final e2e pass against the running server, then push the unpushed milestones (M5 + UI polish + M6 + bug fixes + design passes 1 & 2 + M6 round 2 + M7 + M7 round 10 + M7 round 11) once the user confirms.

## Verified

- **M1:** build green; smoke 18/18 (TOKENS parsing, PDF buffer); e2e convert/template + document CRUD + downloads.
- **M2:** smoke 22/22 (QA HTML, PDF normal + practice).
- **M3:** smoke 36/36 (fences, validation, splitQuestions, structuring, prompts, copy text, titleFromHtml).
- **M4:** smoke 24/24 (hashVersion, seeding, save→history→cache, TOKENS validation, reset, snapshots, resolveConversionInstructions); build green 17 routes.
- **M5:** full implementation + build green; Mongo/Blob typechecks; M5 smoke 32/33 after the hang fix.
- **M6:** `npx tsc --noEmit` clean (only the stale `.next/types/validator.ts` regenerated error — filtered, rebuilds on `next build`); anti-pattern greps clean (`canDownloadPdf`, `previewStale`, `ConvertMode`, `convert/template`, `PreviewPane`, `showPreview`, `?id=` links, `practice=true` — all gone from source; `USER_ANSWER` only in `lib/prompt.ts` comments as intended). Build + smoke suite + test compile pending (verification task).
- **M6 bug-fix pass (2026-08-10, user-reported):** preview 400 fixed (route unwraps `{ doc }`); practice answers autosave (quiet debounced persist); practice shows ALL blocks and paragraphs get "My answer" boxes; PDF/html render paragraph practice answers (`my-answers` variant includes paragraphs). Verified: `next build` green (18 routes); dev-server curl — `/api/preview` 200 for both `{ doc }` and bare shapes, paragraph answer in HTML; all three PDF variants 200 with correct content (text extracted via ToUnicode/cmap decode — full: para answer + translation + qa answer + model answer; questions: title + question only; my-answers: para + answers, no references); smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (pre-existing deferred vocab-row check).
- **M6 design-alignment pass (2026-08-10, same day):** instructions rewritten to the user's print-ready spec (Part 2 verbatim) + conversion protocol (Part 1); `vocabBg`/`rowBorder` tokens added through the whole chain (instructions → parseTokensBlock → DesignTokens → DEFAULT_TOKENS → both renderers); globals.css palette remapped to the spec ramp; PracticeParagraphCard → continuous flow (no card, no duplicated boxes); preview renders printMode (A4 + print font/margins + own shadow, transparent iframe); View-dropdown actions now set status messages; enrichment chips + "Add row" + add-block hover muted to subtle emerald accents; unused RowEditor import removed from Block.tsx. Verified: `next build` green; `/api/preview` 200 wrapped + bare with printMode CSS (210mm / 10.5px / 14mm / #1e3a5f / #eef2f7), paragraph userAnswer dashed box present; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check); no leftover old chip/iframe/blue-hover patterns (grep-verified). **User must click "Reset to repo file" once on /instructions** (active.md still holds the pre-rewrite content).
- **Design pass 2 (2026-08-10, user feedback):** essay block type end-to-end (types/schema/structuring/prompt/template/PDF/parse-back/editor/copy) — practice renders ONE continuous passage with ONE writing field, AI conversion groups essays with ONE enrichment set; practice structure frozen (no add, no per-block controls/tags/heading select, drag disabled — AddBlockMenu `!practiceMode`-guarded); split add control (`+ <last>` + ▾ menu, `writer-app:add-type` in localStorage); super-subtle chips (`opacity-0 group-hover:opacity-100` in QaBlockForm + ParagraphFields); Enter = newline for paragraph/essay only (`block.type !== "paragraph"` guard); instructions Part 1 essay rules added (Part 2 verified verbatim vs the user's re-pasted spec, incl. vocabBg/rowBorder tokens). Verified: `next build` green; `/api/preview` 200 with `block-essay` wrapper, 3 `.block-paragraph`, ONE `p-translation`, vocab grid, dashed `qa-user-answer`; all three PDF variants 200 (full 4122B — passage + userAnswer + translation + analysis + vocab + expressions via text extraction; my-answers 2416B — passage + userAnswer, NO references; questions 2863B — passage + blank ruled area, essay now included on both non-full sheets); smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same pre-existing deferred vocab-row check); grep-verified: essay in every block-type switch, no old blue hover accents, Enter guard present, chips hover-reveal, practice guards present. **User must click "Reset to repo file" once on /instructions** (active.md now ALSO stale after the Part 1 essay-rules rewrite).
- **Design pass 2 round 2 (2026-08-10, user feedback):** French-first practice typography (passage 17px / answers 16px / English reference 13px secondary — "this is for practicing french not english") + convert() no longer loses answers: paragraph/essay `userAnswer`s carry over by text match, merging per-paragraph answers when the AI groups paragraphs into one essay. Verified: tsc clean, build green; carry-over logic check-run across 5 scenarios (paragraphs→essay merge, essay→essay, paragraph→paragraph, qa→qa with flags, essay→paragraphs no-carry limitation); smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check).
- **Design pass 2 round 3 (2026-08-10, user feedback):** "the text for french is still small in preview" — the preview/output bug was `rem`-based secondary sizes (see `buildCss`): English translations were 0.9rem = 14.4px while French paragraphs were the 10.5px print base. Fixed with `em` everywhere + `.block-paragraph` at 1.15em; PDF paragraphs mirror at `basePt * 1.15`; instructions prose + TOKENS `small` updated to `em` (prose↔CSS in sync). Verified: live `/api/preview` CSS check (1.15em paragraph, 0.9em translation, zero `font-size` rem left), PDFs regenerate 200 with full content, tsc clean, build green, smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check). Note: active.md staleness does NOT affect these sizes (they're compiled into the template, not token-driven) — the reseed only matters for the essay rules.
- **Design pass 2 round 4 (2026-08-10, user pasted the original Print-Ready spec):** "i need the preview to be same size and other things described in this" — the preview was showing the PRINT rendering (10.5px base, 14mm margins) while the spec describes base 11.5px / 18mm margins as the on-screen design (10.5px/14mm only under `@media print`). `buildCss` paper rules now render the spec's SCREEN design: `body` keeps the base 11.5px (the `body { font-size: print }` override was removed) and `.document` pads with `pageMargin` 18mm; the `@media print` blocks keep true print behavior (10.5px, `@page` 14mm, no shadow). User chose (AskUserQuestion): French prose keeps its 1.15em lead → ~13.2px; English translation 0.9em → ~10.4px secondary. Verified: live `/api/preview` CSS (body 11.5px, `.document` padding 18mm ×2, `p.block-paragraph` 1.15em, the ONLY 10.5px occurrence inside `@media print`), PDFs regenerate 200 with full content (4154/2465/2873B), tsc clean, build green, smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check). **User must click "Reset to repo file" once on /instructions** (active.md still holds pre-essay-rules content).
- **M6 round 2 (2026-08-10, user feature batch — "AI should never change answers, only suggest"):** AI conversion now returns per-qa **corrections** (spelling/grammar/punctuation, French accents + spacing) that the user applies or dismisses one by one; PDF pages are footed `1/7` style; every multi-line text field auto-grows. Verified: `npx tsc --noEmit` clean; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check), **M6 24/24**; `next build` green (18 routes); **PDF footers verified end-to-end via TJ hex-run decode of inflated content streams** — full: 1/2, 2/2; questions: 1/3, 2/3, 3/3; my-answers: 1/3, 2/3, 3/3 (server route + standalone renderer); **live-server e2e** — POST /api/documents accepts suggestions (201), GET round-trips them, /api/preview 200 with ZERO suggestion strings leaked into HTML (question text renders), PDF route 200 with footer; anti-pattern greps clean (no leftover local autoGrow in Block.tsx, no fixed-row textareas outside AutoGrowTextarea, no "suggestions" in html-template/pdf/CopyDialog/html-to-blocks — no leak paths); dev server log clean. **User must click "Reset to repo file" once on /instructions** (active.md still holds the pre-CORRECTIONS-rules content).
- **M7 (2026-08-10, user feature batch):** focus mode toggle (only main content — question+answer, paragraph/essay text; enrichment hidden); Q&A field order question → translation → analysis → answer unified across editor, HTML template and PDF; Copy dialog exactly 2 options (For sharing + For AI — buildAICopyText round trip pasted back via Paste blocks, no AI call in-app); copyable instructions above the JSON + HTML paste textareas (InstructionCopyBox; Paste questions unchanged per user); home = library (Library single nav item, `/library` → `/`, Writer brand links home); preview field toggles (per-field + "All extras" master; headings/questions/paragraph text never hidden; /api/preview accepts `{ doc, hidden }`); InstructionsEditor crash fix (4 `entry.content` sites guarded). Verified: `npx tsc --noEmit` clean; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check), M6 24/24, **M7 17/17**; `next build` green (18 routes); **live-server e2e** — POST /api/preview with `hidden` toggles → 200, main content survives, hidden fields absent, model answer visible when not hidden; anti-pattern greps clean (no leftover Home nav item, focusMode threaded everywhere, no unguarded `entry.content`); dev server log clean. **User must click "Reset to repo file" once on /instructions** (active.md still holds the pre-CORRECTIONS-rules content — unaffected by M7, which changed no instructions).
- **M7 round 2 (2026-08-10, user feedback batch):** focus mode is **DEFAULT ON** (`useState(true)`); **essay `heading`** — optional field through the whole chain (types/schemas/structuring tolerant parse/prompt `<HEADING>` marker/HTML template `.block-essay-heading`/PDF `essayHeading`/parse-back/editor), practice mode shows ONLY the heading as the writing prompt in editor + PDF non-full variants, instructions rule "only when the passage has a natural title — never invent, never force"; **Copy ▾ dropdown → single `Copy…` button** (FR-39 items + `onCopyPrompt` + Editor `copyPrompt`/`copyToClipboard` + `/api/export/prompt` route deleted — instructions copies live in the paste sections); **paste textareas scrollable** (`AutoGrowTextarea` base now `resize-none overflow-y-auto max-h-[45vh]` — long pastes scroll inside the modal); **preview toggles fixed** ("All extras" `checked={!anyHidden}` — was inverted, checked meant everything hidden; `openPreview(hiddenOverride?)` passes the NEW hidden values straight to the POST, fixing the stale-closure first-click bug); **response label moved ABOVE the answer** in template (`${responseLabel}${modelAnswer}`) and PDF (QABlockPDF), removing the old bottom position. Verified: `npx tsc --noEmit` clean; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check), M6 24/24, **M7 19/19** (essay `<HEADING>` marker + `<h3 class="block-essay-heading">` template render + "Ma ville" survives hidden + hidden-main-survives extended); `next build` green (18 routes); anti-pattern greps clean (no leftover `onCopyPrompt`/`copyPrompt`/`export/prompt`/`/api/export` references, no `allHidden`-semantics leftovers). **User must click "Reset to repo file" once on /instructions** (active.md now also stale for the essay-heading rule — same as the earlier reseeds).
- **M7 round 3 (2026-08-10, user feedback batch — downloads, toggles, empty lines, paste modals):** downloads moved INTO the preview ("user should see the exact document before downloading") — **Download PDF** posts `{ doc, hidden, emptyLines }` (current display options; no variant), **Download HTML** downloads the in-memory `previewHtml` string (byte-identical to the sheet); toolbar Download ▾ dropdown removed (`onDownloadPdf`/`onDownloadHtml` props + PDFVariant import gone from Toolbar; Editor `downloadPdf(variant)`/`downloadHtml()`/`ensureSaved()` deleted — replaced by `downloadPdfDisplay()`/`downloadHtmlDisplay()`); **"All extras" selects all four options** — uniform `checked` = SHOWN semantics (`checked={!hidden[key]}`, master `checked={!anyHidden}`, `toggleAll()` sets all four false when anything is hidden, else all true) so the checkboxes display reality; **"Empty lines" toggle** — blank ruled areas (`<div class="blank-answer">` + 4 `.line` rows in HTML; `BlankAnswerArea` in PDF) replace the hidden model answer, and unanswered essays/paragraphs get writing space when the toggle is on — this replaces the "questions" PDF variant, so the variants dropdown is gone; `/api/preview` + `/api/documents/[id]/pdf` both accept top-level `hidden` + `emptyLines` (`hiddenOptionsSchema` shared); `previewOptions` state (`PreviewOptions` = hidden + emptyLines) with `previewOptionsRef` + `previewSeqRef` (out-of-order-response guard — rapid toggle clicks only apply the latest); **Refresh button removed** from the preview (redundant — toggles re-render); **paste modals scroll as a whole card** (`max-h-[88vh] overflow-y-auto` — textarea cap alone wasn't enough, instruction box + textarea + buttons pushed it off-screen) in PasteBlocksModal + PasteHtmlModal; **instruction text collapsed by default** in InstructionCopyBox with a Show/Hide toggle (Copy instruction button always visible — "user will mainly use the copy button"). Verified: `npx tsc --noEmit` clean; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check), M6 24/24, **M7 28/28** (+9: 6 empty-lines HTML checks incl. `<div class="blank-answer">` element-markup assertion + essay-gets-writing-space, 3 PDF display-mode structure checks — hidden shrinks, empty lines adds, %PDF magic); `next build` green (18 routes); **live-server e2e** — POST /api/preview `{ doc, hidden: {modelAnswers, translations}, emptyLines }` → 1 blank-answer div (the essay — no user answer), model answer + translation ABSENT, question + user answer + essay passage present; pdf route same body → 200 (4479 B — bigger than full 3988 B, empty-lines content on top); anti-pattern greps clean (no leftover onDownloadPdf/onDownloadHtml/variant wiring in Toolbar/Editor, no `checked={hidden[` inversion, no legacy `downloadPdf(`/`ensureSaved` calls, no Refresh in PreviewSheet).
- **M7 round 4 (2026-08-10, user feedback batch — "remove the view options", "it is showing practice answers why", "check the answer in paragraph is showing me the translation fix it", "focus mode… should show the other way around like the detailed", "fix the ui of the bar", "instructions tab… put it on home screen", "dont need button for library", "a lot of blank space around the text fields on laptop"):** **View ▾ dropdown removed** (the preview sheet's per-field toggles cover hide/show at RENDER time without mutating block data — `VisibilityCounts`, the `counts`/`onHideAllTranslations`/`onShowAllTranslations`/`onHideAllAnswers`/`onShowAllAnswers` props and Editor's `setAllQaFlags` are deleted; `counts` survives only for the status bar); **practice answers (userAnswer) NEVER render in the preview/display** — HTML template gains `showUserAnswers?: boolean` (default FALSE — the preview route never sends it, so qa/paragraph/essay omit the user-answer box; blank-answer condition also drops the `!content.userAnswer` guard so an empty slot never shows a stale answer); PDF `showUser = variant === "my-answers"` (was `variant !== "questions"` — the display PDF the UI posts renders with no variant → "full" and matches the preview byte-for-byte; only the server-side my-answers variant shows practice answers; the future "review of the practice area" feature flips the same flag); **paragraph/essay Check answers no longer reveals the English translation** — the green "Reference" box is gone from PracticeParagraphCard + PracticeEssayCard; checked = emerald border/tint on the answer textarea only (paragraphs/essays have no model answer — showing the translation was wrong for French-first practice); **Focus checkbox inverted → UNCHECKED "Detailed" toggle** (`detailed` state `useState(false)`, prop renamed `focusMode` → `detailed` through Toolbar/BlockList/Block/QaBlockForm, branches inverted `if (!detailed)` / `{detailed && <ParagraphFields/>}` — the default state (focus) needs no checked box; checking "Detailed" reveals translations/analysis/vocab); **toolbar UI tidied** — actions in three border-divided groups (Convert+Save+Preview | Practice+Detailed+Check/Reset | Copy+Paste), shared `TogglePill` component (Practice gets the emerald active tint, Detailed is plain), "Reset practice" demoted to a quiet `text-xs text-zinc-400` link (the View dropdown's only practice-only action); **Instructions link moved to the home screen** (a `next/link` beside NewDocumentButton on `/`; the toolbar's Instructions + Library nav links are gone — user: "library is basically home, go there using the writer icon"); **editor column widened** `max-w-2xl → max-w-4xl` (the fixed 672px column left huge gutters on desktop; phone full-width untouched). Verified: `npx tsc --noEmit` clean; smoke M2 22/22 (b3 practice-answer check now asserts NOT-rendered), M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check; round trip now passes `showUserAnswers: true`), M6 24/24, **M7 29/29** (+2 round-4 checks: practice answers absent in default output AND under hidden toggles); `next build` green (exit 0); **live-server e2e** — POST /api/preview default → qa/paragraph practice answers ABSENT, model answer + question + essay passage present; hidden modelAnswers + emptyLines → 3 blank-answer divs, model answer absent; pdf route same body → 200 (4687 B); GET my-answers on a stored doc → 200 (5353 B > full 4411 B — practice answers present, the only remaining consumer); anti-pattern greps clean (no `focusMode`/`onToggleFocus`/`setAllQaFlags`/`VisibilityCounts`/`onHideAll*`/View-dropdown leftovers, no `showUser = variant !== "questions"`, no `.qa-user-answer` rendering without showUserAnswers). **User must click "Reset to repo file" once on /instructions** (active.md still holds pre-essay-rules content — unchanged by this batch).
- **M7 round 5 (2026-08-10, user feedback batch — "remove the view or hidden togle from question translation and answer", "it is showing in same line as question it should be below it", "remove answer label field as it is not going to change at all"):** **hide toggles removed from the editor** — the 👁/🙈 `EyeToggle` (question translation + answer) is deleted; user: "remove the view or hidden togle from question translation and answer as it is now in preview" — the preview sheet's per-field checkboxes cover visibility at render time. The legacy `hideTranslation`/`hideModelAnswer` flags REMAIN in the data model and both renderers still honor them (older documents keep working); the editor simply no longer sets them. **Question translation now renders BELOW the question** — in the HTML template it moves from an inline `<em>` inside `.qa-question-text` to its own `<p class="qa-question-translation">` (italic, 0.9em, muted, 2px top margin) inside a new `.qa-question-body` flex column (the `.qa-question` row stays badge + body); the PDF mirrors it with a `questionBody` View (`flex: 1`) wrapping questionText + translation (was a nested Text on the same line); parse-back (`parseQa`) recovers the new `.qa-question-translation` element with the old `<em>`-in-question regex kept as a fallback for previously-exported HTML; CSS `.qa-question-text em` rule replaced by the `.qa-question-translation` rule. **Answer label field removed** — `responseLabel` dropped from the QaField union / OPTIONAL_FIELDS / usedFields / JSX (user: "remove answer label field as it is not going to change at all so why there is option for user to write the reponse tag"); blocks still default to `responseLabel: "RÉPONSE"` (createBlock/structuring), the template and PDF keep rendering the stored label (default RÉPONSE), and the schema/prompt/AI output shape are untouched. Verified: `npx tsc --noEmit` clean; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check; parse-back check renamed "question translation split out (below the question)" — still recovers the value), M6 24/24, **M7 30/30** (+1 round-5 check: `.qa-question-translation` element present, no `?<em>`/`<em>What` inline-em adjacency); `next build` green (exit 0); **live-server e2e** — POST /api/preview → exactly 1 `.qa-question-translation` element, translation text AFTER the question, question-body wrapper present, RÉPONSE label still rendered, hidden-translations toggle still removes both translations while the question survives; display PDF route 200 (4688 B); anti-pattern greps clean (no EyeToggle in components, no responseLabel editor field, no `qa-question-text em` rule, no nested translation Text in the PDF). **User must click "Reset to repo file" once on /instructions** (active.md still holds pre-essay-rules content — unchanged by this batch).
- **M7 round 6 (2026-08-10, user feedback batch — "when deleting a document it gives a browser pop up i dont want that it should be there somewhere", "only show 10 there and a link to library", "make a library page it will show all the documents… option for making folder too etc", "there is still a lot of empty space around the paragraph and questions"):** **no browser popups** — `LibraryList.remove()` no longer calls `window.confirm`: the card's Delete button arms a two-step IN-APP confirm (red-tinted banner with the doc title + "Delete/Cancel", red card ring while armed, `confirmingDelete` state); the editor's "Reset practice" (the other `window.confirm` in the app) is an in-app red banner under the toolbar (`confirmingReset` state — armed by the toolbar link, cleared by Clear answers / Cancel / leaving practice mode); all three `alert()` failure sites in LibraryList → a single inline red error banner (`error` state, cleared on the next attempt). **Home = 10 most recent** — `app/page.tsx` slices `listDocuments` (already updatedAt-desc) to `RECENT_LIMIT = 10` and renders LibraryList in `recent` mode (no sort/tags/backup/folder controls; a "View all N documents in the Library →" link whenever there are documents — the doorway to the full archive even with few docs). **Real Library page** — `app/library/page.tsx` is no longer a `redirect("/")` stub: server page fetching ALL documents + folders (`Promise.all`), ← home link, counts (documents · folders), Instructions + New document, LibraryList in full mode. **Folders** — `Folder { id, name, createdAt, updatedAt }` type + optional `Document.folderId` (undefined = unfiled; storage clears it on folder delete so it never dangles); `StorageBackend` gains `listFolders` (name-sorted) / `createFolder` / `renameFolder` (→ Folder | null) / `deleteFolder`; FS impl stores `data/folders.json` and `unfileDocuments()` clears folderId on the folder's docs (rewriting only those document.json files); Mongo impl mirrors it with a `folders` collection + `$unset folderId` update; new routes `POST/GET /api/folders` + `PATCH/DELETE /api/folders/[id]` (name trimmed 1–80, 404 on missing; DELETE → 204 and never touches documents) + `PATCH /api/documents/[id]` `{ folderId: string | null }` (`moveDocumentPayloadSchema`; null = unfiled; updatedAt untouched — moving is organization not editing); LibraryList folder bar — "All · N" + per-folder chips (name + doc count, click to filter, ✎ inline rename, 🗑 two-step delete "Documents stay in All") + "No folder · N" chip + "+ New folder" inline input, per-card "Move to…" select (pendingMove busy state, folder name line under the meta row), folder filter message. **Editor column widened** `max-w-4xl → max-w-6xl` (user: "still a lot of empty space around the paragraph and questions" — the remaining desktop gutters around block fields are gone; phone full-width untouched). Verified: `npx tsc --noEmit` clean (exit 0); smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check), M6 24/24, M7 30/30, **M8 23/23** (new folder test — FS storage CRUD, name sort, unfile-on-delete with content intact, schemas incl. whitespace-name rejection — one schema fix during the run: `createFolderPayloadSchema`/`renameFolderPayloadSchema` gained `.trim()` so `"   "` is rejected); `next build` green (exit 0, `/api/folders` + `/api/folders/[id]` in the route table); **live-server e2e** — home: "Recent documents" header, exactly 10 cards with 11 docs stored, "View all 11 documents in the Library →" link with `/library` href; library: "All · 11", "No folder · 11", "New folder", 11 "Move to…" selects, sort + backup controls present; folder lifecycle: POST → 201 folder, PATCH rename → 200, PATCH move doc → 200 + folderId on GET, folder chip "e2e Renamed Folder" rendered on /library, DELETE → 204 → doc unfiled (folderId gone) + still listed, folders list back to 0; PATCH with invalid body → 400; e2e docs cleaned up (8 stored docs again); anti-pattern greps clean (zero `window.confirm(` in app/components/lib — only the "never window.confirm" comment, no `alert(` in components, no `redirect(` in app/library, `max-w-4xl` only in the historical comment). **User must click "Reset to repo file" once on /instructions** (active.md still holds pre-essay-rules content — unchanged by this batch).
- **M7 round 7 (2026-08-10, user feedback — "when I scroll down, I have to go to the top again if I want to toggle the detailed version… Should we add a floating option to toggle the detailed?" — user chose the floating pill via AskUserQuestion; "also add a togle in the writer page for auto save and skip test for now"; "add a simple toggle for dark mode"; "when it toggle detailed on the writer page it shifts my question as new data is added… then it again shift me on another part"):** **floating Detailed pill** — `FloatingDetailedToggle` (fixed bottom-right, fade/slide-in past 200px of scroll, same `detailed` state as the toolbar; `TogglePill` exported from Toolbar to reuse the exact pill styling); **Autosave toggle** — TogglePill in the toolbar document-actions group (default ON = M6 behavior; the debounced autosave effect returns early when off; manual Save/⌘S untouched; preference persisted to `writer-app:autosave` localStorage; "Autosave off — Save manually (⌘S)" chip in the status bar). Verified: `npx tsc --noEmit` clean (exit 0); `next build` green (18 routes); **smoke suites SKIPPED at the user's request** ("skip test for now" — the next full verification run must re-run M2–M8 and the live-server e2e). Anti-pattern greps clean (no duplicate Detailed state, no second scroll container, autosave guard present, `writer-app:autosave` key referenced consistently). **Follow-up fix (same day, user: "there is no floating togle"):** root cause — the inner `overflow-y-auto` container never scrolls because the page scrolls at the window level (body `min-h-full` vs editor `h-full`); the pill now listens to `window.scrollY` AND the container and shows when either passes the threshold. Re-verified: tsc clean, `next build` green. **Dark mode + scroll-anchor (same day):** dark mode verified — compiled CSS carries the `.dark` block (`--zinc-50:#141d2b`, `--blue-600:#3d6da8`), utilities resolve through the vars (`background-color:var(--color-white)`), zero `text-white`/`bg-zinc-900/40` leftovers; **scroll-anchor** verified — tsc clean, build green, both Detailed entry points (toolbar + floating pill) call `toggleDetailed`, `data-block-id` on every block row (grep-verified), anchor math is delta-based (no-op when nothing moved). Both batches committed locally; smoke suites still pending per the user's request. **User must click "Reset to repo file" once on /instructions** (active.md still holds pre-essay-rules content — unchanged by this batch).
- **M7 round 6b (2026-08-10, user feedback batch — "folder thing is not working", "there should not be option for regenerate on the document card….add download icon which will download the document with all fields available in it on card"):** **folder UX fixed on the home page** — the user expected folders to work like the library's: clicking a folder and creating a document should file it there automatically, and the cards should be identical on both pages. So the **folder bar now renders on home too** (`folders.length > 0 || unfiled > 0 || newFolderOpen` gate — was `!recent`; "+ New folder" stays reachable even with zero folders) and **the per-card move select is no longer `recent`-gated** — the same card UI on both pages; home passes the FULL list + `limit`/`total` (was a pre-sliced 10) and a new `gridSource` memo slices the card grid to the first `limit` while `allTags`/`folderCounts` describe all documents, so "All · N = 9", "📁 que · 1", "No folder · 8" are accurate on home; **auto-filing**: when `filterFolder` is a real folder, the bar shows an emerald **"+ New document in 'name'"** `NewDocumentButton` (new `folderId` + `label` props — assigns `doc.folderId` before navigating, so the first save POSTs it and the doc is filed; verified live: POST with folderId persists and GET returns it). **Regenerate removed from the card** — `regenerate()` + `regenerating` state + the button deleted (route `/api/documents/[id]/regenerate` untouched as a server API; the editor keeps its regenerate affordance) and replaced by **⬇** — `downloadDoc()` fetches `GET /api/documents/[id]` and downloads `data.doc` as `<title>.json` (all fields — the only artifact with practice answers too; the PDF "full" variant omits `userAnswer`). Verified: `npx tsc --noEmit` clean (exit 0); smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check), M6 24/24, M7 30/30, M8 23/23; `next build` green (exit 0); **live-server e2e** — home HTML: no "Regenerate" text, 9 ⬇ icons (one per card), folder chips que/jshdfj render, "Move to… (no folder)" selects present, "No folder · 8", "+ New folder", "/library" link; library HTML: 9 cards + 10 ⬇ (9 cards + Backup zip), "All · 9"; auto-file e2e: POST /api/documents with folderId → saved filed → GET returns it → DELETE 204 cleanup; anti-pattern greps clean (zero `regenerating`/`Regenerate` outside comments, no `window.confirm(`/`alert(` in code, no `!recent` gates left on folder bar/move select).
- **M7 round 8 (2026-08-10, user: "in dark mode, the background should be black so that it saves battery"):** the dark palette is now **pure black** for OLED/AMOLED battery savings (pixels are OFF at #000): the canvas `--bg` AND the dominant surfaces `--white` (toolbar, cards, modals, content area — the big areas of the screen) are `#000000`; only the smallest tints stay near-black so hover/input feedback remains visible (`--zinc-50: #0a0e15` hover fills, `--zinc-100: #11161f` input fills, `--zinc-200: #1c2431` borders, `--zinc-300: #2a3444`); text `--fg: #e6ebf2` and the lighter blue/emerald accents unchanged (they already read on dark). CSS-only change — `app/globals.css` `.dark` block + the header comment (now documents the pure-black rationale). Verified: `npx tsc --noEmit` clean; `next build` green; compiled CSS re-checked — `.dark{` present with `--bg:#000000`, `--white:#000000`, `--zinc-50:#0a0e15`; smoke suites still skipped per the user's request (pending full run before push).
**User must click "Reset to repo file" once on /instructions** (active.md still holds pre-essay-rules content — unchanged by this batch).
- **M7 round 9 (2026-08-10, user: "change the text convert with ai to something else it dosent suit it" + badge digit centering):** "Convert with AI" → **"Rethink with AI"** (busy label "Rethinking…"; the paste-blocks instruction composed from the shared `BLOCK_FORMAT_SPEC` so the external-AI flow can't drift) + the question-number badge digit centering fix (`lineHeight: 1.3`, measured — see `lib/pdf.tsx`). The round-9 M7 check fix (fixture synonyms moved onto the qa block) is documented in the tests section.
- **M7 round 10 (2026-08-13, to-do item 2 — analysis bullet points):** `- ` lines render as `<ul class="point-list"><li>` in the HTML template (`renderInlineMarkdown` splits on `\n`, groups CONSECUTIVE bullet lines into ONE list, prose stays outside; inline markdown works inside points via the private `inline()` helper) + PDF bullets via `bulletText` (`•  ` prefix at the three analysis sites); AI rule added to the instructions (ANALYSIS POINTS — "only when the analysis is really a list of points"); the round-9 M7 synonyms check that could NEVER pass was fixed (fixture synonyms moved from the paragraph — which serializes text-only — onto the qa block); suggestions.md/to-do.md/architecture.md synced. Verified: tsc clean, build green, M7 37/37 (see the round-10 commit).
- **M7 round 11 (2026-08-13 — ALL of to-do.md, see the Planned Changes entry above):** verified `npx tsc --noEmit` clean; tests compile; **run-all 7/8 suites** — M2 26/26, M3 48/48, M4 31/31, M5 37+1-deferred (the documented pre-existing "parse: vocab grid rows"), M6 24/24, M7 37/37, M8 23/23, M9 18/18; `next build` green (19 routes incl. `/api/test`). The M2 fixture fix + M5 count updates are documented in the tests section. **The "Reset to repo file" ritual is OVER** — item 10 auto-syncs instructions from `docs/html_instructions.md` into storage on every read (no user action needed).
