# Architecture — Writer App

> **Project root:** `/Users/sukhjot/Desktop/untitled folder 2/writer-app` — Next.js (App Router, TypeScript, Tailwind v4) writer/practice app per `docs/writer_app_requirements.md` (v1.4, FR-1…FR-50) and `docs/Plan.md`.
> **Status:** All milestones **M1–M7 DONE (2026-08-10)** — M1 editor + conversion + PDF + library · M2 Q&A blocks + practice controls · M3 DeepSeek + question import + copy/paste · M4 instructions management (edit/save/reset/history FR-21/22, per-document snapshots + conversion toggle FR-23, TOKENS validation FR-47, version in status bar FR-28) · M5 polish + storage + parse-back (drag-reorder, Enter-split/Backspace-merge FR-3, tags FR-5/18, backup ZIP, HTML→blocks FR-41, MongoDB + Blob FR-44) · UI polish pass (grouped two-row toolbar, Geist font applied, light-only theme, de-jargoned labels). **M6 REDESIGN (2026-08-10):** home page is a dashboard (`/` lists documents + "New document"; editor moved to `/doc/[id]`) · AI conversion returns **editable structured blocks** (template/offline mode deleted; `lib/structuring.ts` canonical JSON-array parser; one conversion, then local edits re-render instantly) · AI enrichment for ALL text (paragraphs get translation/analysis/vocab/expressions) · questions inside paragraphs become `qa` blocks, provided answers kept, never invented · clean worksheet cards (question + ONE reference answer field, extras behind "＋ chips") · **Practice master key** (questions-only view, "My answer" boxes, Check reveals model answers side-by-side, answers saved separately, Reset practice) · **on-demand preview + PDF** (full-screen PreviewSheet via stateless `POST /api/preview`; `POST /api/documents/[id]/pdf` with `{ doc, variant }` — no convert-first gating, `canDownloadPdf`/`previewStale` removed) · **three PDF variants** (`full` / `questions` / `my-answers`) · test generator planned-only (see suggestions.md). **DESIGN PASS 2 (2026-08-10, user feedback):** new **`essay` block type** (a continuous 2–3+ paragraph passage = ONE block with ONE shared translation/analysis/vocab set and ONE practice answer field — fixes per-paragraph enrichment and per-paragraph answer boxes) · **structure frozen in practice** (no add, no per-block controls/tags/heading level, no drag) · **split add control** (`+ <last-chosen>` direct click, ▾ caret opens the full menu; last choice remembered in localStorage) · **super-subtle enrichment chips** (opacity-0 hover-reveal — 99% AI-generated) · **Enter no longer splits paragraphs** (newline only). **DESIGN PASS 2 round 4 (2026-08-10, user pasted the original spec):** the preview sheet now renders at the SPEC's SCREEN size — base 11.5px (not the print 10.5px), 18mm page margins (not 14mm) — while `@media print` keeps true print behavior; French prose keeps its 1.15em lead (~13.2px) with the English translation secondary at 0.9em (~10.4px) per the user's choice. **M6 round 2 (2026-08-10, user feature batch):** AI conversion now also reports **spelling/grammar/punctuation corrections** for qa blocks — the AI NEVER edits text; each qa card shows a "Suggested corrections" list with per-row **Apply** (changes ONLY that one, first occurrence) and **✕** dismiss (lib/suggestions.ts pure logic; `Suggestion` type + tolerant parsing through structuring/schemas; instructions Part 1 gains CORRECTIONS + typography rules incl. accents & French spacing) · **PDF page footers** `1/7` style on every page of all three variants (react-pdf `render` prop + `lib/pdf-labels.ts`) · **auto-grow everywhere** (components/AutoGrowTextarea.tsx wraps lib/auto-grow.ts — Q&A answer/analysis/practice fields, ParagraphFields analysis, practice paragraph/essay answers, essay paragraphs, the three paste modals; grows on every render so AI imports and suggestion Apply re-measure). **M7 (2026-08-10, user feature batch):** **Focus mode** toggle in the toolbar (next to Practice) — shows ONLY the main content (question + answer; paragraph/essay text); translations/analysis/vocab/expressions/suggestions all hidden (same plumbing pattern as practiceMode: Editor state → Toolbar → BlockList → Block → QaBlockForm/ParagraphFields) · **Q&A field order** question → translation → analysis → answer EVERYWHERE (editor, HTML template, PDF — user: "it should show question then translation then analysis then answer") · **Copy dialog = exactly two options** — "For sharing" (selective plain-text copy with toggles) and "For AI" (one-click instruction + serialized content for another AI to restructure; its JSON response pastes back via Paste blocks (AI) — the app itself makes no AI call) · **copyable instructions above the JSON + HTML paste boxes** (shared `InstructionCopyBox`; Paste questions stays as-is per user) · **home = library** (`/library` redirects to `/`; Writer brand icon+text now links home) · **preview field toggles** (per-field + overall "All extras" — translations/analyses/vocab/model answers for qa + paragraph/essay; headings, questions and paragraph text are main content, never hidden) · **InstructionsEditor crash fix** (guarded all 4 `entry.content` sites — history entries without content no longer throw at line 211). **M7 round 2 (2026-08-10, user feedback batch):** **Focus mode is DEFAULT ON** · **essay `heading` field** (optional, never forced — instructions/prompt/paste-blocks instruction updated; renders above the passage in template + PDF; practice mode shows ONLY the heading as the writing prompt — the passage is the task, not reading material; parse-back restores it from `.block-essay-heading`) · **Copy dropdown reduced to ONE "Copy…" button** (the three FR-39 items removed — AI-instruction copies live in the paste sections; `onCopyPrompt`/Editor `copyPrompt`/`copyToClipboard` helper and the `/api/export/prompt` route deleted) · **paste textareas capped** (`max-h-[45vh]` + `overflow-y-auto` in AutoGrowTextarea — long pastes scroll instead of pushing the modal out of the window) · **preview toggles fixed** ("All extras" now checked = everything VISIBLE — was inverted; stale-closure fixed by passing the new hidden values straight to the POST) · **response label moved ABOVE the answer** in template + PDF (it labels the answer area). Build green; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (pre-existing deferred), M6 24/24, M7 19/19. This file is updated after every change; it is the latest and current state of the app.

**Locked decisions (never revisit):** PDF engine = `@react-pdf/renderer` only (no Puppeteer/Chrome anywhere) · design tokens parsed at runtime from the instructions file `TOKENS` block (changing colors = editing `docs/html_instructions.md` only) · storage pluggable (filesystem now, MongoDB + Vercel Blob later) · no auth in v1, `ownerId` seams kept (FR-45) · one-change-one-file (AI → `lib/ai.ts`, PDF → `lib/pdf.ts`, storage → `lib/storage.ts`, design → instructions file, FR-48) · conversion is AI-only and block-based (M6 — preview/PDF render on demand from the current document, no stale-preview gating) · practice answers (`userAnswer`) are private: never sent to the AI, never serialized into prompts.

---

## Runtime layout (FR-17)

```
data/                          # gitignored runtime storage
  documents/<id>/document.json # source blocks (editable truth)
  documents/<id>/document.html # generated HTML (saved alongside the doc)
  documents/<id>/document.pdf  # generated PDF
  documents/<id>/instructions.snapshot.md # per-document rules (FR-23)
  instructions/active.md       # editable instructions (seeded in M4)
  instructions/history/*.md    # version history (M4)
```

## Files

### `docs/html_instructions.md` — Structuring & enrichment instructions (single source, FR-47)
- **Purpose:** THE system prompt for AI conversion + the design system. **M6 rewrite:** Part 1 CONVERSION PROTOCOL — how the AI **structures** content (question detection rules, provided answers kept verbatim, never invent answers for unanswered questions, French→English enrichment for qa AND paragraphs, respect HIDE flags, never output `userAnswer`/HTML). **2026-08-10 design pass (user's PDF spec):** Part 2 PRINT-READY DESIGN SYSTEM — the user's own print spec nearly verbatim (GLOBAL STYLE: A4, 18mm margins, Georgia/Times 11.5px, colors #1a1a1a / #1e3a5f / #2c5f2d; TYPOGRAPHY & ELEMENTS; REUSABLE COMPONENTS for general + Q&A blocks; CRITICAL RULES: break-inside avoid, one-column answer boxes, no floats). Ends with the machine-readable `<!-- TOKENS --> … <!-- /TOKENS -->` block: `colors` (mainText, heading, accentGreen, lightBg, highlightBg, border, tableStripe, **vocabBg, rowBorder (NEW 2026-08-10)**, tagBg, tagText, badgeBg, badgeText), `fonts` (base, mono, pdf), `sizes` (base, print, small), `spacing` (pageMargin, printMargin, cardPadding, answerPadding), `radius` (card, badge, tag). **Changing a color = editing this file only.** The renderers (`lib/html-template.ts` + `lib/pdf.tsx`) implement this spec; the editor UI is remapped to the same palette (globals.css) so screen ≈ paper.
- **Functions:** none (data file — parsed by `lib/tokens.ts`).
- **Note (design pass 2, 2026-08-10):** Part 1 CONVERSION PROTOCOL now has an ESSAYS rule — a continuous 2–3+ paragraph passage becomes ONE `essay` block (all paragraphs in its `paragraphs` array), never per-paragraph blocks; each essay gets exactly ONE translation/analysis/vocab/expressions set. New output shape `{"type":"essay","heading":"…","paragraphs":["…","…"],…}` — **`heading` is OPTIONAL (2026-08-10 #5):** include a short title only when the passage has one or clearly suggests one — never invent, never force (practice mode shows the essay heading as the writing prompt). Part 2 stays the user's spec STRUCTURE (byte-checked incl. #eef2f7/#d8dfe8 grid tokens) with two deliberate size corrections from round 3 (user feedback "french text is small"): relative sizes rewritten `rem`→`em` (rem resolves against the 16px root, making English translations bigger than the French base — a spec-wording bug) and a new "Paragraph / essay prose: 1.15em" rule so the French passage leads the page.
- **Note:** the rewrite does NOT propagate to existing `data/instructions/active.md` — the user clicks "Reset to repo file" once (recorded in suggestions.md).

### `docs/writer_app_requirements.md` / `docs/Plan.md` / `docs/suggestions.md` / `docs/architecture.md`
- **Purpose:** Requirements spec (v1.4), implementation plan (M1–M5), improvement log, and project-level architecture doc (parent folder scope). Documentation only.

### `lib/types.ts` — Shared data model (Plan §5)
- **Purpose:** Single source of truth for `Block`, `QaContent`, `ParagraphContent`, `EssayContent`, `Document` shapes (FR-1/33/34/45).
- **Functions:**
  - `createBlock(type: BlockType, id?)` — factory for a fresh block of any type with a unique id and type-appropriate empty content.
  - `createDocument(title?, id?)` — factory for a fresh unsaved document (uuid id when omitted, `ownerId: null`, `source: "editor"`). M6: optional `id` lets the editor keep the route's id so the first save creates it.
- **Types:** `BlockType = "title" | "heading" | "paragraph" | "essay" | "qa" | "separator"` (essay added 2026-08-10); `ParagraphContent` (M6: `text`, `format?`, `translation?`, `analysis?`, `vocab?`, `expressions?`, `userAnswer?` — practice answer, M6 parity with qa); **`EssayContent` (2026-08-10):** `paragraphs: string[]` (the whole passage), ONE shared `translation?`/`analysis?`/`vocab?`/`expressions?` set, ONE `userAnswer?` — an essay is a single unit, never per-paragraph enrichment or per-paragraph answers; `QaContent` (question, questionTranslation, grammarNote, responseLabel, userAnswer, modelAnswer, answerTranslation, analysis, vocab, expressions, hideTranslation, hideModelAnswer, **`suggestions?: Suggestion[]` — AI-reported corrections, 2026-08-10**); **`Suggestion` (2026-08-10):** `{ id (crypto.randomUUID at AI-parse time), kind: "spelling"|"grammar"|"punctuation", field: "question"|"modelAnswer", original (verbatim substring as written), suggestion (corrected replacement), reason? }` + `SuggestionKind` type; `setBlockContent`/`replaceBlockType` helpers (both handle essay).

### `lib/schemas.ts` — zod validation schemas
- **Purpose:** API payload validation (Plan §3/§10). Mirrors `lib/types.ts`; `.loose()` on content objects preserves unknown/future fields through save/load.
- **Exports:** `blockSchema` (discriminated union on `type`; paragraph content += `translation`/`analysis`/`vocab`/`expressions`/`userAnswer` — M6; **essay variant 2026-08-10** — `paragraphs: string[]` required, optional shared enrichment + `userAnswer`, `.loose()`; **`heading` optional (2026-08-10 #5)**), `documentSchema`, `saveDocumentPayloadSchema` (`{ doc, html?, instructionsVersion? }` — M6), **`suggestionSchema` (2026-08-10):** `id?` on the wire (AI output has none — the `.transform` guarantees one so the inferred type IS a `Suggestion` and saved docs round-trip ids intact); per-field `.catch()` tolerance — a malformed entry degrades to an empty `original`/`suggestion` (dropped downstream) instead of failing the whole list; kind defaults `"spelling"`, field catches to `"modelAnswer"`, original/suggestion `min(1).max(500)` (runaway-AI guard). Also added to `qaContentSchema.suggestions`.

### `lib/tokens.ts` — TOKENS block parser (FR-47)
- **Purpose:** Reads the instructions file and extracts the machine-readable design tokens at runtime. Never rewrites source files.
- **Constants:** `REPO_INSTRUCTIONS_PATH` (`docs/html_instructions.md`), `ACTIVE_INSTRUCTIONS_PATH` (`data/instructions/active.md`).
- **Functions:**
  - `parseTokensBlock(markdown, defaults)` — extracts `<!-- TOKENS -->`…`<!-- /TOKENS -->`, parses whitespace-lenient `section:`/`key: value` lines (strips quotes), merges over `defaults`; returns `DesignTokens | null` (null when the block is missing). **2026-08-10:** explicit mappings added for `vocabBg` and `rowBorder` (`?? defaults` — every key is mapped EXPLICITLY, never by generic spread, so new token keys must be added here).
  - `readActiveInstructions()` — seeds `data/instructions/active.md` from the repo copy on first run (M4, FR-21), then reads it; throws if the repo copy is missing.

### `lib/design-tokens.ts` — Runtime design tokens (FR-43)
- **Purpose:** Cached access to the parsed tokens + fallback defaults so the app never crashes. Both renderers (`html-template.ts`, `pdf.ts`) consume the same token object → HTML preview and PDF can never drift apart.
- **Exports:** `DesignTokens` interface (colors incl. `vocabBg` + `rowBorder` — 2026-08-10), `DEFAULT_TOKENS` (fallback, kept in sync with the instructions file — incl. `vocabBg: "#eef2f7"`, `rowBorder: "#d8dfe8"`), `getTokens(): Promise<DesignTokens>` (cached), `invalidateDesignTokensCache()` (dropped on instructions save — M4), `getTokensFromInstructions(instructions: string): DesignTokens` (parses arbitrary content — used by snapshot conversion, FR-23).

### `lib/storage.ts` — Pluggable storage interface + factory (FR-44/45)
- **Purpose:** The only storage gateway app code uses. v1: filesystem (`DATA_DIR`); Vercel deploy: MongoDB + Vercel Blob (M5, `lib/storage-mongo.ts`). Owner seams on every operation (v1 ignores them).
- **Exports:** `StorageBackend` interface (incl. M4 additions `listInstructionsHistory()` → `{ version, savedAt }[]` newest-first, `readInstructionsVersion(version)` → content or null), `getStorage()` (factory — MongoDB when `MONGODB_URI` set, else FS), `createMongoBlobStorage()` (M5 — real Mongo/Blob implementation, no longer a throw stub).

### `lib/storage-mongo.ts` — MongoDB + Vercel Blob storage implementation (M5, FR-44)
- **Purpose:** The remote backend behind the pluggable interface. MongoDB collections (explicitly typed rows): `documents` (`_id` = doc id, all doc fields), `files` (`_id`, `url`, `contentType` — blob URL index per attachment), `instructions` (`_id` = `"active"` | `"history:<version>"`, `content`, `savedAt`). Lazy singleton connection (cached promise). Missing instructions seeded from the repo copy on first read (same M4 behavior as FS).
- **Functions:**
  - `getDb()` (private) — lazy `MongoClient.connect` cache; throws with a clear message when `MONGODB_URI`/`BLOB_READ_WRITE_TOKEN` are absent.
  - `createMongoBlobStorage()` — returns a `StorageBackend`: `listDocuments` (find + sort updatedAt desc), `getDocument`, `saveDocument` (updateOne `$set` + upsert), `deleteDocument` (best-effort blob `del` + deleteMany), `readFile/writeFile/deleteFile` (Blob `put`/fetch/`del` + files index rows), instructions ops (read/write/snapshot/history/version — see `lib/instructions.ts` for the same flow).
- **Note:** PUT/POST document routes keep the same `persistDocument` flow — only the backend differs; API behavior is identical between FS and Mongo/Blob.

### `lib/storage-fs.ts` — Filesystem storage implementation (FR-17)
- **Purpose:** `data/` layout per FR-17. Path-traversal guard via `SAFE_FILENAMES` allowlist.
- **Functions:**
  - `createFSStorage(dataDir)` — returns a `StorageBackend`: `listDocuments` (scan `data/documents/*/document.json`, skip invalid, sort updatedAt desc), `getDocument` (null when missing/corrupt), `saveDocument` (mkdir + pretty JSON), `deleteDocument` (recursive rm), `readFile/writeFile/deleteFile(docId, filename)`, `readInstructions` (seeds from repo copy when missing, M4), `writeInstructions`, `snapshotInstructions` (copies to `history/<version>.md`), `listInstructionsHistory` (newest-first by mtime), `readInstructionsVersion` (sanitizes `[^\w.-]` → `_`).

### `lib/instructions.ts` — Instructions management (M4, FR-21/22/23/47)
- **Purpose:** THE owning file for the runtime instructions lifecycle: idempotent first-run seeding from the repo copy, version hashing, save-with-history (previous version snapshotted before overwrite), reset-to-repo, per-document snapshots (`documents/<id>/instructions.snapshot.md`), and resolution of which instructions a conversion uses (active vs. snapshot). Token-cache invalidation is coupled to every write so design changes take effect immediately (FR-47).
- **Functions:**
  - `hashVersion(content)` — sha1 of the content, first 8 hex chars.
  - `seedInstructionsIfMissing(activePath)` — idempotently copies `docs/html_instructions.md` → the given path (default `ACTIVE_INSTRUCTIONS_PATH`) + invalidates the token cache.
  - `getInstructionsState(storage)` — `{ content, version, source: "active", history }`.
  - `saveInstructions(storage, content)` — throws `InstructionsError` when no `<!-- TOKENS -->` block (FR-47); snapshots the current version to history, writes active, invalidates the token cache; returns the new version.
  - `resetInstructions(storage)` — restores the repo copy as active (previous kept in history); returns the new version.
  - `readDocumentSnapshot(storage, docId)` — reads the doc's snapshot → `{ content, version } | null`.
  - `resolveConversionInstructions(storage, docId, useSnapshot)` — snapshot rules when `useSnapshot` and the doc has one, else the active instructions.
  - `InstructionsError` — typed error (TOKENS validation, missing repo copy).

### `lib/html-template.ts` — Styled HTML generator (FR-9, Plan §8.1)
- **Purpose:** Deterministic, self-contained styled HTML from block data + shared tokens. M6: now THE single HTML renderer (the offline "template conversion" mode was deleted — this renders previews/downloads only). Follows `docs/html_instructions.md`: A4 `@page`, Georgia/Times, token colors, `.qa-block` cards, `break-inside: avoid` in print. Full Q&A rendering: sequential circular badges, translation `<em>`, grammar note, response label, user-answer box (dashed left border), model answer, answer translation, analysis, vocab grid (`.two-col`/`.one-col`). **M6 paragraph enrichment:** `p.p-translation` (English translation under the text), `.p-analyse` ("Analyse :" box), and the same vocab/expressions grid as Q&A cards — plus the paragraph practice answer rendered as a `.qa-user-answer` dashed box (M6 bug fix, parity with qa). Color policy: every color from tokens only (FR-47); omission rules FR-36: hidden elements omitted entirely.
- **Functions:**
  - `escapeHtml(text)` — HTML-escapes user content (XSS defense; preview iframe is also sandboxed).
  - `renderInlineMarkdown(text)` — light inline markdown: `` `code` ``, `**bold**`, `*italic*` (applied after escaping).
  - `buildCss(tokens, paper = false)` (private) — the `<style>` block; when `paper` is true, appends the A4-sheet screen rules. **2026-08-10 #4 (user: "preview must be same size as the spec"):** the sheet now renders at the SPEC's SCREEN design — `body` stays at base `11.5px` (the old `body { font-size: print }` override was dropped) and `.document` pads with `pageMargin` (`18mm`), so the preview matches "GLOBAL STYLE: margins 18mm, base 11.5px" exactly. True print behavior lives in the `@media print` rules (base CSS: 10.5px, padding 0; paper block: margin/shadow stripped), so printing the preview still produces a clean A4 at print size/14mm. **2026-08-10 #3:** every secondary `font-size` is `em` (base-relative — `rem` would resolve against the 16px root and render English translations BIGGER than the French base; that was the "french small, english bigger" bug). `.block-paragraph` leads at `1.15em` (~13.2px at the 11.5px base; English translation 0.9em ≈ 10.4px, clearly secondary).
  - `generateTemplateHTML(doc, tokens, opts?: TemplateRenderOpts)` — builds the full HTML document (doctype, `<style>` from tokens, `<main class="document">` with block sections; Q&A numbering is sequential across the doc). `printMode: true` (preview route) shows the document as an A4 sheet at the spec's SCREEN size (base 11.5px, 18mm margins, white + shadow) — the `@media print` rules inside keep true print behavior when the preview is printed. **TemplateRenderOpts (2026-08-10, M7):** exported interface `{ printMode?: boolean; hidden?: { translations?, analyses?, vocab?, modelAnswers? } }` — the `hidden` flags (from the preview sheet's field toggles) omit those enrichment sections for qa + paragraph/essay blocks; passed through to `blockToHtml`/`qaBlockHtml`.
  - `vocabGridHtml(vocab, expressions)` — 2026-08-10: grid background `t.colors.vocabBg`, row borders `t.colors.rowBorder` (spec tokens).
  - `tagClass(tag)` (private) — sanitizes user tags into CSS classes (`#past-tense` → `tag-past-tense`).
  - `qaVisible(doc, content, kind)` (private) — visibility check: hidden when the per-block flag OR the document `practice` default is set (FR-34/35).
  - `vocabGridHtml(vocab, expressions)` (private) — builds the vocab/expressions grid for BOTH qa cards, enriched paragraphs AND essays (2026-08-10); `.two-col` when both lists exist, `.one-col` otherwise.
  - `qaBlockHtml(doc, block, tokens, number, opts)` (private) — assembles a full `.qa-block` card in the **2026-08-10 (M7) field order** (question → question translation → analysis → **response label (moved ABOVE the answer 2026-08-10 #5 — it labels the answer area)** → model answer → answer translation → user answer → grammar note → vocab grid), each optional part gated by the `hidden` opts (`translations` hides question + answer translations, `analyses` hides analysis, `modelAnswers` hides the model answer, `vocab` hides the vocab/expressions grid); omitted parts never emitted.
- **Essay rendering (2026-08-10):** `blockToHtml` case "essay" wraps in `.block-essay` (margin 0 0 0.9em) — **an optional `<h3 class="block-essay-heading">` (2026-08-10 #5, navy bold 0.95em)**, paragraphs as `<p class="block-paragraph">`, then the single `userAnswer` as `.qa-user-answer` dashed box, then the ONE translation/analysis/vocab-grid set, exactly like the paragraph case.

### `lib/pdf.tsx` — @react-pdf/renderer PDF generation (FR-14/15, Plan §8.2)
- **Purpose:** The ONLY PDF engine. Generates A4 PDFs from block data (never from HTML) — no Chrome/Puppeteer anywhere. Styles come from the shared tokens (FR-43): Times-Roman, token colors, print margins (~14mm). `.tsx` because it contains JSX. **M6:** three variants via `PDFVariant` — `"full"` (everything), `"questions"` (shareable practice sheet: title + questions + blank answer areas only), `"my-answers"` (after practice: questions + the user's own answers, no reference answers — send for checking). **M6 bug fix:** paragraphs participate in practice PDFs — `"full"` renders paragraph `userAnswer` (dashed box, parity with qa); `"my-answers"` includes paragraphs with the user's written answer (or `BlankAnswerArea` when unanswered). **Essay (2026-08-10):** `BlockToPDF` case "essay" mirrors the paragraph case — all paragraphs as one continuous passage, single userAnswer box, one enrichment set (full only). **2026-08-10 #5:** an optional heading leads the passage (`styles.essayHeading` — bold, heading color); on the PRACTICE sheets (questions / my-answers) ONLY the heading renders (the passage is the writing task — the sheet shows the title + rules/answer, not the source text; user: "in practice it should only show the heading"). Block filter: title + qa + **essay on BOTH non-full variants**; standalone paragraphs remain my-answers-only. Paragraph/essay translation/analysis/vocab grid render only in `"full"`. Note: react-pdf v4.6 has no `breakInside: "avoid"` — closest available is `minPresenceAhead` (documented limitation, see suggestions.md).
- **Functions:**
  - `lengthToPt(value)` — converts token lengths ("14mm", "11.5px", "0.8rem") to points for react-pdf.
  - `BlockToPDF({ block, doc, tokens, qaNumber, variant })` — maps a block to react-pdf elements; QA numbering sequential across the doc.
  - `QABlockPDF(...)` (private) — full Q&A card; omission matrix by variant (`hideAnswers = variant !== "full"`, `showUser = variant !== "questions"`, `showExtras = variant === "full"`) over the `qaVisible` rules (FR-36). **2026-08-10 (M7):** field order reordered to question → analysis → **response label (2026-08-10 #5: moved above the answer)** → model answer → blank answer area → answer translation → user answer → grammar note → vocab grid — mirrors the editor + HTML template (user-requested order).
  - `BlankAnswerArea({ basePt, color })` (private) — empty ruled dashed area ≈4 lines (FR-49) for unanswered questions in non-full variants.
  - `qaVisible(doc, content, kind)` (private) — same visibility logic as the HTML template (FR-34/35).
  - `generatePDFBuffer(doc, tokens, opts?)` — renders `<PDFDocument>` via `renderToBuffer`; `opts.variant` selects the variant (M6 replaces the old `practice?` flag); non-full variants filter blocks to title + qa + essay (2026-08-10), with paragraphs only on my-answers. (2026-08-10: vocab grid rows use `t.colors.vocabBg` background + `t.colors.rowBorder` borders — matches the HTML spec.) **Page footer (2026-08-10):** every page (all three variants) carries a `fixed` absolutely-positioned View at `bottom: printMargin` (the margin band below the content box — react-pdf absolute positioning is page-relative) with a `<Text render={({ pageNumber, totalPages }) => pageNumberLabel(pageNumber, totalPages)} />` — "1/7" style footers, fontSize `basePt * 0.8`, color `tokens.colors.border`. Verified end-to-end: full = 1/2, 2/2; questions/my-answers = 1/3, 2/3, 3/3 (TJ hex-run decode).

### `lib/html-to-blocks.ts` — HTML→blocks parse-back (M5, FR-41)
- **Purpose:** Best-effort parser that reconstructs editable blocks from rendered HTML (imported via "Paste HTML back" or the editor's "Parse to blocks" button on external-html documents). Scans for the class vocabulary emitted by `lib/html-template.ts` (`block-title`, `block-heading`, `block-paragraph`, **`block-essay` (2026-08-10)**, `block-separator`, `block-qa`/`qa-block`; inside `.qa-block`: `.qa-question-text` with `<em>` translation, `.qa-grammar-note`, `.qa-response-label`, `.qa-user-answer`, `.qa-answer` exact-single-class, `.qa-translation`, `.qa-analyse`, `.qa-vocab-grid` `.two-col`/`.one-col` → `.qa-vocab-col` with headers "Vocabulaire Clé"/"Expressions Avancées"). Purely string-based — no DOM — runs identically in the browser and in node smoke tests. Unrecognized elements become paragraph blocks with the raw HTML preserved. Bold/italic/code markers restored as markdown (FR-42). Hide flags can't be detected from rendered HTML (hidden fields are omitted, so they never fabricate).
- **Exports:** `parseHtmlToBlocks(html): { blocks, unparsedCount }`, `collectTopLevel(html): Element[]`.
- **Functions:** `classList(attrs)`, `decodeHtml`, `textOnly(inner)`, `innerToMarkdown(inner)`, `elementEnd(html, start, tag)`, `collectTopLevel(html)`, `extractMain(html)`, `byClass(inner, cls)` (stack-based deep traversal — the M5 infinite-loop fix), `elementsByClass(inner, cls)`, `parseRows(container, rowClass)`, `parseQa(inner)`, **`parseEssay(inner)` (2026-08-10 — `.block-paragraph` children → `EssayContent.paragraphs`, plus the single userAnswer/translation/analysis/vocab-grid like `parseQa`; **`.block-essay-heading` → `heading` (2026-08-10 #5)**)**, `blockWithContent(type, content)`.

### `lib/structuring.ts` — Canonical AI block parser (M6, NEW)
- **Purpose:** THE one JSON-array parser for AI-structured content. Both the convert route and the client-side "Paste blocks (AI)" modal consume it, so the AI output shape, the Copy-for-AI format, and the paste round-trip are always the same format (FR-42). Tolerant: strips markdown fences, extracts first `[`…last `]`, zod-validates each entry, maps validated entries to blocks via the typed factories.
- **Exports:** `extractJsonArray(raw)` (fence-stripped slice → JSON or null), `aiBlockEntrySchema` (zod discriminatedUnion on `type` — title/heading/paragraph/**essay (2026-08-10)**/qa/separator, `.loose()`, optional fields; qa requires `question`; paragraph carries `text` + `translation`/`analysis`/`vocab`/`expressions`; essay carries required `paragraphs: string[]` + **optional `heading` (2026-08-10 #5)** + the same optional enrichment; **qa carries `suggestions` (2026-08-10) — `z.array(suggestionSchema).catch([]).optional()`: the array-level catch is the final safety net so a malformed list can never kill the qa block**), `parseStructuredBlocksResponse(raw): Block[]` (validates + normalizes: trims empty strings, drops empty lists, qa defaults `responseLabel: "RÉPONSE"` + `hideTranslation: false` + `hideModelAnswer: false`; essay filters empty paragraphs and drops the block when none remain; skips empty-text entries). **Suggestions normalization (2026-08-10):** `optSuggestions(items)` — trims original/suggestion/reason, drops empty entries (the schema's per-field catches already degraded malformed ones to empty), caps at 10, assigns `crypto.randomUUID()` per row (works server + browser — precedent: `createBlock`); field mismatches are NOT dropped at parse — the editor's render-time filter (`lib/suggestions.ts`) handles them losslessly.

### `lib/suggestions.ts` — AI corrections logic (2026-08-10, NEW)
- **Purpose:** Pure (no React/DOM) rules for the AI-reported corrections list on qa blocks. The AI NEVER edits text — the user applies or dismisses each row individually.
- **Functions:**
  - `targetText(c, s)` — the field text a suggestion targets (`question` or `modelAnswer`).
  - `visibleSuggestions(c)` — cosmetic stale filter: only suggestions whose `original` still exists verbatim in the field render (user edits hide them, never corrupt them).
  - `applySuggestion(c, s)` — replaces the FIRST occurrence of `original` with `suggestion` (literal string replace — no regex injection), removes ONLY that row, then re-filters the surviving rows against the post-replacement text (an applied edit invalidates overlapping suggestions); a stale `original` → dismiss-only, text untouched.
  - `dismissSuggestion(c, id)` — removes the row from content (so it stays gone across re-renders).
- Covered by `tests/smoke-m6.ts`.

### `lib/auto-grow.ts` — Textarea auto-grow helper (2026-08-10, NEW)
- **Purpose:** The `autoGrow(el)` DOM helper extracted from Block.tsx so Q&A cards, enrichment fields and practice answers share one implementation (FR-25).
- **Functions:** `autoGrow(el)` — reset height to auto, then clamp to `scrollHeight`.

### `lib/pdf-labels.ts` — Pure PDF label helpers (2026-08-10, NEW)
- **Purpose:** Lives OUTSIDE `lib/pdf.tsx` so smoke tests can import it without dragging in `@react-pdf/renderer` (the tests tsconfig only compiles `../lib/**/*.ts`).
- **Functions:** `pageNumberLabel(pageNumber, totalPages)` — `"1/7"` style label for the PDF footer.

### `lib/questions.ts` — Question-list parsing + AI structuring (M3, FR-32/38)
- **Purpose:** Local numbered/bulleted-list parser for question import, plus the AI-structuring prompt + tolerant JSON response parser (uses `extractJsonArray` from `./structuring`).
- **Functions:**
  - `splitQuestions(text)` — splits a pasted list: numbered (`1.`/`1)`), bulleted (`-`/`*`), blank-line separated; continuation lines merge into the current item; a numbered/bulleted line always starts a new item.
  - `questionsToQaBlocks(questions)` — question-only Q&A blocks with `responseLabel: "RÉPONSE"`.
  - `buildStructuringUserPrompt(questions)` — numbered list + exact JSON-array schema instruction for the AI.
  - `parseStructuredQaResponse(raw)` — tolerant: `extractJsonArray` → zod `.loose()` per entry → `QaContent[]`; empty array on garbage.

### `lib/prompt.ts` — Prompt assembly + block serialization (M3, FR-12/39, Plan §9.1)
- **Purpose:** The user section serializes every block with type markers (`<TITLE>`, `<HEADING LEVEL="2|3">`, `<PARAGRAPH FORMAT="markdown">`, `<ESSAY>` with an optional `<HEADING>…</HEADING>` first line (2026-08-10 #5) + `<P>…</P>` per paragraph (2026-08-10), `<SEPARATOR/>`, `<QA>` with `QUESTION:`/`QUESTION_TRANSLATION:`/`GRAMMAR_NOTE:`/`RESPONSE_LABEL:`/`MODEL_ANSWER:`/`ANSWER_TRANSLATION:`/`ANALYSIS:`/`VOCAB: term|def; …`/`EXPRESSIONS: …`/`HIDE_TRANSLATION:`/`HIDE_MODEL_ANSWER:`). **M6:** `USER_ANSWER:` was REMOVED from `serializeQa` — practice answers are private and never serialized anywhere (the "Copy for AI" markers and the convert prompt both exclude them). `buildAIPrompt` ends with the exact JSON-blocks demand: "Return ONLY a JSON array of block objects — no markdown fences, no explanations, no HTML" with the SIX block shapes (essay included), plus the rule "Group consecutive prose paragraphs of the same passage into ONE essay object with a single shared enrichment set — never split an essay into per-paragraph parts", "Omit any optional field you cannot fill with confidence", "Never invent an answer for an unanswered question — leave modelAnswer out entirely", "Never include user answers". **CORRECTIONS demand (2026-08-10):** the qa JSON shape example carries `"suggestions":[{"kind":"…","field":"…","original":"…","suggestion":"…","reason":"…"}]` and a CORRECTIONS paragraph — check every qa question/modelAnswer for spelling (accents), grammar, punctuation (French spacing: no space before `,` `.` `;`, space before `:` `;` `!` `?`); NEVER rewrite the text (verbatim); one suggestion per distinct mistake with `original` matching verbatim; omit `suggestions` when correct; max 10. **Suggestions are never serialized** in `serializeQa`/`serializeBlocksForAI` (conversion-time feedback, not document content — verified by smoke-m6).
- **Functions:**
  - `serializeQa(c)` (private) — one QA block in marker format (no user answer).
  - `serializeBlocksForAI(doc)` — whole document with type markers (was also the `markerText` of the export-prompt route — **route removed 2026-08-10 #5**, the Copy menu is now a single FR-50 button). Essay → `<ESSAY>` with optional `<HEADING>` + `<P>` per paragraph.
  - `buildAIPrompt(doc, instructions, goal?)` — `{ system, user }`: system = active instructions verbatim; user = optional `GOAL: …` line + serialized blocks + the JSON-blocks demand.
  - `serializePlainText(doc)` — quick plain-text flattening (kept for parity even though the FR-39 copy UI was removed); essay = heading + paragraphs joined with blank lines (2026-08-10 #5).
  - `buildAICopyText(doc)` — **2026-08-10 (M7):** the copy dialog's "For AI" tab — one copyable text = round-trip instruction ("You are helping prepare French practice material. Convert the content below into structured document blocks… Return ONLY a JSON array of block objects…" with every block shape incl. essay + qa suggestions) + `\n=== CONTENT ===\n` + `serializeBlocksForAI(doc)`. The other AI's response pastes back via Paste blocks (AI) (same seam, same parser) — the app makes no AI call. Practice answers stay private (never included — verified by smoke-m7).

### `lib/validate.ts` — HTML validation & wrapping (M3, FR-10)
- **Purpose:** Pasted HTML is normalized before import: fences stripped, sanity-checked, fragments wrapped in a full document, DOCTYPE prepended. Never trusted raw. (AI conversion no longer goes through this — the convert route returns blocks, not HTML.)
- **Functions:**
  - `validateAndWrapHtml(input)` — strip fences → keep full documents (`<!doctype html>`/`<html`) as-is → otherwise wrap fragment with `<title>` from the first `<h1>` (fallback "Document").

### `lib/save.ts` — Document persistence flow (FR-17, M6)
- **Purpose:** Shared by POST/PUT document routes: always writes `document.json`; when a preview exists, also writes `document.html` + regenerates `document.pdf`. **M6:** `html?` is passed only by the import-html route; regular saves carry `instructionsVersion?` — when present, the server snapshots the ACTIVE instructions to `documents/<id>/instructions.snapshot.md` (FR-23), which is what makes "convert with this document's snapshot rules" possible later.
- **Functions:**
  - `persistDocument(storage, doc, html?, instructionsVersion?)` — saves JSON, then (if html) writes the html file + renders and writes the PDF file; when `instructionsVersion` is provided, reads the active instructions and writes the per-doc snapshot.

### `lib/ai.ts` — DeepSeek client (M3, FR-8/30/31, one-change-one-file FR-48)
- **Purpose:** THE ONLY file that talks to the AI. Plain `fetch` against `${baseUrl}/v1/chat/completions` (OpenAI-compatible), temperature 0.3, Authorization Bearer. Env-configurable base URL + model. Actionable error messages (FR-30). Token-usage console logging per conversion (FR-31).
- **Functions:**
  - `getAIConfig()` — `{ apiKey, baseUrl, model }` from `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` (default `https://api.deepseek.com`) / `DEEPSEEK_MODEL` (default `deepseek-chat`).
  - `hasAIKey()` — true when `DEEPSEEK_API_KEY` is set.
  - `stripMarkdownFences(text)` — removes ```…``` code fences from AI output.
  - `convertWithAI(system, user)` — POST chat completion; throws `AIError` (message + optional HTTP status) on API errors; strips fences; logs usage (FR-31).

### `lib/zip.ts` — Zero-dependency ZIP writer (M5, backup)
- **Purpose:** Minimal ZIP archive builder (deflate method 8, node:zlib `deflateRawSync`) for the library backup — no external archive dependency.
- **Functions:**
  - `createZip(entries: { name: string; data: Buffer }[]): Buffer` — local file headers (0x04034b50, 30 bytes), central directory (0x02014b50, 46 bytes, offsets at 42), EOCD (0x06054b50, 22 bytes); per-entry CRC-32 from a table-driven `crc32` (256-entry `CRC_TABLE`).

### `lib/tags.ts` — Tag parsing (M5, FR-5/18)
- **Purpose:** Normalizes raw tag input ("French, #past-tense, french") into canonical tag strings.
- **Functions:**
  - `parseTags(raw: string): string[]` — lowercase, strip `#` prefixes, whitespace → `-`, trim, dedupe, drop empties.

### API routes (`app/api/…`)
- `api/convert/ai/route.ts` — **POST** `{ doc, goal?, useSnapshot? }` → `{ blocks, instructionsVersion }` (AI conversion, FR-8/29/30, **M6: returns editable structured blocks**). Zod-validates; missing key → 400 with actionable message (FR-30); instructions via `resolveConversionInstructions` (FR-23) as the system prompt; `buildAIPrompt` → `convertWithAI` → `parseStructuredBlocksResponse`; `blocks.length === 0` → 502 with a hint about the snapshot-rules toggle; `instructionsVersion = hashVersion(instructions)` returned so the editor records it on save (FR-23).
- `api/preview/route.ts` — **POST** `{ doc, hidden? }` → `{ html }` (M6, NEW). Stateless on-demand preview: zod-validates the document, `getTokens()` → `generateTemplateHTML(doc, tokens, { printMode: true, hidden })`. The editor posts its CURRENT doc (unsaved edits included) — no save, no artifact written. (Bug fix 2026-08-10 #1: unwraps `payload.doc ?? body` like `/pdf` and `/convert/ai` — the editor's `{ doc }` wire shape previously hit a 400 "Invalid document payload". **2026-08-10 #4:** `printMode` semantics changed — the preview now shows the SPEC's SCREEN design (base 11.5px, 18mm margins) instead of the forced print size; `@media print` still applies when the sheet itself is printed. **2026-08-10 #3 (M7):** `hiddenSchema` (4 optional booleans: translations/analyses/vocab/modelAnswers, `.optional()` at the object level) validated via zod and passed to the renderer — the preview sheet's field toggles need no new endpoint.)
- `api/documents/route.ts` — **GET** list (optional `?owner=` filter, FR-45) → `{ documents }`; **POST** `{ doc, instructionsVersion? }` → creates + persists artifacts (201).
- `api/documents/[id]/route.ts` — **GET** → `{ doc, snapshotInfo: { version, differs } | null }` (404 if missing; `differs` = snapshot version ≠ active version — FR-23); **PUT** `{ doc, instructionsVersion? }` (id must match route; persists artifacts); **DELETE** → 204. `params` typed as `Promise<{ id }>` (Next 15+ convention).
- `api/documents/[id]/html/route.ts` — **GET** download HTML (saved file, else freshly generated from block data — regenerate, FR-20); attachment filename from doc title.
- `api/documents/[id]/pdf/route.ts` — **GET** `?variant=full|questions|my-answers` + **POST** `{ doc, variant }` (M6). Both generate the PDF instantly from block data via `generatePDFBuffer` — the POST variant renders the editor's CURRENT doc (unsaved edits included, no gating, FR-46 gate removed); `parseVariant` helper; per-variant attachment filenames (`-questions` / `-my-answers` suffixes).
- `api/documents/[id]/regenerate/route.ts` — **POST** re-convert from JSON (template render) + re-render PDF (FR-20); 404 when missing.
- `api/convert/structure/route.ts` — **POST** `{ questions: string[] }` (1..200) → `{ blocks }` (FR-32 AI mode, used by Paste questions → "Structure with AI"). Missing key → 400; instructions as system prompt + `buildStructuringUserPrompt`; `parseStructuredQaResponse`; empty result → 502.
- `api/documents/import-html/route.ts` — **POST** `{ html, title? }` → 201 `{ doc, html }` (FR-40). `titleFromHtml` (title tag or first h1), `validateAndWrapHtml`, new document with `source: "external-html"` + empty blocks, `persistDocument` (with html → writes html + pdf).
- `api/documents/backup/route.ts` — **GET** full-library backup ZIP (M5). Lists documents → entries `doc.id/` (directory), `doc.id/document.json` (pretty), plus `document.html`, `document.pdf`, `instructions.snapshot.md` when present → `createZip` → `Content-Type: application/zip` + `Content-Disposition: attachment; filename="writer-app-backup-<date>.zip"`; body via `new Uint8Array(zip)` (Buffer isn't a valid BodyInit).
- ~~`api/export/prompt/route.ts`~~ — **REMOVED 2026-08-10 #5:** the Copy dropdown is now a single "Copy…" button (FR-50 dialog only) — the FR-39 marker/system/plain-text copies moved into the paste sections, so this route had no UI path left.
- `api/config/route.ts` — **GET** → `{ model, hasAIKey, instructionsVersion }` for the status bar / UI state (FR-28), never leaks the key; instructions version read via storage (seeds on first run).
- `api/instructions/route.ts` — **GET** → `getInstructionsState(storage)` (content/version/history — FR-21/22); **PUT** `{ content }` (zod min 1) → `saveInstructions`, `InstructionsError` → 400 with its message.
- `api/instructions/reset/route.ts` — **POST** → `resetInstructions(storage)` → `{ ok, version }` (FR-22).

### `next.config.ts`
- **Purpose:** `serverExternalPackages: ["@react-pdf/renderer"]` so the PDF engine works in route handlers (FR-14/15).

### `.gitignore` / `.env.local.example`
- **Purpose:** `/data` runtime storage ignored; `.env*` ignored except the committed example; env vars per requirements §12 (DEEPSEEK_*, DATA_DIR, MONGODB_URI, BLOB_READ_WRITE_TOKEN).

### `components/Editor.tsx` — Main editor (client, M6 redesign)
- **Purpose:** All document state + flows. M6: init by `docId` (route `/doc/[id]`; unknown ids start fresh with the same id so the first save creates the doc — no localStorage draft anymore); convert with AI → POST /api/convert/ai → the doc's blocks are REPLACED with the AI's editable structured blocks (practice answers + hide flags carried over by matching question text); save sends `{ doc, instructionsVersion }` (the version from the last conversion — drives snapshot bookkeeping); **autosave (M6 bug fix):** quiet debounced (~1.2s) server save on every edit — practice answers and any change land on the server without touching the toolbar's busy state (guards: `busyRef` + `savingRef`, dirty-flag cleared only when the saved snapshot is still current); on-demand preview (POST /api/preview → full-screen PreviewSheet); instant PDF (POST /api/documents/[id]/pdf with `{ doc, variant }` — three variants); Practice master key (all blocks shown — questions AND paragraphs get "My answer" boxes; Check/Hide-answers cycle + Reset practice clears qa AND paragraph answers); Cmd/Ctrl+S + Cmd/Ctrl+Enter shortcuts (FR-7); global visibility buttons (FR-35); copy-for-AI (FR-39); paste blocks/questions/HTML (M6 adds "Paste blocks (AI)") + copy-for-sharing dialog (FR-50); instructions version in the status bar (FR-28/47); snapshot-aware conversion toggle (FR-23); block operations (update/convert-type/remove/move/insert-after/append + M5 drag-reorder, Enter-split, Backspace-merge); document tags (M5); "Parse to blocks" for imported HTML (M5, FR-41).
- **Functions:** `beginBusy`/`endBusy` (busy label via state + `busyRef` so keyboard handlers never read stale state), `mutateDoc(fn)` (marks dirty), `updateBlock`/`convertBlock`/`removeBlock`/`moveBlock`/`insertAfter`/`appendBlock`/`setTitle` (useCallback ops), `reorderBlock(fromId, toId)`, `splitBlock(id, rest)`, `removeBlockFocusUp(id)`, `updateBlockTags(id, tags)`/`setDocTags(tags)`, `parseToBlocks()` (GET /api/documents/[id]/html → `parseHtmlToBlocks` → replace blocks + `source: "editor"`), `setAllQaFlags(key, value)` (FR-35; 2026-08-10: sets a status message — "All question translations hidden — PDF and preview will omit them" / visible again — so the View dropdown actions give visible feedback), `convert(goal)` (M6: single AI path — replace blocks, carry over practice data, record `lastConvertInstructionsVersion`), `openPreview()` (M6), `copyPrompt(part)` (FR-39), `applyImportedBlocks(blocks)` (paste questions/blocks result — replace when empty, else append; closes both modals), `applyImportedHtml(doc, html)` (paste-HTML result — adopt the doc, resets snapshot info), `resetPractice()` (M6: confirm → clear every qa/paragraph/**essay** `userAnswer` + uncheck — 2026-08-10), `persist()` (M6: the server round-trip — POST when `!persistedRef`, else PUT, `{ doc, instructionsVersion }`; clears the dirty flag only when the doc didn't change mid-flight), `save()` (busy-wrapped persist for the Save button / Cmd+S), autosave effect (M6: 1.2s debounce on `[doc, loading, isDirty]` → quiet `persist()` → "Saved automatically"), `downloadPdf(variant)` (M6: POST `{ doc, variant }`, no save, no gating, variant filename suffix), `downloadHtml()`, `ensureSaved()`, `downloadBlob()`/`safeFilename()`/`copyToClipboard()`/`blockHasContent()` helpers (2026-08-10: array-valued content — essay `paragraphs` — counted by any non-empty string, not just `term`/`def` rows); computed `counts` (FR-37). State: `practiceMode` + `checked` (M6), **`focusMode` (M7 — toolbar checkbox; **DEFAULT ON (2026-08-10 #5, user: "focus mode should be default")**; passed down to BlockList/Block/QaBlockForm/ParagraphFields so enrichment is never rendered)**, `previewOpen` + `previewHtml` (M6), **`previewHidden` (M7 — `{ translations, analyses, vocab, modelAnswers }` preview field toggles; `openPreview(hiddenOverride?)` POSTs `{ doc, hidden: hiddenOverride ?? previewHidden }` — **2026-08-10 #5 stale-closure fix: toggles pass the NEW values straight into the POST so the preview re-renders correctly on the first click**; PreviewSheet's `onHiddenChange` re-renders immediately on every toggle)**, `lastConvertInstructionsVersion` (M6), `aiModel`, `instructionsVersion`, `snapshotInfo` + `useSnapshot`, modal flags (`showPasteQuestions`/`showPasteBlocks`/`showPasteHtml`/`showCopyDialog`); refs `docRef`/`persistedRef`/`useSnapshotRef`/`lastConvertRef`/`busyRef`/`savingRef`/`convertRef`/`saveRef`. **convert() carry-over (2026-08-10 #2, bug fix — user-reported answers lost):** practice answers now survive conversion for EVERY block type — qa matches on question text (+ hide flags), paragraphs on exact text, essays on joined paragraphs; when the AI groups previously-separate paragraphs into ONE essay, their answers merge in order (`essayAnswerFromParagraphs` run-matcher, module-level helper).

### `components/Toolbar.tsx` — Primary actions (client, M6 redesign, M7, FR-29/30/35/37/38/39/46/50)
- **Purpose:** Two-row toolbar: **title row** = ✎ brand + "Writer" wordmark as a **link home (M7 — clicking the Writer icon/text returns to the dashboard)**, borderless large title input, subtle tags input (M5, FR-5/18), quiet Instructions + Library nav links (M7: "Home" removed — **Home = Library**, `/library` redirects to `/`, so Library is the single nav item); **actions row** = Convert with AI split button (primary converts; caret opens the goal input + snapshot-rules checkbox — M6: single AI mode, template removed), Save, Preview (M6: full-screen sheet), **Focus checkbox (M7 — next to Practice; shows only the main content, all enrichment hidden)** + Practice toggle (M6 master key) + Check answers / Hide answers button (visible in practice, emerald when checked), and grouped dropdowns: Download ▾ (three PDF variants — full / questions only (share) / questions + my answers — + Download HTML, no disabled gating M6), View ▾ (Hide/Show all translations + answers FR-35; "Reset practice answers…" entry in practice mode M6), Copy ▾ (AI type markers / instructions system prompt / plain text — FR-39, Copy for sharing… — FR-50), Paste ▾ (blocks (AI) / questions / HTML — M6 adds blocks). `Dropdown` supports per-item `disabled` + `"—"` divider rows; all FR-* jargon removed from visible labels (kept in code comments).
- **Exports:** `VisibilityCounts` interface (translationsHidden/Total, answersHidden/Total — FR-37). (`ConvertMode` export REMOVED in M6.)
- **Functions:** `ActionButton` (module component), `Dropdown` (module component — fixed overlay for outside-click close), default export; convert-goal + tagsDraft state; props `practiceMode`/`checked`/`onToggleChecked`/`onResetPractice` (M6), **`focusMode`/`onToggleFocus` (M7 — the Focus checkbox, title: "Focus mode: only the main content — question and answer, paragraph text. All enrichment hidden.")**, `onPreview` (M6), `onDownloadPdf(variant: PDFVariant)` (M6), `onPasteBlocks` (M6), `snapshotInfo`/`useSnapshot`/`onToggleSnapshot` (FR-23). **2026-08-10 #5 (user: "why are there 4 options — there should only be one"):** the Copy ▾ dropdown is GONE — a single **`Copy…` ActionButton** opens the FR-50 copy dialog (the three FR-39 items + `onCopyPrompt` prop were removed; the AI-instruction copies live in the paste sections).

### `components/BlockList.tsx` — Block list (client, M5 drag-reorder, M6)
- **Purpose:** Renders blocks in order with per-block controls and the bottom add-block affordance. Native HTML5 drag-and-drop reordering (M5, FR-3). **M6:** practice mode shows EVERY block (bug fix: paragraphs were hidden) — qa/paragraph/essay blocks render their "My answer" cards (via `Block`), title/heading read as read-only context; the bottom `AddBlockMenu` is hidden in practice so structure can't change mid-practice. **2026-08-10:** structure FULLY frozen in practice — `draggable={!practiceMode && …}`; `lastType` state (`"paragraph"` default) persisted to localStorage (`writer-app:add-type`), consumed by the split AddBlockMenu (direct `+` adds `lastType` via `onAppend`, caret picks a type) and by each block's `＋` add-below (`onInsertAfter(block.id, lastType)`).
- **Functions:** maps visible blocks → `Block` rows; `onDragStart`/`onDragOver`/`onDrop`/`onDragEnd` handlers (dragId/overId state); `pickType(type)` (set + persist `lastType`); props `practiceMode` + `checked` (M6) + **`focusMode` (M7)** forwarded to `Block`.

### `components/Block.tsx` — Single block editor row (client, FR-1/3/25, M5, M6)
- **Purpose:** Text editing for title/heading/paragraph/essay (auto-grow textarea), Q&A form (renders `QaBlockForm` with M6 `mode`/`checked`), separator render, `/` slash-command popup (FR-2), FR-3 keyboard editing (Enter-split, Backspace-merge). **M6:** paragraphs render `ParagraphFields` (AI enrichment: translation/analysis/vocab/expressions) under the textarea when NOT in practice mode. **M6 practice (bug fix):** paragraphs render `PracticeParagraphCard`; title/heading render as read-only text; the slash menu / textarea editing are absent in practice. Header row: type label, heading-level select, per-block tags input (M5, FR-5), ↑/↓/＋/✕ control cluster (revealed on hover/focus); title/heading textareas get document typography.
- **2026-08-10 redesign:** `PracticeParagraphCard` is now a CONTINUOUS flow — paragraph text in normal document typography (no card, no "Paragraph" label, no duplicated boxes) with the dashed "My answer" textarea flowing directly beneath; checked → green left-border "Reference" box with the AI translation. The old boxed layout (separate labeled boxes) was removed per user feedback — "a paragraph is a continuous thing". Unused `inputCls`/`labelCls` import from RowEditor removed.
- **2026-08-10 essay (NEW block type):** `PracticeEssayCard` — the whole passage as continuous document-typography paragraphs + EXACTLY ONE "My answer" textarea (essays were being treated like per-paragraph Q&A; now one field for the whole essay); checked → green Reference box with the single essay translation. Normal mode: per-paragraph auto-grow textareas ("+ Add paragraph" button) + shared `ParagraphFields` enrichment. **Enter-split (FR-3) no longer applies to paragraphs/essays** — Enter inserts a newline (`block.type !== "paragraph"` guard); qa/heading still split. **Practice freeze:** controls cluster, tags, heading select, slash menu all `!practiceMode`-guarded.
- **2026-08-10 #3 (M7 — focus mode):** `focusMode` prop threaded from BlockList; in focus mode paragraphs render ONLY the textarea (no ParagraphFields enrichment) and essays render only the paragraphs (no shared enrichment) — Q&A delegates to QaBlockForm's focus branch.
- **2026-08-10 #5 (essay heading):** the normal essay editor gains an optional heading input above the paragraphs ("Essay title (optional)…", semibold — the AI fills it when possible); **`PracticeEssayCard` now shows ONLY the heading** (17px semibold — or a muted "(essay title)" placeholder) as the writing prompt + the single "My answer" textarea + checked Reference — the passage text is NOT shown in practice (user: "in practice it should only show the heading for the essay").
- **2026-08-10 #2 (user feedback): French-first practice typography** — the passage text leads at `text-[17px] text-zinc-900`, the "My answer" textarea at `text-[16px]` (both French — the material being practiced), and the English Reference box is clearly secondary at `text-[13px] text-zinc-600`. Practice is for French, not English.
- **2026-08-10 #3 (user feedback): preview/output French-first typography** — root cause: the template's secondary sizes were `rem` (root-relative = 16px), so English translations rendered at 14.4px while French paragraphs sat at the 10.5px print base — English literally bigger. All secondary `font-size` values are now `em` (base-relative: 0.9em/0.88em/0.82em/0.78em/0.8em) and `.block-paragraph` leads at `1.15em` (≈12.1px in print). PDF mirrors with `basePt * 1.15` for paragraphs/essays, translations stay `basePt * 0.9`.
- **2026-08-10 round 2 — auto-grow everywhere:** the local `autoGrow` callback-ref helper was REMOVED (moved to `lib/auto-grow.ts`); the practice answer textareas (`PracticeParagraphCard`, `PracticeEssayCard`) and the essay's per-paragraph textareas are now `AutoGrowTextarea`. The primary title/heading/paragraph textarea keeps its own ref+effect (needs `selectionStart` for Enter-split).
- **Exports:** `BLOCK_LABELS`, `SLASH_TYPES` (essay added after paragraph); `PracticeParagraphCard` + `PracticeEssayCard` (module components).
- **Functions:** `handleKeyDown`, `handleChange`, `applySlash(type)`; tags input (`tagsDraft`, commit on blur/Enter).

### `components/QaBlockForm.tsx` — Q&A card editor (client, FR-4/26/33/34/37, M6)
- **Purpose:** Two modes driven by the Practice master key. **Normal:** question + ALWAYS-visible "Answer" (modelAnswer) textarea with 👁/🙈 toggle (sets `hideModelAnswer`), optional extras behind "＋ chips" (question translation + EyeToggle, grammar note, answer label, answer translation, analysis, vocab, expressions) — M6: the reference answer is no longer an optional chip and `userAnswer` is no longer a normal-mode field; a muted note appears when a practice answer exists. **Practice (unchecked):** blue-tinted read-only question + "My answer" box (userAnswer, dashed border). **Practice (checked):** + green read-only "Model answer" box (or a "No model answer saved" placeholder). Rounded card container.
- **2026-08-10:** chips are SUPER subtle — `opacity-0 transition-opacity duration-200 group-hover:opacity-100 group-focus-within:opacity-100` (hover-reveal on the block's `group` root; 99% of enrichment comes from the AI, manual entry is the rare fallback).
- **2026-08-10 round 2 — "Suggested corrections" list:** under the Answer textarea (normal mode only, never practice), `SuggestionList` renders `visibleSuggestions(content)` — amber-tinted rows with a kind badge (spelling amber / grammar orange / punctuation rose), a Question/Answer field tag, ~~original~~ → suggestion, italic reason, an **Apply** button (`onUpdate(applySuggestion(content, s))` — changes ONLY that one, first occurrence) and a **✕** dismiss (`onUpdate(dismissSuggestion(content, s.id))`). Rows whose `original` the user already edited away simply don't render; dismissed rows are removed from content and stay gone. All logic lives in `lib/suggestions.ts` (pure, smoke-tested).
- **2026-08-10 round 2 — auto-grow:** modelAnswer, analysis and the practice "My answer" textareas are now `AutoGrowTextarea` (grow with content, re-measured on every render so AI imports and suggestion Apply re-grow them).
- **2026-08-10 #3 (M7):** **focus mode** (`focusMode?: boolean` prop) — renders ONLY the Question input + Answer textarea (no suggestions, no chips, no extras). **Field order (M7):** normal-mode JSX reordered to question → questionTranslation → analysis → Answer (+SuggestionList) → answerTranslation → chips → grammarNote → responseLabel → vocab → expressions — the editor matches the preview/PDF order ("question then translation then analysis then answer part").
- **Functions:** `usedFields(content)` (module-level), `EyeToggle` + `SuggestionList` (module components), default export form; `hideField(key)` clears + hides, `reveal(key)` shows; `RowEditor` + `inputCls`/`labelCls` MOVED to `components/RowEditor.tsx` (M6).

### `components/RowEditor.tsx` — Shared form primitives (client, M6, NEW)
- **Purpose:** Extracted from QaBlockForm so Q&A cards and paragraph enrichment share identical styling and the term/def row editor (vocabulary / expressions lists).
- **Exports:** `inputCls`, `labelCls` (shared class strings), `RowEditor({ rows, onRows, placeholderTerm, placeholderDef, termCls })` (term/def row list with add/remove). (2026-08-10: "+ Add row" button restyled to the muted emerald-hover look.)

### `components/ParagraphFields.tsx` — Paragraph/essay enrichment editor (client, M6, NEW)
- **Purpose:** AI enrichment for paragraphs AND essays (2026-08-10 — props loosened to `ParagraphContent | EssayContent`, so the ONE shared enrichment set of an essay edits here): translation input, analysis textarea (now `AutoGrowTextarea`, 2026-08-10), vocab/expressions RowEditor lists — mirrors QaBlockForm's "＋ chip" pattern: empty optional fields stay hidden, a chip reveals each, removing a field clears its data. Rendered under paragraph/essay editors when not in practice mode.
- **2026-08-10:** chip row label renamed to "AI details:" (enrichment is AI-driven; manual entry is the fallback) and styled SUPER subtle — `opacity-0 group-hover:opacity-100` hover-reveal, same as QaBlockForm.
- **Functions:** `usedFields(content)` (module-level — auto-reveal), default export; `reveal`/`hideField`/`set` helpers.

### `components/AutoGrowTextarea.tsx` — Auto-growing textarea (client, 2026-08-10, NEW)
- **Purpose:** Wrapper around `lib/auto-grow.ts` for every multi-line text field (Q&A answer/analysis/practice, ParagraphFields analysis, practice paragraph/essay answers, essay paragraphs). Adds `resize-none overflow-hidden` (without overflow-hidden the scrollbar makes scrollHeight jitter) and a no-deps `useEffect` that re-measures on EVERY render — typing, mount, AND programmatic value changes (AI convert, suggestion Apply) that fire no onInput event.
- **Functions:** none (props = `React.TextareaHTMLAttributes`; forwards `onInput` after growing). **2026-08-10 #5 (user: "the text box goes out of limit — make it scrollable"):** base class now `resize-none overflow-y-auto max-h-[45vh]` — very long content (big pastes in the paste modals, long answers) scrolls inside the box instead of growing past the window.

### `components/AddBlockMenu.tsx` — Split "+" add control (client, FR-2, 2026-08-10 rewrite)
- **Purpose:** Split control (user feedback: "should have a dropdown and a default choice, and later the last chosen option"): the main button is `+ <LABELS[lastType]>` — a DIRECT click adds the last-chosen type (default `paragraph`); the `▾` caret opens the full type menu (paragraph/essay/heading/qa/title/separator — `ITEMS`), with `✓ default` marking the current `lastType`; picking an item from the menu adds it AND becomes the new default. One pill, two buttons, emerald hover.
- **Exports:** `ITEMS` list, `LABELS` map; default component `{ lastType, onAdd(type), onPickType(type) }`.

### `components/PreviewSheet.tsx` — Full-screen preview (client, M6, NEW)
- **Purpose:** "Preview" opens this full-screen sheet (fixed inset-0 z-50) instead of the old always-on pane. Header: title, Refresh (re-renders via the editor's `openPreview` — shows "Rendering…" while busy), a "no saving needed" hint, ✕ close. Body: A4 sheet in a sandboxed iframe (`sandbox=""` — no scripts, no same-origin, per suggestions.md). Placeholder when there's no HTML yet.
- **2026-08-10:** iframe class `mx-auto block min-h-[600px] w-full max-w-[210mm]` — transparent, so the printMode A4 sheet (its own white + shadow) shows against the sheet backdrop; the old `bg-white` + outer shadow were removed. **2026-08-10 #4:** the sheet content is now the spec's screen rendering (11.5px base, 18mm) — the iframe itself is unchanged.
- **2026-08-10 #3 (M7 — field toggles):** header gains a "Show:" box — **"All extras"** master checkbox (`toggleAll` — checks/unchecks all four at once) + per-field checkboxes (Translations / Analyses / Vocab & expressions / Model answers) + a note "Headings, questions & paragraph text stay" when anything is hidden. Props: `hidden: PreviewHidden` + `onHiddenChange(next)`; the editor re-renders immediately on every toggle (see Editor). **2026-08-10 #5 (user-reported: "nothing is showing even though all extras is clicked"):** the master checkbox semantics were INVERTED — `checked` was `allHidden` (checked meant everything hidden). Now `checked={!anyHidden}` — "All extras" checked = every extra VISIBLE; clicking it hides all extras, clicking again restores them.
- **Functions:** none (stateless — props `html`, `busy`, `hidden`, `onHiddenChange`, `onRefresh`, `onClose`); exports `PreviewHidden` type + `EMPTY_PREVIEW_HIDDEN`.

### `components/PasteQuestionsModal.tsx` — Paste-questions import (client, FR-32/38)
- **Purpose:** Single-step flow: paste a question list → live count of detected questions → "Structure with AI" (POST /api/convert/structure) or "Parse locally (offline)" (`questionsToQaBlocks`) → `onResult(blocks)`.
- **Functions:** `structureWithAI()` (async, inline errors), `parseLocally()`; `splitQuestions` memo on the pasted text.
- **Note (UI polish):** all four modals (`PasteQuestionsModal`, `PasteBlocksModal`, `PasteHtmlModal`, `CopyDialog`) share a unified chrome — `bg-zinc-900/40` backdrop with blur, rounded-2xl white panel with header bar (title + ✕ close) and a border-t footer with right-aligned Cancel/primary buttons.
- **2026-08-10 (M6 round 2):** the paste textarea is now `AutoGrowTextarea` (grows with a long paste; `autoFocus` passes through the props spread).

### `components/PasteBlocksModal.tsx` — Paste AI blocks back (client, M6, NEW)
- **Purpose:** The JSON block array from "Copy → Copy for AI" (or any external AI) re-imports client-side via `parseStructuredBlocksResponse` (lib/structuring.ts) — completing the copy → external AI → paste round-trip (FR-42) for the new block format. Inline error when no valid blocks are found.
- **Functions:** `importBlocks()` (validates + `onResult(blocks)`).
- **2026-08-10 (M6 round 2):** paste textarea → `AutoGrowTextarea`.
- **2026-08-10 #3 (M7):** an `InstructionCopyBox` ("Instructions for another AI") sits ABOVE the textarea — the module constant `BLOCKS_INSTRUCTION` (the JSON-shape contract + essay-grouping + never-invent-answers + never-include-user-answers rules) with a one-click copy button, so the user can hand the other AI its parsing context (user request; Paste questions intentionally unchanged). **2026-08-10 #5:** the essay shape in `BLOCKS_INSTRUCTION` now carries the optional `"heading"` + the never-invent-never-force rule.

### `components/PasteHtmlModal.tsx` — Paste HTML back (client, FR-40)
- **Purpose:** Paste HTML from any external AI → POST /api/documents/import-html → `onImported(doc, html)` (editor adopts the document; the amber "Parse to blocks" banner offers editing).
- **Functions:** `importHtml()` (async, inline errors).
- **2026-08-10 (M6 round 2):** paste textarea → `AutoGrowTextarea`.
- **2026-08-10 #3 (M7):** an `InstructionCopyBox` ("Instructions for another AI") above the textarea — `HTML_INSTRUCTION` constant (print-ready A4 worksheet spec for the other AI: base font 11.5px/10.5px printed, 18mm/14mm margins, Georgia, French-first) with one-click copy (user request).

### `components/CopyDialog.tsx` — Copy dialog: exactly two options (client, FR-50, M7)
- **Purpose:** Exactly TWO tabs (user request — "this option and the ai instructions should be fine only these 2"): **"For sharing"** — checkboxes choose exactly what goes to the clipboard as clean plain text (paragraphs, title/headings, questions, user answers, model answers, translations, grammar notes, analysis, vocab). Translations + model answers off by default; Q&A numbering preserved (1., 2., …); last-used selection remembered in localStorage (`writer-app:copy-selection`); live preview of the output. **"For AI"** — one-click copy of `buildAICopyText(doc)` (round-trip instruction + `=== CONTENT ===` + serialized document; see lib/prompt.ts) so the user can take a finished document to any other AI and paste its JSON response back via Paste blocks (AI) — no AI call in the app. Clipboard with textarea fallback; disabled state only on the sharing tab (no selection).
- **Exports:** `CopySelection` interface, `DEFAULT_SELECTION`, `buildCopyText(doc, sel)` (pure — covered by smoke tests).
- **Functions:** `joinVocab` (private), `loadSelection` (private), `toggle(key)`, `copy()`; **M7:** `tab` state (`"share" | "ai"`) + header tab buttons; `aiText` = `buildAICopyText(doc)` (memo); copy uses `tab === "share" ? text : aiText`. **2026-08-10:** `buildCopyText` case "essay" — paragraphs joined with blank lines under the `paragraphs` selection, `Ma réponse : …` under `userAnswers` (indented when paragraphs are also copied).

### `components/InstructionCopyBox.tsx` — Copyable instruction box (client, 2026-08-10, M7, NEW)
- **Purpose:** Shared box ABOVE the JSON and HTML paste textareas (user request: "provide the copy instructions for each in above the text box which user can use to provide other ai the context in which it need to parse the data"): a blue-tinted panel with the instruction title ("Instructions for another AI"), the instruction text, and a one-click **Copy instruction** button (navigator.clipboard + execCommand fallback → "Copied ✓"). Reused by PasteBlocksModal (`BLOCKS_INSTRUCTION`) and PasteHtmlModal (`HTML_INSTRUCTION`); Paste questions intentionally unchanged (user likes it as-is).
- **Functions:** internal `copy()` with clipboard + fallback; `copied` state resets after 2s.

### `components/NewDocumentButton.tsx` — New-document button (client, M6, NEW)
- **Purpose:** Home page + library empty state: creates a fresh document client-side (`createDocument()` + one empty paragraph block, FR-24) and navigates to `/doc/<id>`; the first save then persists it.
- **Functions:** `createNew()` (router.push to the new id). Optional `className` prop for call-site styling.

### `components/LibraryList.tsx` — Library cards (client, FR-18/19/20, M5, M6)
- **Purpose:** Document cards (title → editor `/doc/<id>` — M6, updated date, block count, tags) + Regenerate (FR-20, per-card busy state) + Delete with confirm (DELETE route + `router.refresh()`); client-side sort control (updated/created/title); M5: clickable `#tag` chips row with filter state, "⬇ Backup zip" button (filename parsed from the Content-Disposition header). M6: empty state uses `NewDocumentButton` (creates a fresh doc and navigates, instead of linking to `/`).
- **Functions:** `remove(doc)`, `regenerate(doc)`, `formatDate(iso)`, `downloadBackup()`; `allTags` memo (usage-frequency sorted); `useMemo` sorted/filtered list by `sort` key + `filterTag` state.

### `app/page.tsx` — Home dashboard (M6, NEW)
- **Purpose:** `/` — SERVER component (was the client editor route). Header (brand mark, headline, document count) + `NewDocumentButton` + `LibraryList`. `export const dynamic = "force-dynamic"` (fs read at request time — never prerendered). Metadata "Home — Writer App".

### `app/doc/[id]/page.tsx` — Editor route (M6, NEW)
- **Purpose:** `/doc/<id>` — async server page: `const { id } = await params` (Next 16: params is a Promise), renders `<Editor docId={id} />`. `force-dynamic` (the editor is fully client-side — never prerender a specific document). Metadata "Editor — Writer App".

### `app/library/page.tsx` — Legacy library route (M6)
- **Purpose:** `/library` — redirects to `/` (`redirect("/")`); the dashboard at `/` lists documents now.

### `app/instructions/page.tsx` — Instructions route (M4, FR-21/22)
- **Purpose:** `/instructions` — server page shell (`force-dynamic`), renders `InstructionsEditor`. Metadata "Instructions — Writer App".

### `components/InstructionsEditor.tsx` — Instructions editor (client, M4, FR-21/22/47)
- **Purpose:** Edit the active instructions (the ONE place the design system + AI rules live): loads GET /api/instructions, textarea with dirty tracking, Save (PUT — rejected client-side/server-side when the TOKENS block is missing, FR-47), Reset to repo copy (confirm dialog → POST /api/instructions/reset), version history list (version hash, date, char count; Preview → fills the editor as draft, Restore → PUT), status/error banners; explains the `<!-- TOKENS -->` block + FR-47.
- **Functions:** `load()` (initial GET), `save()` (PUT, dirty-gated), `reset()` (confirm + POST), `previewVersion(entry)`, `restoreVersion(entry)`; dirty state via `useState`. **2026-08-10 #3 (M7 — crash fix, user crash report "Cannot read properties of undefined (reading 'length')" at line 211):** all four `entry.content` sites are now guarded with `?? ""` — `setDraft(entry.content ?? "")` (Preview ×2), `body: JSON.stringify({ content: entry.content ?? "" })` (Restore), and `{(entry.content ?? "").length} chars` (history list) — a history entry with no content can no longer throw.

### `app/layout.tsx` / `app/globals.css` / `public/`
- **Purpose:** Root layout (`LayoutProps<"/">` — Next 16.3 global helper), Tailwind v4 global styles, static assets. Metadata: "Writer App — Online Writer + Practice". `globals.css`: light-only theme (dark-mode block removed), body font routed through `var(--font-sans)` so the loaded Geist font actually applies, global navy focus-visible outline + slate selection color.
- **2026-08-10 design pass:** the Tailwind v4 `@theme` palette is REMAPPED to the PDF spec's ramp so the editor UI and the printed sheet share one look — `--background: #f7f9fb`, `--foreground: #1a1a1a`; zinc-* remapped to the spec grey ramp (#f7f9fb → #1a2738), blue-* to the navy ramp (#eef2f7 → #0d1a2e, blue-600 = #1e3a5f), emerald-* to the green ramp (emerald-50 = #e8f0e9 … emerald-600 = #2c5f2d); `:focus-visible` outline #1e3a5f, `::selection` #b9c9de. Document/print styles live in the instructions file, not here (FR-47).

### `tests/` — Smoke tests (M2 + M3 + M4 + M5 + M6 round 2 + M7)
- **Purpose:** In-project verification harness. `smoke-m2.ts` (22 checks: QA HTML + PDF variants — patched in M6: `{ practice: true }` → `{ variant: "questions" }`), `smoke-m3.ts` (37 checks: fences/validation, splitQuestions, structuring parser, prompt serialization + M6 "JSON blocks demand" / "user answer never serialized", buildCopyText, titleFromHtml), `smoke-m4.ts` (25 checks: hashVersion, seeding idempotence, save→history→cache-invalidate, TOKENS validation, reset, per-doc snapshot write via persistDocument — M6: version-gated, no version → no snapshot, resolveConversionInstructions active-vs-snapshot, history ordering/content), `smoke-m5.ts` (32/33 checks: parse-back round trip through the real template generator, tags parsing, ZIP structural signatures + inflate round-trip; 1 deferred check — vocab grid row term/def attribution, see suggestions.md), **`smoke-m6.ts` (24 checks, 2026-08-10 round 2:** suggestions preserved with uuid ids + kind default, malformed suggestion object → list dropped + qa block survives (tolerant parse), empty original/suggestion dropped, cap of 10, prompt demand carries the CORRECTIONS rule + suggestions shape, `serializeBlocksForAI` never serializes suggestions, applySuggestion first-occurrence-only / only-selected-removed / overlapping-stale-removed / stale-original-dismiss-only, dismissSuggestion by id, visibleSuggestions stale filter incl. question-field targeting, documentSchema round-trip keeps suggestions, pageNumberLabel 1/7 + 12/12**), **`smoke-m7.ts` (19 checks, 2026-08-10 M7 + round 2:** buildAICopyText — instruction heading, both JSON shapes present (essay shape now with `"heading"`), `=== CONTENT ===` marker, content follows (marker serialization), never includes practice answers; essay heading — `<HEADING>Ma ville</HEADING>` marker inside `<ESSAY>` + template renders `<h3 class="block-essay-heading">Ma ville</h3>` above the passage; qa field ORDER in the rendered HTML via indexOf (question < question translation < analysis < model answer < answer translation — apostrophe search uses the template's escaped `&#39;` forms); hidden toggles — all-four hides translations/analyses/vocab/model answers while MAIN content (question, paragraph text, heading, essay paragraphs + **essay heading**) and practice answers survive; vocab-only toggle leaves translations/analyses/answers intact**). Compiled with `tests/tsconfig.json` (outDir `tests/build`, `rootDir ..`, `@/*` alias via `paths`), run with `node --require <abs>/tests/alias-hook.js tests/build/tests/smoke-*.js` + `NODE_PATH=<abs>/tests/build` (absolute paths required). `tests/build` and `tests/.tmp-*` are gitignored.
- **Files:** `smoke-m2.ts`, `smoke-m3.ts`, `smoke-m4.ts`, `smoke-m5.ts`, `smoke-m6.ts`, `smoke-m7.ts`, `tsconfig.json`, `alias-hook.js` (resolves `@/` to the emitted build tree at runtime).

---

## Environment Variables

| Variable | Required | Purpose | Referenced in |
|---|---|---|---|
| `DATA_DIR` | no | Storage root (default `./data`) | `lib/storage.ts` factory |
| `DEEPSEEK_API_KEY` | AI mode | DeepSeek API key; absent → 400 with actionable message (FR-30) | `lib/ai.ts` (`hasAIKey`) → routes `convert/ai`, `convert/structure`; `config` route reports presence |
| `DEEPSEEK_BASE_URL` | no | API base (default `https://api.deepseek.com`); model route is `${baseUrl}/v1/chat/completions` | `lib/ai.ts` (`getAIConfig`) |
| `DEEPSEEK_MODEL` | no | Model name (default `deepseek-chat`); shown in the editor status bar (FR-28) via `/api/config` | `lib/ai.ts` (`getAIConfig`), `api/config/route.ts` |
| `MONGODB_URI` | Vercel deploy | MongoDB storage switch (activates Mongo/Blob factory — M5, FR-44) | `lib/storage.ts` factory, `lib/storage-mongo.ts` |
| `BLOB_READ_WRITE_TOKEN` | Vercel deploy | Vercel Blob token for html/pdf file storage (M5, FR-44) | `lib/storage-mongo.ts` |

---

## Planned Changes

- **M6 done (2026-08-10):** dashboard home, AI-only editable-block conversion, practice master key, on-demand preview + 3-variant PDF, AI enrichment for all text, paste-blocks round-trip. Bug-fix pass done: preview payload, practice autosave, paragraphs in practice (editor + PDF + HTML). **Design pass 2 done (2026-08-10):** essay block type through the full stack, practice structure-freeze, split add control with remembered default, super-subtle hover-reveal chips, Enter = newline in paragraphs, instructions Part 1 essay rules (Part 2 verbatim). **M6 round 2 done (2026-08-10):** AI corrections (suggestions) for qa blocks with per-row apply/dismiss, punctuation/typography rules in the AI protocol, PDF `1/7` page footers, auto-grow everywhere. **M7 done (2026-08-10):** focus mode, Q&A field order (question → translation → analysis → answer in editor + preview + PDF), Copy dialog = exactly 2 options (sharing + For AI round trip via Paste blocks), copyable instructions above JSON/HTML paste boxes, home = library + Writer brand home link, preview field toggles (per-field + All extras), InstructionsEditor crash fix. **M7 round 2 done (2026-08-10, user feedback batch):** focus default ON, optional essay `heading` field (never forced; practice = heading-only prompt), Copy ▾ dropdown → single `Copy…` button + `/api/export/prompt` route deleted, paste textareas scrollable (max-h cap), preview "All extras" checkbox semantics fixed (checked = all visible) + stale-closure fix on toggle, response label moved ABOVE the answer in template + PDF. Type-checks clean; build green; smoke M2–M7 green (M5 32/33).
- **Test generator (PLAN ONLY, FR-seam):** select 3–4 documents → AI builds a test from topics + questions only (paragraphs/answers NOT sent) — design recorded in suggestions.md, deliberately not built.
- Remaining: fix the one deferred M5 check (vocab grid row term/def attribution — see suggestions.md), final e2e pass against the running server, then push the unpushed milestones (M5 + UI polish + M6 + bug fixes + design passes 1 & 2 + M6 round 2 + M7) once the user confirms.

## Verified

- **M1:** build green; smoke 18/18 (TOKENS parsing, PDF buffer); e2e convert/template + document CRUD + downloads.
- **M2:** smoke 22/22 (QA HTML, PDF normal + practice).
- **M3:** smoke 36/36 (fences, validation, splitQuestions, structuring, prompts, copy text, titleFromHtml).
- **M4:** smoke 24/24 (hashVersion, seeding, save→history→cache, TOKENS validation, reset, snapshots, resolveConversionInstructions); build green 17 routes.
- **M5:** full implementation + build green; Mongo/Blob typechecks; M5 smoke 32/33 after the hang fix.
- **M6:** `npx tsc --noEmit` clean (only the stale `.next/types/validator.ts` regenerated error — filtered, rebuilds on `next build`); anti-pattern greps clean (`canDownloadPdf`, `previewStale`, `ConvertMode`, `convert/template`, `PreviewPane`, `showPreview`, `?id=` links, `practice=true` — all gone from source; `USER_ANSWER` only in `lib/prompt.ts` comments as intended). Build + smoke suite + test compile pending (verification task).
- **M6 bug-fix pass (2026-08-10, user-reported):** preview 400 fixed (route unwraps `{ doc }`); practice answers autosave (quiet debounced persist); practice shows ALL blocks and paragraphs get "My answer" boxes; PDF/html render paragraph practice answers (`my-answers` variant includes paragraphs). Verified: `next build` green (18 routes); dev-server curl — `/api/preview` 200 for both `{ doc }` and bare shapes, paragraph answer in HTML; all three PDF variants 200 with correct content (text extracted via ToUnicode/cmap decode — full: para answer + translation + qa answer + model answer; questions: title + question only; my-answers: para + answers, no references); smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (pre-existing deferred vocab-row check).
- **M6 design-alignment pass (2026-08-10, same day):** instructions rewritten to the user's print-ready spec (Part 2 verbatim) + conversion protocol (Part 1); `vocabBg`/`rowBorder` tokens added through the whole chain (instructions → parseTokensBlock → DesignTokens → DEFAULT_TOKENS → both renderers); globals.css palette remapped to the spec ramp; PracticeParagraphCard → continuous flow (no card, no duplicated boxes); preview renders printMode (A4 + print font/margins + own shadow, transparent iframe); View-dropdown actions now set status messages; enrichment chips + "Add row" + add-block hover muted to subtle emerald accents; unused RowEditor import removed from Block.tsx. Verified: `next build` green; `/api/preview` 200 wrapped + bare with printMode CSS (210mm / 10.5px / 14mm / #1e3a5f / #eef2f7), paragraph userAnswer dashed box present; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check); no leftover old chip/iframe/blue-hover patterns (grep-verified). **User must click "Reset to repo file" once on /instructions** (active.md still holds the pre-rewrite content).
- **Design pass 2 (2026-08-10, user feedback):** essay block type end-to-end (types/schema/structuring/prompt/template/PDF/parse-back/editor/copy) — practice renders ONE continuous passage with ONE writing field, AI conversion groups essays with ONE enrichment set; practice structure frozen (no add, no per-block controls/tags/heading select, drag disabled — AddBlockMenu `!practiceMode`-guarded); split add control (`+ <last>` + ▾ menu, `writer-app:add-type` in localStorage); super-subtle chips (`opacity-0 group-hover:opacity-100` in QaBlockForm + ParagraphFields); Enter = newline for paragraph/essay only (`block.type !== "paragraph"` guard); instructions Part 1 essay rules added (Part 2 verified verbatim vs the user's re-pasted spec, incl. vocabBg/rowBorder tokens). Verified: `next build` green; `/api/preview` 200 with `block-essay` wrapper, 3 `.block-paragraph`, ONE `p-translation`, vocab grid, dashed `qa-user-answer`; all three PDF variants 200 (full 4122B — passage + userAnswer + translation + analysis + vocab + expressions via text extraction; my-answers 2416B — passage + userAnswer, NO references; questions 2863B — passage + blank ruled area, essay now included on both non-full sheets); smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same pre-existing deferred vocab-row check); grep-verified: essay in every block-type switch, no old blue hover accents, Enter guard present, chips hover-reveal, practice guards present. **User must click "Reset to repo file" once on /instructions** (active.md now ALSO stale after the Part 1 essay-rules rewrite).
- **Design pass 2 round 2 (2026-08-10, user feedback):** French-first practice typography (passage 17px / answers 16px / English reference 13px secondary — "this is for practicing french not english") + convert() no longer loses answers: paragraph/essay `userAnswer`s carry over by text match, merging per-paragraph answers when the AI groups paragraphs into one essay. Verified: tsc clean, build green; carry-over logic check-run across 5 scenarios (paragraphs→essay merge, essay→essay, paragraph→paragraph, qa→qa with flags, essay→paragraphs no-carry limitation); smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check).
- **Design pass 2 round 3 (2026-08-10, user feedback):** "the text for french is still small in preview" — the preview/output bug was `rem`-based secondary sizes (see `buildCss`): English translations were 0.9rem = 14.4px while French paragraphs were the 10.5px print base. Fixed with `em` everywhere + `.block-paragraph` at 1.15em; PDF paragraphs mirror at `basePt * 1.15`; instructions prose + TOKENS `small` updated to `em` (prose↔CSS in sync). Verified: live `/api/preview` CSS check (1.15em paragraph, 0.9em translation, zero `font-size` rem left), PDFs regenerate 200 with full content, tsc clean, build green, smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check). Note: active.md staleness does NOT affect these sizes (they're compiled into the template, not token-driven) — the reseed only matters for the essay rules.
- **Design pass 2 round 4 (2026-08-10, user pasted the original Print-Ready spec):** "i need the preview to be same size and other things described in this" — the preview was showing the PRINT rendering (10.5px base, 14mm margins) while the spec describes base 11.5px / 18mm margins as the on-screen design (10.5px/14mm only under `@media print`). `buildCss` paper rules now render the spec's SCREEN design: `body` keeps the base 11.5px (the `body { font-size: print }` override was removed) and `.document` pads with `pageMargin` 18mm; the `@media print` blocks keep true print behavior (10.5px, `@page` 14mm, no shadow). User chose (AskUserQuestion): French prose keeps its 1.15em lead → ~13.2px; English translation 0.9em → ~10.4px secondary. Verified: live `/api/preview` CSS (body 11.5px, `.document` padding 18mm ×2, `p.block-paragraph` 1.15em, the ONLY 10.5px occurrence inside `@media print`), PDFs regenerate 200 with full content (4154/2465/2873B), tsc clean, build green, smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check). **User must click "Reset to repo file" once on /instructions** (active.md still holds pre-essay-rules content).
- **M6 round 2 (2026-08-10, user feature batch — "AI should never change answers, only suggest"):** AI conversion now returns per-qa **corrections** (spelling/grammar/punctuation, French accents + spacing) that the user applies or dismisses one by one; PDF pages are footed `1/7` style; every multi-line text field auto-grows. Verified: `npx tsc --noEmit` clean; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check), **M6 24/24**; `next build` green (18 routes); **PDF footers verified end-to-end via TJ hex-run decode of inflated content streams** — full: 1/2, 2/2; questions: 1/3, 2/3, 3/3; my-answers: 1/3, 2/3, 3/3 (server route + standalone renderer); **live-server e2e** — POST /api/documents accepts suggestions (201), GET round-trips them, /api/preview 200 with ZERO suggestion strings leaked into HTML (question text renders), PDF route 200 with footer; anti-pattern greps clean (no leftover local autoGrow in Block.tsx, no fixed-row textareas outside AutoGrowTextarea, no "suggestions" in html-template/pdf/CopyDialog/html-to-blocks — no leak paths); dev server log clean. **User must click "Reset to repo file" once on /instructions** (active.md still holds the pre-CORRECTIONS-rules content).
- **M7 (2026-08-10, user feature batch):** focus mode toggle (only main content — question+answer, paragraph/essay text; enrichment hidden); Q&A field order question → translation → analysis → answer unified across editor, HTML template and PDF; Copy dialog exactly 2 options (For sharing + For AI — buildAICopyText round trip pasted back via Paste blocks, no AI call in-app); copyable instructions above the JSON + HTML paste textareas (InstructionCopyBox; Paste questions unchanged per user); home = library (Library single nav item, `/library` → `/`, Writer brand links home); preview field toggles (per-field + "All extras" master; headings/questions/paragraph text never hidden; /api/preview accepts `{ doc, hidden }`); InstructionsEditor crash fix (4 `entry.content` sites guarded). Verified: `npx tsc --noEmit` clean; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check), M6 24/24, **M7 17/17**; `next build` green (18 routes); **live-server e2e** — POST /api/preview with `hidden` toggles → 200, main content survives, hidden fields absent, model answer visible when not hidden; anti-pattern greps clean (no leftover Home nav item, focusMode threaded everywhere, no unguarded `entry.content`); dev server log clean. **User must click "Reset to repo file" once on /instructions** (active.md still holds the pre-CORRECTIONS-rules content — unaffected by M7, which changed no instructions).
- **M7 round 2 (2026-08-10, user feedback batch):** focus mode is **DEFAULT ON** (`useState(true)`); **essay `heading`** — optional field through the whole chain (types/schemas/structuring tolerant parse/prompt `<HEADING>` marker/HTML template `.block-essay-heading`/PDF `essayHeading`/parse-back/editor), practice mode shows ONLY the heading as the writing prompt in editor + PDF non-full variants, instructions rule "only when the passage has a natural title — never invent, never force"; **Copy ▾ dropdown → single `Copy…` button** (FR-39 items + `onCopyPrompt` + Editor `copyPrompt`/`copyToClipboard` + `/api/export/prompt` route deleted — instructions copies live in the paste sections); **paste textareas scrollable** (`AutoGrowTextarea` base now `resize-none overflow-y-auto max-h-[45vh]` — long pastes scroll inside the modal); **preview toggles fixed** ("All extras" `checked={!anyHidden}` — was inverted, checked meant everything hidden; `openPreview(hiddenOverride?)` passes the NEW hidden values straight to the POST, fixing the stale-closure first-click bug); **response label moved ABOVE the answer** in template (`${responseLabel}${modelAnswer}`) and PDF (QABlockPDF), removing the old bottom position. Verified: `npx tsc --noEmit` clean; smoke M2 22/22, M3 37/37, M4 25/25, M5 32/33 (same single pre-existing deferred check), M6 24/24, **M7 19/19** (essay `<HEADING>` marker + `<h3 class="block-essay-heading">` template render + "Ma ville" survives hidden + hidden-main-survives extended); `next build` green (18 routes); anti-pattern greps clean (no leftover `onCopyPrompt`/`copyPrompt`/`export/prompt`/`/api/export` references, no `allHidden`-semantics leftovers). **User must click "Reset to repo file" once on /instructions** (active.md now also stale for the essay-heading rule — same as the earlier reseeds).
