# Suggestions Log

## 🐛 Known Bugs

- **2026-08-10 — M6 first-run bugs (ALL FIXED):** (1) Preview 400 "Invalid document payload" — `/api/preview` validated the raw body while the editor sends `{ doc }`; now unwraps `payload.doc ?? body` like `/pdf` and `/convert/ai`. (2) Practice answers weren't saved — M6 removed the localStorage draft autosave; added a quiet debounced (~1.2s) server autosave in the editor (`persist()` split out of `save()`, `savingRef` + `busyRef` guards, dirty flag cleared only when the saved snapshot is still current). (3) Practice hid paragraphs and offered no writing surface for them — practice now shows EVERY block; paragraphs get a `PracticeParagraphCard` (read-only text + "My answer" box; Check → green "Reference" translation box); `ParagraphContent.userAnswer` added; PDF `my-answers` variant includes paragraphs (blank rules when unanswered), `full` renders paragraph answers, HTML renders the dashed answer box; `Reset practice` clears qa AND paragraph answers.
- **2026-08-10 — HTML→blocks parse-back infinite loop (FIXED):** `lib/html-to-blocks.ts` `byClass`/`elementsByClass` descended into `#text` nodes: `collectTopLevel(text)` re-yields the same `#text` node, which was pushed back onto the search stack → infinite loop at 98% CPU. Killed the editor's "Parse to blocks" button (FR-41) on any document containing text and hung `tests/smoke-m5.ts`. Fixed 2026-08-10 with a `if (el.tag === "#text") continue;` guard in both functions.
- **2026-08-10 — Parse-back vocab row check failing (1/33, PENDING):** `tests/smoke-m5.ts` "parse: vocab grid rows" fails — `q1.vocab[1]` (term "le cinéma") doesn't match after round-trip, though "round trip: vocab survives two passes" passes and expressions parse fine. Suspect row ordering or term/def attribution inside `parseRows`/vocab-col splitting. Diagnosis deferred per session note — testing skipped for now.

## 🟢 Improvements

- **2026-08-10 — Copy-for-sharing should include paragraph practice answers:** `CopyDialog`'s "User answers" toggle covers qa blocks only (`buildCopyText` iterates qa). Paragraphs now carry `userAnswer` (M6 fix); the copy dialog should offer paragraphs' answers too when that selection is on.
- **2026-08-10 — Commit the PDF text extractor to tests/:** verification of PDF content currently needs a throwaway Python script (react-pdf stores text as hex-string TJ arrays in WinAnsi, subset fonts — `pdftotext` unavailable on macOS, `strings` useless). A committed `tests/pdf-extract.py` (used during the M6 bug-fix pass) would let smoke tests assert PDF text content per variant instead of eyeballing stream sizes.
- **2026-08-09 — Unify page margins:** `html2pdf/server.js` forces 20px margins, while `html_instructions.md` specifies `@page { margin: 14mm }`. The print CSS should be the single source of truth (the writer app already does this via the instructions file).
- **2026-08-09 — Validate HTML size server-side:** `server.js` limits JSON to 10MB but doesn't cap HTML length; add a sane length cap and strip `<script>` tags from user-pasted HTML in the preview iframe (currently `sandbox="allow-scripts"` permits scripts).
- **2026-08-09 — Golden-file check between HTML preview and PDF:** since HTML and PDF are now two renderers over the same block data + shared design tokens (FR-43 in the writer app), add a visual regression test comparing the template-rendered HTML against the react-pdf output for a sample document, so token drift is caught automatically instead of by eye.
- **2026-08-09 — Testable block markers in generated HTML:** the M1 e2e "library shows block count" check failed as a grep artifact (React SSR inserts `<!-- -->` comment nodes between text children). Give the template generator stable `data-*` hooks (e.g. `data-block-type="paragraph"` per section) so tests and future HTML→blocks parse-back (FR-41) can locate blocks structurally instead of string-matching rendered text.
- **2026-08-09 — "Draft restored" indicator:** the editor silently restores a localStorage draft on load (FR-24). Surface a small toast/chip ("Restored draft from <time>") so the user knows stale content was recovered, with an explicit "discard draft" affordance. **Moot since 2026-08-10 (M6):** the localStorage draft autosave was removed — the editor always routes through `/doc/<id>`, and unsaved work is lost on navigation by design.
- **2026-08-09 — Embed a print-matched serif font in the PDF:** PDF uses Times-Roman while HTML print uses Georgia (per tokens). Intended for M1, but embedding Georgia (or a metric-compatible open serif) into the react-pdf bundle would make PDF and print preview pixel-identical — a natural M5 polish candidate.
- **2026-08-10 — react-pdf has no `break-inside: avoid`:** react-pdf v4.6 exposes no `pageBreakInside`/`breakInside` style (only `break` = page-break-before and `minPresenceAhead`). QA cards use `minPresenceAhead={150}` as a keep-together approximation; a long card can still split across pages. Candidate future fix: a per-card `break` toggle in the editor for cards the user wants on their own page.
- **2026-08-10 — "Structure with AI" preview step:** FR-38's preview shows only the detected question count; AI-structured blocks land directly in the editor and replace/append. A staged preview (structured blocks → confirm → apply) would let users discard bad structuring without undoing edits — parseStructuredQaResponse already returns blocks, so this is a small UI addition.
- **2026-08-10 — Remember the last convert goal per document:** the Convert goal input (FR-29) resets every session. Persist it with the draft (localStorage) so themed sessions ("today: passé composé") survive reloads.
- **2026-08-10 — Copy dialog Markdown option:** FR-50 outputs clean plain text; a "Copy as Markdown" variant (headings as `##`, vocab as bullet lists, bold field labels) would paste nicer into notes/chat apps — buildCopyText is already the pure seam for it.
- **2026-08-10 — One-time reseed needed after the M6 instructions rewrite:** `docs/html_instructions.md` was rewritten from an HTML-design spec to structuring & enrichment instructions (question detection, keep provided answers, never invent, French→English enrichment for qa AND paragraphs), but existing installs keep the OLD content in `data/instructions/active.md` (seeded in M4). Click "Reset to repo file" once on `/instructions` so conversions use the new rules. The TOKENS block was deliberately kept byte-identical, so design tokens are unchanged (verified with `git show`).
- **2026-08-10 — Practice answers survive conversion by question-text matching:** M6 carries `userAnswer`/hide flags to re-structured blocks by matching trimmed question text. A question that changes wording loses its practice answer (by design — it's a different question); document that behavior in the instructions file if it ever confuses.

## 🟡 New Features

- **2026-08-10 — Practice-area review option (DEFERRED by the user, M7 round 4):** "in future we will add an option for the review of the practice area also but not for now". When built, it flips the existing `showUserAnswers` seam: the preview/display would render the user's practice answers (and, for paragraphs/essays, real reference material — e.g. an AI model paragraph — instead of the removed English translation box) for checking. Everything is already plumbed: the HTML template opts in via `showUserAnswers: true`, the PDF via the `my-answers` variant, and `serializeBlocksForAI` deliberately never serializes userAnswer (privacy preserved).
- **2026-08-09 — Offline "Template mode"** in the writer app (already specced, FR-9): build styled HTML locally from block data so conversion works with no API key and zero cost; AI mode stays as an upgrade. **Dropped 2026-08-10 (M6 redesign):** conversion is AI-only — the AI returns editable structured blocks (one conversion, local edits re-render); `lib/html-template.ts` still renders previews/downloads from block data, it just never runs without the AI.
- **2026-08-09 — Instructions versioning + per-document snapshots** (FR-21–23): editing `html_instructions.md` never breaks old documents; each document remembers which rules it was generated with. **✓ Implemented 2026-08-10 (M4):** `/instructions` editor with save/reset/history, `data/instructions/history/*.md`, per-document `instructions.snapshot.md` + "convert with snapshot rules" toggle (FR-23), TOKENS validation on save + token-cache invalidation (FR-47), version in the status bar (FR-28).
- **2026-08-10 — Diff view between instruction versions:** the history list shows version/date/char-count but not *what changed*. A `git diff`-style view (old vs new content) before Restore/Preview would make the "why did my PDF change" question answerable at a glance — the history files are already plain markdown on disk.
- **2026-08-09 — Batch export:** convert the whole `data/documents/` library (or a tagged subset) to PDFs in one action — a server-side loop over the react-pdf renderer (`lib/pdf.ts`).
- **2026-08-09 — Git-friendly document storage:** keep `document.json` files in a git repo (with `data/documents/` otherwise gitignored) so every edit is diffable and restorable; plus a one-click "backup all documents" zip.
- **2026-08-09 — Per-session focus goal:** a small "goal" field (e.g. "today: passé composé") appended to the AI prompt so practice sessions follow a theme without editing the instructions file. **✓ Implemented 2026-08-10 (M3):** goal input in the Convert dropdown (FR-29), passed as `GOAL: …` in `lib/prompt.ts`.
- **2026-08-09 — Per-block AI convert:** alongside whole-document conversion, allow "convert just this block" so a single paragraph can be re-polished without re-running the full document through DeepSeek.
- **2026-08-10 — Test generator (PLANNED, not built — the M6 seam):** select 3–4 documents → AI builds a test from TOPICS + QUESTIONS only (paragraphs, model answers, and practice answers are never sent). Design: `POST /api/test/generate` with `{ docIds: string[] }`; the server reads each doc's blocks, collects title/headings (topics) + qa questions, sends only those to DeepSeek with the active instructions as system prompt, and returns a test as structured blocks (heading per topic + qa blocks with empty `modelAnswer` — the AI invents nothing). UI: checkboxes on the dashboard + "Generate test" → opens the result in `/doc/<new-id>` for editing. All seams already exist (`lib/ai.ts`, `lib/structuring.ts`, the block model, snapshot resolution).
- **2026-08-10 — Practice-sheet PDF variants (✓ implemented M6):** the three-variant PDF flow the user described — "Questions only (share)" (blank shareable sheet) and "Questions + my answers" (send your written answers for checking) — plus the full document. `PDFVariant = "full" | "questions" | "my-answers"` in `lib/pdf.tsx`, rendered on demand from the current doc (`POST /api/documents/[id]/pdf`).

## 🔴 Vulnerabilities

- **2026-08-09 — No auth on the Express app:** `html2pdf` runs a listening server with no auth; fine for localhost but should bind to `127.0.0.1` explicitly or require auth if ever exposed. The writer app (per requirements) stays local-first, single-user.
- **2026-08-09 — AI output injected into iframe preview:** generated HTML from DeepSeek will be rendered in the preview iframe — keep the sandbox attribute and strip executable content before saving/printing.

## 🟢 Improvements

- **2026-08-10 — Print-accurate preview (DONE):** the preview route now renders `generateTemplateHTML(doc, tokens, { printMode: true })` — A4 sheet (210mm), print font size (10.5px), print margins (14mm), and the paper's own white + shadow inside a transparent iframe, so what you see in Preview is exactly what the PDF prints. `buildCss(tokens, paper)` carries the paper rules; `@media print` resets margin/shadow so printing from the preview is still a clean A4.
- **2026-08-10 — Editor UI remapped to the PDF design palette (DONE):** `app/globals.css` now remaps the Tailwind palette to the instructions spec ramp — zinc-* = grey ramp (#f7f9fb…#1a2738), blue-* = navy (#eef2f7…#0d1a2e, blue-600 #1e3a5f), emerald-* = green (#e8f0e9…#1a3a1c, emerald-600 #2c5f2d) — so the editor, preview, and PDF share one colour scheme (user's request: "make the ui for now in same colour scheme as we are making pdf").
- **2026-08-10 — AI enrichment fields made subtler (DONE):** paragraph "＋ chips" now read "AI details:" and both Q&A and paragraph add-chips are muted (`border-dashed border-zinc-200 bg-transparent text-[10px] text-zinc-400`, emerald hover) — enrichment is mainly AI-generated, so manual-entry affordances sit quietly out of the way. "+ Add row" and the add-block "+" hover were matched to the same emerald accent.
- **2026-08-10 — Practice paragraphs are continuous (DONE):** `PracticeParagraphCard` no longer boxes the paragraph into "Paragraph" label + white text box + separate answer box — the text renders in normal document typography with the dashed "My answer" textarea flowing directly beneath (a paragraph is one continuous thing), and Check reveals the green Reference box under the answer.
- **2026-08-10 — View dropdown now gives feedback (DONE):** "Hide/Show all translations" and "Hide/Show all answers" write per-question flags + document defaults but previously gave zero visible response in the editor (the flags only affect PDF/preview/HTML output). `setAllQaFlags` now sets the status bar message ("All question translations hidden — PDF and preview will omit them" / visible again). Note for the user: the View items intentionally DON'T hide things in the editor itself — they control what the printed/previewed output contains; individual 👁/🙈 toggles on each Q&A card do the same per-question.
- **2026-08-10 — Design-spec clash resolved (instructions):** the user's pasted print spec ("Print-Ready HTML Style Instructions") described AI-generated HTML; the app's AI never produces HTML — it returns structured blocks that the app renders through `lib/html-template.ts` / `lib/pdf.tsx`. Resolution: the spec is now instructions Part 2 (PRINT-READY DESIGN SYSTEM), documented as describing what the app's renderers implement, and Part 1 (CONVERSION PROTOCOL) describes the actual AI output contract. Two new tokens were needed to match the spec exactly — `vocabBg: #eef2f7` and `rowBorder: #d8dfe8` — wired through parseTokensBlock → DesignTokens → DEFAULT_TOKENS → both renderers. **One-time reseed needed:** existing `data/instructions/active.md` holds the OLD instructions — click "Reset to repo file" once on /instructions.
- **2026-08-10 — Essay block type (DONE, design pass 2):** user feedback: "paragraphs are getting problems as when a essay has 2-3 paragraphs it is making 2-3 parts for translation vocab etc for each para" + "it should just give 1 field to write para". New `essay` block: ONE block holds ALL paragraphs of a passage in `paragraphs: string[]`, with ONE shared translation/analysis/vocab/expressions set and ONE practice answer field. Wired end-to-end: types/schemas, AI conversion (structuring parser + prompt rule + `<ESSAY>` marker), HTML + PDF renderers (continuous passage), HTML parse-back, editor (`PracticeEssayCard` — the whole passage + a single "My answer" textarea), copy-for-sharing, and the questions PDF variant (essays appear on BOTH non-full sheets — they're a reading + writing task). One consequence for users of older content: a document converted BEFORE this change still has per-paragraph blocks — re-convert it to group the paragraphs into an essay.
- **2026-08-10 — Practice mode structure is frozen (DONE):** user feedback: "in practice why can i add paragraphs etc?" Practice is for answering, not editing — the bottom add menu, per-block ＋/↑/↓/✕ controls, tags input, heading-level select, slash menu, and drag-reorder are all hidden/disabled in practice. Document structure can only change in normal mode.
- **2026-08-10 — Split add-block control (DONE):** user feedback: "should have a dropdown and a default choice para and later the last chosen option if directly clicked not on dropdown". The "+" is now `+ <last-chosen-type>` — a direct click adds it instantly (default: Paragraph); the ▾ caret opens the full menu, and picking an item there adds it AND becomes the new default. Last choice persists in localStorage (`writer-app:add-type`); the per-block ＋ adds-below also uses it.
- **2026-08-10 — Enrichment chips super-subtle (DONE):** user feedback: "adding options for translation vocab etc is not subtle, it is making it look cluttered, make it super subtle as 99% time it will be made by ai". The add-chips are now invisible until the block is hovered/focused — `opacity-0 transition-opacity group-hover:opacity-100 group-focus-within:opacity-100` — so a normal AI-generated worksheet shows a clean card with zero chip clutter.
- **2026-08-10 — Enter = newline in paragraphs (DONE):** user feedback: "when writing in paragraph field in normal mode and press enter it is making new paragraph field why it should not do this". Enter no longer splits paragraph blocks (the FR-3 split remains for qa/heading); paragraphs/essays are prose — Enter inserts a newline, Shift+Enter behaves the same.
- **2026-08-10 — Instructions reseed (still pending):** active.md now holds pre-CORRECTIONS-rules content (the M6 round 2 rewrite added the CORRECTIONS section — spelling/grammar/punctuation suggestions for qa blocks — plus the TYPOGRAPHY RULE and the "never auto-rewrite" bullet). Click "Reset to repo file" once on /instructions to pick up the essay rules (Part 1), the CORRECTIONS protocol, and the verbatim design spec (Part 2). Documents created with the snapshot toggle (FR-23) keep old rules until re-converted.
- **2026-08-10 — French-first practice typography (DONE):** user feedback: "the french text in paragraphs is small... english is bigger... this is for practicing french not english". Practice paragraphs/essays now lead with the French passage at 17px (darker zinc-900), the "My answer" textarea at 16px (both are the French material), and the English Reference box drops to 13px zinc-600 — clearly the secondary element. Q&A practice untouched (user: "the functionality of q/a is good").
- **2026-08-10 — AI convert no longer loses practice answers (DONE):** user feedback: "when i write an answer and then convert with ai i am losing my answers why?". The carry-over only matched qa question text — paragraph and essay answers were silently dropped on re-convert. Now: paragraphs match by exact text, essays by joined paragraphs, and when the AI groups previously-separate paragraphs into ONE essay (the new essay rule), their answers merge in order into the essay's single answer field. Edge case (documented): if the AI ever splits an essay into separate paragraphs, answers can't be attributed — the instructions forbid that split, so this shouldn't occur.
- **2026-08-10 — Preview/output French-first sizing (DONE):** user feedback: "the text for french is still small in preview". Root cause found: the template's secondary sizes used `rem`, which resolves against the 16px browser root — so the English translation rendered at 0.9rem = 14.4px while French paragraphs sat at the 10.5px print base. The spec's "0.9rem" intended "relative to the base font" but rem did the opposite. Fixed: all secondary sizes are now `em` (0.9em translation / 0.88em analysis / 0.82em grammar / 0.78em labels — clearly smaller than the French), and `.block-paragraph` prose leads at 1.15em (≈12.1px in print; PDF mirrors at 1.15× base). French is now the biggest regular text on the sheet; English is visibly secondary.
- **2026-08-10 — Preview renders the spec's SCREEN size (DONE, design pass 2 round 4):** user pasted the original "Print-Ready HTML Style Instructions" and asked "i need the preview to be same size and other things described in this". The preview was showing the PRINT rendering (base 10.5px, 14mm margins), but the spec's screen design is base 11.5px with 18mm margins — the 10.5px/14mm values apply only under `@media print`. `buildCss(tokens, paper)` no longer forces the print font on screen: the A4 sheet now renders at base 11.5px with 18mm page padding, and the `@media print` rules inside keep true print behavior (10.5px, `@page` 14mm, no shadow) when the sheet itself is printed. Per the user's choice, French prose keeps its 1.15em lead (~13.2px) with the English translation secondary at 0.9em (~10.4px). PDF unchanged (it was always print-true).
- **2026-08-10 — Unmatched-suggestion affordance (M6 round 2 follow-up):** a suggestion whose `original` no longer exists in the field text is silently hidden at render (the user fixed it by hand, or an Apply landed first). That's safe but opaque — when `visibleSuggestions` drops rows, show a subtle "N suggestions no longer match the text" note so users don't wonder where their list went.
- **2026-08-10 — On-demand "check my answer" (M6 round 2 follow-up):** corrections currently come only from the full Convert flow. A per-qa-card "Check" action would re-run just that block's question + answer through the AI for a fresh suggestions list (the practice-mode Check could surface suggestions too) — same `suggestions` protocol, no document re-conversion.
- **2026-08-10 — Suggestions in practice mode (M6 round 2 follow-up):** suggestions render in normal mode only by design (they describe the model answer). In practice mode, showing the corrections list after Check would give learners the same self-check benefit the AI already computes — just a render-mode condition in QaBlockForm.
- **2026-08-10 — Focus mode (DONE, M7):** user request: "add a focus mode also which user can toggle which will only show the important part like in case of questions answer that is just a question answer no other stuff and in case of paragraph only the main topic and area to write paragraph no translations no vocab or anything". Toolbar checkbox next to Practice; same plumbing pattern as practiceMode (Editor state → Toolbar → BlockList → Block → QaBlockForm focus branch / paragraph + essay enrichment gated). Q&A shows ONLY question + answer; paragraphs/essays show only the text. Follow-up idea: persist the toggle (localStorage `writer-app:focus-mode`) and/or make it a document property so PDF/preview can also render focus-only — today it's an editing-view aid.
- **2026-08-10 — Q&A field order unified (DONE, M7):** user request: "it should show question then translation then analysis then answer prt same in preview and other area too". Editor, HTML template and PDF now all render question → question translation → analysis → model answer → answer translation (→ user answer → grammar note → response label → vocab grid). If the order ever feels wrong for a new field, there are now THREE places to keep in sync (QaBlockForm JSX, `qaBlockHtml`, `QABlockPDF`) — a future refactor could render both outputs from one field-order list.
- **2026-08-10 — Copy dialog: exactly 2 options (DONE, M7):** user request: "copy should give option for copy everything in the document in plain text form... then copy ai instructions... i think this option and the ai instructions should be fine only these 2". Tabs: **For sharing** (selective plain-text with toggles) + **For AI** (one copy = instruction + `=== CONTENT ===` + serialized blocks; the other AI's JSON response pastes back via Paste blocks (AI) — no AI call in the app). Follow-up: when Paste blocks detects an obvious "I gave you this instruction" drift (the pasted response references a different instruction), offer a confirm — probably overkill, the content parse is self-validating.
- **2026-08-10 — Copyable instructions above paste boxes (DONE, M7):** user request: "make sure to provide the copy instructions for each in above the text box which user can use to provide other ai the context in which it need to parse the data". `InstructionCopyBox` above the JSON-blocks paste textarea (BLOCKS_INSTRUCTION) and the HTML paste textarea (HTML_INSTRUCTION); Paste questions stays simple (user likes it as-is).
- **2026-08-10 — Home = library (DONE, M7):** user request: "home and library takes to same page... also make so when we click on writer text or icon it takes us to home page". `/library` already redirected to `/`; the editor toolbar now has ONE nav item (Library → `/`) and the Writer brand icon+wordmark is a link home. The toolbar's old "Home" item was removed to avoid two identical destinations.
- **2026-08-10 — Preview field toggles (DONE, M7):** user request: "in preview for para and q/a there should be option for all fields to be toggle view off so that user can select individual vise and overall wise on what to take in preview or not... headings and questions and para title can be exempted as it is main". Preview sheet header: "Show:" box with **All extras** master checkbox + per-field checkboxes (Translations / Analyses / Vocab & expressions / Model answers); toggling re-renders immediately (`/api/preview` accepts `{ doc, hidden }`). Headings, questions and paragraph text are never hidden. Follow-ups: (a) the hidden state currently lives only in the editor — persist per document (`writer-app:preview-hidden:<docId>`) so it survives reloads; (b) PDF doesn't honor the toggles yet (user asked for preview only) — a PDF variant "focus/full/extras-off" would reuse the same `hidden` object.
- **2026-08-10 — InstructionsEditor history crash fixed (DONE, M7):** user crash report "Cannot read properties of undefined (reading 'length') components/InstructionsEditor.tsx (211:64)" — a history entry whose `content` is undefined blew up the char-count render. All four `entry.content` sites now guard `?? ""` (Preview ×2, Restore payload, char count). Root cause hypothesis: an old history file written before the content field existed, or a truncated write; the guard makes every site immune regardless.
- **2026-08-10 — Focus mode default ON (DONE, M7 round 2):** user request: "focus mode should be default". `useState(true)`. Follow-up (from the M7 entry): persist the toggle in localStorage (`writer-app:focus-mode`) so the default isn't lost on every reload — cheap win since the toolbar checkbox already centralizes the value.
- **2026-08-10 — Optional essay heading (DONE, M7 round 2):** user request: "in practice the paragraph is still showing the essay written see it should only show the heading for the essay... update instruction so that whenever possible if not available essay should have heading not compulsary only if possible not forcefully". The essay block now has an optional `heading` field: the AI adds one only when the passage has a natural title (never invented, never forced — instruction + prompt + paste-blocks instruction all updated); the editor shows the heading input above the paragraphs; practice mode shows ONLY the heading as the writing prompt (the passage is the task — hidden in practice editor AND in the PDF questions/my-answers variants), with a muted "(essay title)" placeholder when there is none. Follow-up: documents converted before this change have no heading — re-convert to let the AI supply one where natural (same re-convert precedent as the essay grouping).
- **2026-08-10 — Copy option reduced to ONE (DONE, M7 round 2):** user request: "still in copy option why are there 4 options... there should only be one option that is the copy for sharing or you can just name it as copy as there is only one option as the instructions for we already moved to the paste sections". The toolbar Copy ▾ dropdown (4 items incl. the three FR-39 instruction copies) is now a single **`Copy…`** button opening the sharing/AI dialog; `onCopyPrompt` + Editor `copyPrompt`/`copyToClipboard` + the `/api/export/prompt` route are deleted. Instruction copies remain one click away inside the paste modals (InstructionCopyBox), per the user's own framing.
- **2026-08-10 — Paste boxes scroll instead of overflowing (DONE, M7 round 2):** user request: "when the data is entered in all the options in paste the text box goes out of limit check and fix that... for it should become scrollable". `AutoGrowTextarea`'s base class now includes `resize-none overflow-y-auto max-h-[45vh]` — long pastes grow up to ~half the viewport and scroll from there instead of pushing the modal out of the window. The auto-grow effect still runs (it just caps the growth).
- **2026-08-10 — Preview "All extras" semantics fixed (DONE, M7 round 2):** user report: "the selection of thing in preview is not working properly answer is shown but i have to unclick and click all extras in starting nothing is showing even though all extras is clicked and everything". Two bugs: (1) the master checkbox was `checked={allHidden}` — INVERTED: when everything was hidden the box was CHECKED ("all extras" ticked yet nothing shown); now `checked={!anyHidden}` — ticked = all extras visible. (2) stale closure: `openPreview()` POSTed the OLD hidden values captured at render, so the first toggle click never re-rendered the preview; `openPreview(hiddenOverride?)` now passes the new values straight into the POST.
- **2026-08-10 — Response label above the answer (DONE, M7 round 2):** user report: "reponse text is shown below the answer why? fix it". The "RÉPONSE" label rendered BELOW the model answer in the HTML template and PDF (it was grouped with the grammar note + vocab grid at the bottom). It labels the answer area, so it now sits directly ABOVE the model answer (`qaBlockHtml` order: analysis → responseLabel → modelAnswer → answerTranslation → userAnswer → grammarNote → grid; same in QABlockPDF).
- **2026-08-10 — Instructions reseed (still pending, now covers essay heading too):** `data/instructions/active.md` is gitignored runtime state and has been stale since the M6 round 2 rewrite. The M7 round 2 change updated the ESSAYS rule ("heading OPTIONAL — only when the passage has a natural title, never invented, never forced"), the essay output shape (`{"type":"essay","heading":"…",...}`), and added a NEVER bullet — so the reseed now also picks up the essay-heading rule. **Click "Reset to repo file" once on /instructions.**
- **2026-08-10 — Downloads moved into preview (DONE, M7 round 3):** user request: "i want the download option to be moved to preview part as there user should be able to see the exact document before downloading... just add a download the pdf Button that download the currently displayed PDF and download HTML button which will download the HTML for the currently displayed document". The toolbar's Download ▾ (3 PDF variants + HTML) is GONE; the Preview sheet header now has **Download PDF** + **Download HTML** — both render the CURRENT display (the sheet's hidden/empty-lines toggles are sent with the PDF POST; the HTML is the in-memory preview string, byte-identical). The 3-variant dropdown disappears because the toggles cover the old variants: "questions only" = hide extras + Empty lines; "questions + my answers" = hide extras (user answers are never hidden). The pdf route still accepts `variant` for GET/tests, but the UI never sends it. Follow-up: the hidden/emptyLines state currently lives only in the editor's memory — persist per document (`localStorage "writer-app:preview-options:<docId>"`) so a reload keeps the user's display choices (see the M7 preview-toggles follow-up).
- **2026-08-10 — "All extras" now selects all four options (DONE, M7 round 3):** user report: "make it so that when all extras is clicked all the other options are selected as that is what it means... it is not doing it properly right now the thing that is selected it should show that". Root cause: mixed checkbox semantics — per-field boxes were `checked={hidden[key]}` (checked = HIDDEN) while the master was `checked={!anyHidden}` (checked = VISIBLE), so the display lied about selection. Now uniform: every box `checked` = SHOWN; `toggleAll()` sets all four to false (show all) whenever anything is hidden, else hides all — clicking "All extras" genuinely selects all four per-field options, and the master reflects the sum.
- **2026-08-10 — Empty lines toggle replaces the "questions" PDF variant (DONE, M7 round 3):** user request: "add option for empty lines if selected should show empty lines as in download only questions does... so then we wont need that option... q+a is also can be done from the toggle then we wont need that too". The preview sheet's "Empty lines" checkbox renders blank ruled writing areas where the model answer is hidden (the old "questions" variant's behavior), and unanswered essays/paragraphs get ruled space when it's on — so the variants dropdown is entirely unnecessary: questions-only = hide extras + empty lines; questions + my answers = hide extras. Wire shape: `/api/preview` and `/api/documents/[id]/pdf` both accept top-level `{ doc, hidden, emptyLines }` (shared `hiddenOptionsSchema`). Follow-up: the PDF route's `variant` param is now UI-dead — could be removed once GET/tests stop using it, or kept as a server-side convenience.
- **2026-08-10 — Preview Refresh removed (DONE, M7 round 3):** user request: "we dont need refresh button in preview". Deleted — it was redundant (toggles re-render automatically; the editor posts the current doc on open).
- **2026-08-10 — Paste modals scroll as a whole card (DONE, M7 round 3):** user report: "still the paste blocks in the paste option are going out of screen even when the field is scrollable... fix that the whole block also becomes if it starts to go out out of display". The scrollable textarea cap wasn't enough — the instruction box + textarea + footer buttons could still exceed the viewport. Both paste modals now scroll as ONE card: `max-h-[88vh] w-full max-w-xl overflow-y-auto` on the panel (same for PasteBlocksModal + PasteHtmlModal).
- **2026-08-10 — Instruction text collapsible in paste modals (DONE, M7 round 3):** user request: "hide the instructions... give a toggle to see Instructions as user will mainly use the copy button". `InstructionCopyBox` now renders ONLY the header row by default — the "Show/Hide" toggle reveals the instruction text; the **Copy instruction** button is always visible (the main use). Saves vertical space in the modals.
- **2026-08-10 — Preview toggles: glitch fix verification note (M7 round 3):** the toggle race (rapid clicks posting overlapping renders, out-of-order responses applying stale state) is now guarded by a sequence ref (`previewSeqRef`) in `openPreview` — only the latest response applies, busy clears only for the latest. Live e2e confirmed the display options path end-to-end: hidden modelAnswers+translations + emptyLines → 1 blank ruled area (essay, no user answer), model answer/translation absent, question/user answer/passage present; PDF route returns 200 with the same options (display-mode PDF larger than full — blank lines add content).
- **2026-08-10 — View dropdown removed (DONE, M7 round 4):** user request: "we dont need view option as for the preview everything is in there so remove the view options". The View ▾ (Hide/Show all translations + answers; Reset practice in practice mode) is GONE — the preview sheet's per-field toggles cover hide/show at render time without mutating block data. `VisibilityCounts`/`setAllQaFlags`/the 4 bulk-flag props deleted; `counts` survives only for the status bar; "Reset practice" survives as a quiet inline link in practice mode.
- **2026-08-10 — Practice answers never appear in the preview (DONE, M7 round 4):** user request: "it is showing practice answers why, it should not do this....same for para it is showing the practice answers there dont need that". The HTML template now gates userAnswer behind `showUserAnswers` (default OFF — the preview never sends it), and the PDF shows practice answers only on the my-answers variant (the display PDF the UI posts matches the preview byte-for-byte). The user explicitly deferred a practice-review feature: "in future we will add an option for the review of the practice area also but not for now" — the flag is the future seam (see New Features).
- **2026-08-10 — Paragraph/essay Check answers no longer reveal the translation (DONE, M7 round 4):** user request: "when I check the answer in paragraph it is showing me the translation fix it". The green "Reference" English box is removed from both practice cards — paragraphs/essays have no model answer, so the translation was the wrong reference for French-first practice. Checked = emerald tint on the answer box only. Follow-up idea: the future practice-review feature could add REAL reference material (e.g. an AI-written model paragraph) instead of the translation.
- **2026-08-10 — Focus checkbox inverted into an UNCHECKED "Detailed" toggle (DONE, M7 round 4):** user request: "The focus mode is a part one should not show focus on top and have it selected… It should show the other way around like the detailed or something that fits the theme". The default state (focus mode) now needs no checked box — the checkbox is labeled "Detailed", unchecked by default; checking reveals translations/analysis/vocab. Prop renamed `focusMode` → `detailed` (Editor → Toolbar → BlockList → Block → QaBlockForm, branches inverted).
- **2026-08-10 — Toolbar UI tidy + nav moves (DONE, M7 round 4):** user requests: "fix the ui of the bar a little bit it is not good", "instructions tab is in the docuemnt why we dont need it there..put it on home screen", "dont need button for library as it is basically home and can be go there using the writer icon". Actions now sit in three border-divided groups (Convert+Save+Preview | Practice+Detailed+Check/Reset | Copy+Paste) with a shared `TogglePill` (Practice gets the emerald active tint); Instructions is now a link on the home screen; the toolbar's Instructions/Library nav links are gone — the Writer brand icon is the way home.
- **2026-08-10 — Desktop blank space around text fields fixed (DONE, M7 round 4):** user request: "there is a lot of blank space around the text fields on laptop...on phone it is fine". Root cause: the editor column was a fixed `max-w-2xl` (672px) — huge gutters on a wide window; phone (full-width) was untouched. Widened to `max-w-4xl`.
- **2026-08-10 — Per-document preview-options persistence (follow-up to M7 round 3/4):** the preview sheet's hidden/emptyLines state lives only in editor memory — reloading loses the user's display choices. Persist per document (`localStorage "writer-app:preview-options:<docId>"`) so a reload keeps them.
- **2026-08-10 — Hide toggles removed from the Q&A card (DONE, M7 round 5):** user request: "remove the view or hidden togle from question translation and answer as it is now in preview". The 👁/🙈 `EyeToggle` is gone from the question translation and the answer — the preview sheet's per-field checkboxes cover visibility at render time. The legacy `hideTranslation`/`hideModelAnswer` flags stay in the data model (older documents keep rendering as before; the renderers still honor them), the editor just no longer edits them.
- **2026-08-10 — Question translation below the question in preview/PDF (DONE, M7 round 5):** user request: "the way of showing question translation is showing in preview is not good fix it... it is showing in same line as question it should be below it". The inline `<em>` inside `.qa-question-text` is replaced by its own `<p class="qa-question-translation">` below the question (the question row is now badge + `.qa-question-body` flex column); the PDF mirrors it with a `questionBody` View; parse-back recovers the new element with the em-in-question fallback for older exports.
- **2026-08-10 — Answer label field removed (DONE, M7 round 5):** user request: "remove answer label field as it is not going to change at all so why there is option for user to write the reponse tag". The `responseLabel` editor field (chip + input) is gone — the label is always "RÉPONSE" and the renderers keep using the stored value (default "RÉPONSE"). Follow-up idea: since the label is now fixed, the schema/AI-output shape could drop `responseLabel` entirely (it currently still travels through conversion) — cosmetic cleanup only, no behavior change.
- **2026-08-10 — No browser popups (DONE, M7 round 6):** user request: "when deleting a document it gives a broswer pop up i dont want that it should be there somewhere". `window.confirm()` is gone from the whole app: deleting a document is now a two-step IN-APP confirm inside the card (Delete → red banner "Delete? JSON/HTML/PDF removed" → Delete/Cancel, card rings red while armed); the editor's "Reset practice" confirm is an in-app red banner under the toolbar; LibraryList's three `alert()` failure sites became an inline error banner.
- **2026-08-10 — Home shows only the 10 most recent + Library link (DONE, M7 round 6):** user request: "on home page it shows the recent one which includes the updated ones too..its good only show 10 there and a link to library". `app/page.tsx` slices to `RECENT_LIMIT = 10` (listDocuments is already updatedAt-desc) and LibraryList's `recent` mode hides sort/tags/backup controls, showing a "View all N documents in the Library →" link whenever documents exist.
- **2026-08-10 — Library page with folders (DONE, M7 round 6):** user request: "make a library page It will show all the documents ...option for making folder too etc". `/library` is now a real page (was a redirect): every document + a folder chip bar (create / rename ✎ / delete 🗑 — deleting a folder only unfiles its documents, never deletes them), per-card "Move to…" select, folder + "No folder" filters. Storage: `Folder` type + optional `Document.folderId`, folder CRUD on the StorageBackend (FS `data/folders.json`, Mongo `folders` collection), `/api/folders` + `/api/folders/[id]`, `PATCH /api/documents/[id]` for moving (updatedAt untouched — moving isn't editing).
- **2026-08-10 — Editor column widened to max-w-6xl (DONE, M7 round 6):** user request: "there is still a lot of empty space around the paragraphand questions etc fix it too". The M7 round 4 widening (max-w-4xl) wasn't enough — now 1152px; the remaining desktop gutters around block fields are gone (phone full-width untouched).
- **2026-08-10 — Library follow-up ideas (after the folders batch):** (1) drag-and-drop documents onto folder chips instead of the select; (2) a search box on the library page (title/tag search); (3) include `folders.json` in the backup zip so a restore recreates folder structure; (4) folder emoji/color picker for the chips; (5) bulk actions (select several documents → move/delete as a group).
- **2026-08-10 — Folder UX: home/library card parity + auto-filing (DONE, M7 round 6b):** user report: "folder thing is not working", clarified as "folder shows as a tag or something and when i click on it and make a document in there it doesn't automatically move it there but i have to go in library and then choose to move that document in that folder... Document card should be the same on the home and the library page". The folder bar + per-card "Move to…" select now render on the HOME page too (identical cards); home passes the full document list with `limit=10` (chips/counts describe everything, the grid shows 10); with a folder selected, "+ New document in 'name'" files the new document from its first save (`NewDocumentButton` gained `folderId` + `label` props). Follow-up ideas: drag-and-drop onto chips (already in the library ideas list), a folder picker in the editor toolbar itself, and showing the folder filter state in the page URL (`/library?folder=<id>`) so it survives reloads.
- **2026-08-10 — Regenerate removed from the card, replaced by a download icon (DONE, M7 round 6b):** user request: "there should not be option for regenerate on the document card....add download icon which will download the document with all fields available in it on card". The ↻ Regenerate button/state is gone from LibraryList (the `/api/documents/[id]/regenerate` route stays as a server API; the editor keeps its regenerate affordance); each card now has ⬇ which downloads the document as `<title>.json` with EVERY field — title, tags, folder, all blocks (translations/analyses/vocab/expressions/practice answers/suggestions), practice settings (the PDF "full" variant omits practice answers, so JSON is the only complete artifact). Follow-up idea: a "download as PDF" icon alongside it if the user wants a printable file from the card too.
- **2026-08-10 — Library link back in the editor navbar (DONE, M7 round 6b):** user request: "add library on navbar too". Round 4 removed the toolbar's Library nav link (library was home then); with a real /library page + folders, the title row now carries a "Library" text link (→ /library) beside the Writer brand. The brand icon still goes home; Instructions stays on the home screen per round 4.
